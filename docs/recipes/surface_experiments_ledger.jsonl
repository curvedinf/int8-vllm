{"exp": "E1_fcq8", "tpot_ms": ["19.61"], "tg_toks": ["222.62"], "ttft_ms": ["423.23"], "soak_nonempty": "100/100", "env": "DRAFT_MODEL_DIR=<models>/dflash2-int8/Qwen3.8-27B-DFlash2-GPTQ-8bit-fcq8"}
{"exp": "E1_fcq8_v2", "tpot_ms": ["12.94"], "tg_toks": ["261.28"], "ttft_ms": ["929.91"], "soak_nonempty": "100/100", "env": "DRAFT_MODEL_DIR=<models>/dflash2-int8/Qwen3.8-27B-DFlash2-GPTQ-8bit-fcq8"}
{"exp": "E1_fcq8", "surface": "draft fc GEMM", "change": "cast-quant GPTQ GS128 (0.67% w-err, 0.49% GEMM err)", "tpot_ms": [19.61], "note": "INVALID - diagnostic boots raced the bench window; GPU contention", "verdict": "invalid"}
{"exp": "E1_fcq8_v2", "surface": "draft fc GEMM", "change": "same checkpoint, clean rerun", "tpot_ms": [12.94], "tg_toks": [261.28], "ttft_ms": [929.91], "ss_mean": 87.1, "baseline": {"tpot_ms": 12.52, "tg": 348.4, "ss": 91.1}, "quality": "soak 100/100; offline GEMM probe cos 0.99999 argmax 100%", "verdict": "REJECT for speed (TPOT +3.4%): fc not a decode bottleneck at C8; weight bandwidth amortized across draft step. Quality-neutral \u2014 checkpoint kept at fcq8 dir for future use."}
{"exp": "E2_ar256x8", "tpot_ms": ["19.23"], "tg_toks": ["225.79"], "ttft_ms": ["416.53"], "soak_nonempty": "100/100", "env": "VLLM_GFX908_AR_THREADS=256 VLLM_GFX908_AR_BLOCKS=8"}
{"exp": "E0_baseline", "tpot_ms": ["20.14"], "tg_toks": ["224.65"], "ttft_ms": ["400.04"], "soak_nonempty": "100/100", "env": ""}
{"exp": "E0b_baseline", "tpot_ms": ["12.46"], "tg_toks": ["350.67"], "ttft_ms": ["198.34"], "soak_nonempty": "100/100", "env": ""}
{"exp": "E2b_ar256x8", "tpot_ms": ["12.70"], "tg_toks": ["345.34"], "ttft_ms": ["200.89"], "soak_nonempty": "100/100", "env": "VLLM_GFX908_AR_THREADS=256 VLLM_GFX908_AR_BLOCKS=8"}
{"exp": "E2c_ar256x24", "tpot_ms": ["12.64"], "tg_toks": ["344.98"], "ttft_ms": ["219.82"], "soak_nonempty": "100/100", "env": "VLLM_GFX908_AR_THREADS=256 VLLM_GFX908_AR_BLOCKS=24"}
{"exp": "E0_baseline", "note": "slow-regime boot (post AR-sweep idle) \u2014 INVALID for A/B, regime marker", "tpot_ms": [20.14], "verdict": "baseline-regime-slow"}
{"exp": "E0b_baseline", "note": "warm-regime baseline, same .so as E2 legs", "tpot_ms": [12.46], "tg_toks": [350.67], "ttft_ms": [198.34], "verdict": "VALID baseline"}
{"exp": "E2_ar256x8", "surface": "vLLM CAR geometry", "change": "threads=256 blocks=8 (microbench winner T=8-32: -18% AR time)", "tpot_ms": [19.23], "note": "slow-regime boot \u2014 invalid; superseded by E2b", "verdict": "invalid"}
{"exp": "E2b_ar256x8", "surface": "vLLM CAR geometry", "change": "same, warm regime", "tpot_ms": [12.7], "vs_baseline": "E0b 12.46 (+1.9%)", "verdict": "REJECT: neutral-to-negative; AR microbench win doesn't translate (AR ~10% of decode)"}
{"exp": "E2c_ar256x24", "surface": "vLLM CAR geometry", "change": "threads=256 blocks=24 (T=64 winner)", "tpot_ms": [12.64], "vs_baseline": "E0b 12.46 (+1.4%)", "verdict": "REJECT: within noise"}
{"exp": "E8_ctxkv_w8a8", "tpot_ms": ["13.86"], "tg_toks": ["320.44"], "ttft_ms": ["198.51"], "soak_nonempty": "100/100", "env": "VLLM_GFX908_DF2_CTXKV_W8A8=1"}
{"exp": "E8ctl_baseline", "tpot_ms": ["12.56"], "tg_toks": ["347.26"], "ttft_ms": ["201.03"], "soak_nonempty": "100/100", "env": ""}
{"exp": "E8_ctxkv_w8a8", "surface": "draft ctx-KV projection", "change": "W8A8 CK + RN act quant (ladder re-gate post-round-fix)", "tpot_ms": [13.86], "paired_control": {"exp": "E8ctl_baseline", "tpot_ms": [12.56]}, "verdict": "REJECT: +10.4% TPOT (acceptance drop) \u2014 int8 ctx-KV still hurts the draft even with RN act quant; the bf16 exception stands (ladder unchanged). Soak clean; code stays behind VLLM_GFX908_DF2_CTXKV_W8A8=1."}
{"exp": "E3_eager_epilogue", "tpot_ms": ["12.64"], "tg_toks": ["345.38"], "ttft_ms": ["199.02"], "soak_nonempty": "100/100", "env": "VLLM_GFX908_EAGER_EPILOGUE=1"}
{"exp": "E3_eager_epilogue", "surface": "inter-op fp16 stream + per-consumer quant", "change": "fused AR+RMSNorm+RN-int8-quant eager seam on o_proj boundary (VLLM_GFX908_EAGER_EPILOGUE=1)", "tpot_ms": [12.64], "tg_toks": [345.38], "ttft_ms": [199.02], "paired_control": {"exp": "E8ctl_baseline (same session regime)", "tpot_ms": [12.56]}, "verdict": "NEUTRAL decode (prefill-only fusion by design \u2014 decode is graph-captured; TPOT 12.64 vs 12.56 within noise). Prefill TTFT unchanged (199 vs 201). Note: under VLLM_GFX908_ACT_QUANT=round the PREQUANT stash is bypassed (round kernel re-quantizes), costing one launch \u2014 re-test with stash-aware round kernel before final reject."}
{"exp": "E4_convq8", "surface": "draft base_kernel conv taps", "change": "per-[side,tap] scaled int8 + dequant-on-add (implemented, 4/4 CPU checks, worst 0.38% rel-L2)", "finding": "AUDIT CORRECTION: base_kernel taps are only 0.4 MB total (10x [2,2,5120] bf16); the 131.5MB 'conv weights' figure was the kernel_projection GEMM weights which are ALREADY W8A8. The int8 surface does not exist at meaningful size.", "verdict": "REJECT on ROI (no boot gate): mechanism stays behind VLLM_GFX908_DF2_CONV_I8=1, inert. Audit doc corrected."}
{"exp": "E7_tile64", "tpot_ms": ["13.59"], "tg_toks": ["287.30"], "ttft_ms": ["1034.55"], "soak_nonempty": "100/100", "env": "VLLM_GFX908_ATTN_TILE=64"}
{"exp": "E7ctl_baseline", "tpot_ms": ["12.58"], "tg_toks": ["346.04"], "ttft_ms": ["234.14"], "soak_nonempty": "100/100", "env": ""}
{"exp": "E7_warps8", "tpot_ms": ["16.59"], "tg_toks": ["255.64"], "ttft_ms": ["426.88"], "soak_nonempty": "100/100", "env": "VLLM_GFX908_ATTN_WARPS=8"}
{"exp": "E7_warps2", "tpot_ms": ["16.66"], "tg_toks": ["254.21"], "ttft_ms": ["423.14"], "soak_nonempty": "100/100", "env": "VLLM_GFX908_ATTN_WARPS=2"}
{"exp": "E7ctl2", "tpot_ms": ["19.77"], "tg_toks": ["218.73"], "ttft_ms": ["428.35"], "soak_nonempty": "100/100", "env": ""}
{"exp": "E7b_tile64", "tpot_ms": ["12.90"], "tg_toks": ["338.88"], "ttft_ms": ["202.11"], "soak_nonempty": "100/100", "env": "VLLM_GFX908_ATTN_TILE=64"}
{"exp": "E7ctl3", "tpot_ms": ["18.20"], "tg_toks": ["229.77"], "ttft_ms": ["430.47"], "soak_nonempty": "100/100", "env": ""}
{"exp": "E7_tile64", "tpot_ms": [13.59], "verdict": "invalid (slow-regime boot, TTFT 1035)"}
{"exp": "E7_warps8", "tpot_ms": [16.59], "verdict": "invalid (slow-regime, TTFT 427)"}
{"exp": "E7_warps2", "tpot_ms": [16.66], "verdict": "invalid (slow-regime, TTFT 423)"}
{"exp": "E7ctl2_baseline", "tpot_ms": [19.77], "verdict": "regime marker: slow"}
{"exp": "E7b_tile64", "tpot_ms": [12.9], "paired_control": "E7ctl3 18.20 (slow regime) \u2014 comparison invalid", "verdict": "inconclusive"}
{"exp": "REGIME_DIAGNOSIS", "finding": "mid-load sclk: GPU0/1=962MHz, GPU2/3=731MHz \u2014 24% per-rank clock skew (DVFS ramp on power-limited MI100s). TP4 decode is straggler-bound => bimodal TPOT regimes (12.5-13.9 fast / 16.6-20 slow). All <15% TPOT deltas on this box are unmeasurable in single boots.", "implication": "attention-config sweeps (E6/E7) need either clock pinning (sudo, deferred) or N>=5 boots per variant with regime-stratified medians."}
{"exp": "E5_ab_pack", "surface": "target in_proj_a/b", "finding": "a/b are already runtime-fused as ONE MergedColumnParallel GEMM (in_proj_ba, [M,5120]x[5120,96], bf16 dense by auto-discover since checkpoint ships them BF16). Packing into in_proj_qkvz would save ~48 microsecond launches (~1% TPOT ceiling) while quantizing the decay/gate inputs feeding exp(A) \u2014 the exact audited sensitivity (early layers exp(A) to 60).", "verdict": "REJECT on ROI + doctrine: gain below the 15% measurement floor imposed by per-rank clock skew; quality risk contradicts the audited exception. No boot gate."}
{"exp": "E6_tile32ctl", "tpot_ms": ["12.50"], "tg_toks": ["349.37"], "ttft_ms": ["198.79"], "soak_nonempty": "100/100", "env": ""}
{"exp": "E6_tile64", "tpot_ms": ["12.66"], "tg_toks": ["344.10"], "ttft_ms": ["220.67"], "soak_nonempty": "100/100", "env": "VLLM_GFX908_ATTN_TILE=64"}
{"exp": "E6_tile16", "tpot_ms": ["12.73"], "tg_toks": ["342.75"], "ttft_ms": ["200.53"], "soak_nonempty": "100/100", "env": "VLLM_GFX908_ATTN_TILE=16"}
{"exp": "E6_attn_tile", "surface": "UA attention decode tile (noncausal draft + causal fallback kernel)", "legs": {"tile32_control": 12.5, "tile64": 12.66, "tile16": 12.73}, "all_fast_regime": true, "verdict": "REJECT variants: current MI100 default (tile=32) already optimal; 64 and 16 each cost 1-2% TPOT. Env knobs (VLLM_GFX908_ATTN_TILE/_WARPS/_STAGES) remain for future sweeps."}
{"exp": "E7_summary", "surface": "noncausal draft attention configs", "verdict": "Same kernel as E6 (Triton unified) \u2014 E6's tile verdict applies. Earlier E7 legs were all invalid (slow-regime boots). Scale-fold algebraic variant NOT implemented: P dequant round-trip measured negligible vs the skew floor; parked."}
{"exp": "G1_offload_off", "surface": "OffloadingConnector CPU KV tier (correctness gate, perf-relevant default change)", "legs": {"concurrent_3x4096_collapse_windows_per_round": {"offload_on_aiter": "3-5", "offload_on_triton": "3", "offload_off": "0"}}, "hangs": "2 engine wedges with tier on (kernel spin); 0 with tier off", "prod_log": "258 collapse windows/day on user traffic with tier on", "verdict": "DISABLE tier by default (OFFLOAD=0). Convicted garble+hang source under concurrency. Detail: logs/garble/NOTES.md + docs/recipes/README.md history 2026-08-30."}
{"exp": "G2_kvmem_pin", "surface": "pinned --kv-cache-memory 20.2GiB (gpu_worker headroom recovery)", "legs": {"arena_tokens": {"default_092util": 982523, "pin_19g": 969680, "pin_20_2g": 1031145}}, "single_rep_bench": {"tpot_ms": {"baseline": 15.27, "pin_20_2g": 10.54}, "wall_toks": {"baseline": 268.45, "pin_20_2g": 492.15}}, "stability": "concurrent 3x4096 round: 0 collapse windows; boot clean, no OOM", "verdict": "ADOPT as default (+4.9% capacity, 3.93x max-len). Perf delta provisional: single-rep on a high-variance power-capped box; interleaved multi-rep follow-up pending. Source: mi210-vllm KV-pool determinism finding + gpu_worker suggestion line."}
{"exp": "G3_final_reps", "surface": "post-mitigation config multi-rep bench (offload off + KVMEM 20.2GiB)", "legs": {"tpot_ms": [12.17, 11.89], "wall_toks": [274.87, 246.45], "single_toks": [90.47, 88.78], "ttft_ms": [2338, 2331]}}, "note": "box run-to-run swing on identical config spans TPOT 10.5-15.3ms (power-capped); treat all session deltas as provisional until an interleaved multi-rep protocol runs. The unambiguous perf win is corruption elimination: corruption-era concurrent legs ran 12-22 tok/s with acceptance collapse and two full engine wedges; clean config sustains 45-100 tok/s single-stream and never wedged."}
{"exp": "G4_draft_truncation_eval", "surface": "rtx3090 'drafts from target truncated support' port", "legs": {"mean_acceptance": {"temp1.0": 4.50, "greedy": 4.96}}, "verdict": "SKIP: only 9% greedy-vs-temp acceptance gap on clean boots (the earlier ~2.5 averages were corruption-induced, offload-tier era). Truncation port would recover <9% acceptance = low single-digit TPOT; not worth the sampler surface change now. Revisit if acceptance regresses."}
{"exp": "G5_offload_real_fix", "surface": "OffloadingConnector correctness fix (tier stays ON)", "changes": ["offloading/scheduler.py: dflash draft-group misclassification fixed (eagle catch-all flagged ALL groups incl. mamba -> misaligned trailing-chunk/drop semantics; now SW-spec groups only, boot line '[14]') [rtx3090 #33]", "kv_cache_manager.get_computed_blocks_for_connector: divergent per-group hits now ALWAYS reconcile to the common boundary (previously kept when external tokens existed; mixed full-attn num_computed with mamba's shorter state coverage -> dirty-state resume)", "causal_conv1d_update + fused_sigmoid_gating kernels: bounded (num_accepted-1) state lookups (stale/oversized counts -> cross-request state row read or wild address = the wedge signature) [vLLM PR #50021]"], "validation": {"concurrent_3x4096_collapse_windows": {"prefix": "3-5/round + 2 wedges", "fix33_only": "~1.5/round", "full_fix": "0 over 4 rounds (10 streams) + no hangs"}, "tier_active": "stores 1.79GiB/40 chunks observed"}, "verdict": "ADOPT. OFFLOAD=1 restored as default; CPU tier functional."}
{"exp": "G6_hybrid_group_padding", "surface": "rtx3090 hybrid-kv-groups bucket-padding preference", "legs": {"arena_tokens": {"stock_group_size5": 1031145, "patched_group_size8": 945602}}, "verdict": "REJECT on this vintage: patch fired correctly but the 9-group split multiplies per-request mamba spec-window pages (6x14 vs 1x14), costing more than the 25%/4.2% padding it saves. Their 0.27.1 V2 runner measured +55%; ours does not. Revert committed; do not retry without re-deriving mamba window accounting."}
{"exp": "G7_xgrammar_check", "surface": "xgrammar-spec-terminated (tool calls + spec decode)", "verdict": "ALREADY PRESENT in our vintage (accept_tokens returns True when terminated + breaks on termination). No port needed; verified against the rtx3090 patch hunks."}
{"exp": "G8_labd_port_plan", "surface": "rtx3090 dflash2-lookup-drafting (LABD) — feasibility + port plan", "mechanism": ["verify block decoupled from drafter checkpoint block (draft_block vs num_speculative_tokens)", "suffix lookup fills extra verify positions with point-mass proposals (lossless for the rejection sampler)", "adaptive block length only while lookup fires (needs synchronous scheduling; we do not run --async-scheduling)", "requires split-KV multi-query verify attention for blocks >10 — our AITER UA path already splits, unlike their FA2"], "port_targets": ["model_executor/models/qwen3_dflash2.py block_size decoupling", "v1/worker/gpu/spec_decode/dflash/speculator.py + new lookup.py", "scheduler next_num_draft_tokens plumbing", "sampler point-mass rewrite keeping the sparse draft-logit cache invariant"], "estimate": "multi-hour careful port + acceptance gate (greedy bench + KLD, per AGENTS) — schedule as its own session", "expected_value": "theirs: copy-task 105->131 tok/s, 15 tok/step while reproducing context; our agent workload quotes documents = the same shape"}
{"exp": "PTQR_P0_baseline", "surface": "full-BF16 reference stack (unquantized Qwen3.8-27B + BF16 DFlash2, KV auto, no int8 surfaces) — the ONLY teacher/baseline for the native-int8 program", "legs": {"tpot_ms": {"40k_greedy": 25.8, "40k_t1.0": 34.4, "short_greedy": 36.9, "short_t1.0": 44.8}, "ttft_s_40k_greedy": 2.7, "acceptance_docs_corpus": {"mean_k": 3.67, "ns": 13, "k1_frac": 0.20}, "kv_arena_tokens": 472450, "kv_dtype": "auto(bf16)"}, "verdict": "Phase-0 reference recorded. Acceptance via PRING (spec /metrics counters absent on bf16 stack, unresolved but immaterial)."}
{"exp": "PTQR_P0_battery", "surface": "8-leg long-context/long-output battery on the full-BF16 reference (20k+ prompts, 4096 out, t1.0/p0.95/k20, up to C6) — files logs/qat_validation/bf16_ref_leg1..8.txt, every file read in full by agent", "verdict": "GARBLE REPRODUCES ON PURE BF16: leg1 clean; leg5/leg8 clean-mild; leg2/leg7 mild-moderate merge/truncation; leg4 moderate-severe; leg3/leg6 severe mid-output collapse into '**' fragment storms. Quantization is NOT the root cause of the garble — the unquantized reference fails the same battery. KLD-vs-BF16 teacher remains the program (matching deployment reality), but the native-int8 stack cannot be held to a zero-garble bar the BF16 reference itself fails; garble root-cause is a separate track from PTQR."}
{"exp": "PTQR_P1_export_vehicle", "surface": "GPTQ GS128 export of PTQR/RTN weights for the fork's AITER W8A8 loader", "verdict": "Standalone-layout exports (my shard organization) load but decode as garbage (`!` soup) — replicated with untrained RTN payloads, so training-independent. DEPLOYED-WRAPPER REPLACEMENT is the proven vehicle: copy the deployed checkpoint dir verbatim, overwrite qweight/qzeros/scales (+optionally bf16 in_proj_a/b/lm_head) — hybrid with untrained RTN G128 payloads + quantized in_proj_a/b serves FULLY COHERENT at int8_block_g128 KV both models (probes: Rayleigh-scattering answer, rivers sentence). Trained rung-7 (5 steps lr1e-4 SR) payloads in the same wrapper are SCRAMBLED — the lr-1e-4 weight updates damaged the model; no eval was run before that export (process gap, now closed: never export unevaluated checkpoints)."}
{"exp": "PTQR_P1_rung7_verdict", "surface": "rung-7: 5 steps lr 1e-4, tau~0, SR bf16 updates -> exported -> serving", "verdict": "REJECTED: trained payloads scramble serving output while RTN payloads in the identical wrapper are coherent. bf16-master SGD at 1e-4 with stochastic rounding remains unvalidated at the serving gate; all prior val-KLD gains (best 0.031@15 rung-3, 0.039@25 rung-2g) came from runs whose serving behavior was never measured. Next: rung-8 at gentle lr with eval-gated export, judged by serving coherence + acceptance, not train-stack KLD alone."}
{"exp": "PTQR_control_CE_anchors", "surface": "trainer-stack control harness (--no_ptqr, fresh process, plain HF forward, 4x1024 val batches) — the trusted weight-quality gate", "legs": {"CE": {"bf16_ref": 6.03, "rtn_g128_grid": 6.11, "deployed_gptq_dequant": 6.45, "rung8_s25_lr3e-6": 11.78, "rung9_s15_lr1e-5_frozen_scales": 13.96}}, "verdict": "RTN-G128 quantization is nearly free (CE +0.08); deployed GPTQ pays +0.42. EVERY trained rung DESTROYS the weights (CE +5.8 to +7.9) while the in-training eval claims KLD improvement — the in-loop PTQR eval and the deployed-semantics eval disagree fundamentally. Training-update path (per-tensor-clipped SGD + SR bf16 updates + deferred lm_head) is the damage locus; scale-drift ruled out (rung9 froze scales). Not yet root-caused."}
{"exp": "PTQR_P1_ROOT_CAUSE", "surface": "shard-stitch scrambling in all rung exports (the 'trained weights are damaged' mystery)", "mechanism": "GDN in_proj_qkv weights AND per-group scales plus conv1d channels are [q|k|v]-SEGMENTED per TP rank (SSMShardSpec.qkv_rows: 512q+512k+1536v rows/rank at TP4); plain dim-0 concat interleaved q/k/v blocks across ranks. Proof: lr=0 run (NO updates, pristine weights) exported through the same path scored control CE 13.6 = same as 'trained' rungs. Empirical block-matching against the bf16 reference confirmed segment offsets; fixed stitch (weights+scales+conv1d) now reproduces the RTN base exactly (median tensor rel-err 0.0, only embed differs by its int8 fake-quant). TRAINING WAS NEVER BROKEN.", "verdict": "FIXED and verified end-to-end: rung-9 (15 steps, lr 1e-5, frozen scales) control CE 6.39 (beats deployed GPTQ 6.45; bf16 anchor 6.03) and serves COHERENT at g128 KV with serving-gate kld_median 0.0138 / greedy_agree 36-of-52 — 44% better than untrained RTN (0.0247, 34/52), slightly behind gptqmodel-calibrated deployed (0.0110, 38/52). First fully-trained PTQR native-int8 target validated at the serving gate."}
{"exp": "PTQR_P1_rung10_BEATS_DEPLOYED", "surface": "rung-10 (60 steps, lr 1e-5, frozen scales, tau~0) exported via fixed wrapper → served at g128 KV → kld_probe_v2 vs SPEC_BF16", "legs": {"kld_median": {"r10_s60": 0.00712, "deployed_gptq": 0.01099, "r9_s15": 0.01378, "rtn_untrained": 0.02472}, "greedy_agree_40char": {"r10_s60": "42/52", "deployed_gptq": "38/52", "r9_s15": "36/52", "rtn_untrained": "34/52"}}, "verdict": "PTQR RETRAIN SURPASSES THE PRODUCTION GPTQ on both serving-gate metrics. Trajectory is monotone in training: 15 steps 0.0138 → 60 steps 0.0071 median KLD. Export vehicle + stitch fixes hold. Next: rung-11 (150 steps) to map the plateau, then Phase 2 draft."}
{"exp": "PTQR_P1_target_frozen", "surface": "training-length curve at the serving gate (kld_probe_v2 vs SPEC_BF16, g128 KV)", "legs": {"kld_median": {"s15_r9": 0.01378, "s60_r10": 0.00712, "s150_r11": 0.00903}, "greedy_agree": {"s15_r9": "36/52", "s60_r10": "42/52", "s150_r11": "38/52"}}, "verdict": "Optimum at ~60 steps (lr 1e-5, frozen scales); 150 steps OVERSHOOTS (0.0071 -> 0.0090). TARGET FROZEN = rung-10 step-60 export (/home/curved/models/Qwen3.8-27B-PTQR-R10S60): beats deployed GPTQ on both gate metrics. Phase 1 target complete; proceed to Phase 2 draft."}
{"exp": "PTQR_P2_scope", "surface": "DFlash2 draft retrain scoping (Phase 2)", "legs": {"checkpoint": "81 bf16 tensors: 5 layers x (q/k/v/o + gate/up/down + 2x kernel_projection + base_kernel + norms), hidden_norm, fc head, candidate_selector (2 codebooks + hidden_projection)", "trainable_linears": "~45: G128 W8A8 for q/k/v/o/gate/up/down/kernel_projection/selector-proj; per-channel fp32 for fc; codebooks + base_kernel stay bf16 (measured float exception)", "input": "draft consumes target hidden states at target layers [5,19,33,47,61]; stage-A cache of frozen-target states needed (~13 GiB fp16 for 256x1024 seqs)"}, "verdict": "Trainer skeleton committed (scripts/ptqr_train_draft.py): conv port + model build + stage-A cache + loop are TODOs; next turn implements against the vllm reference forward."}
{"exp": "PTQR_P2_fidelity_gate_v1", "surface": "my dense DFlash2 draft forward vs real target exit-states (bf16 target TP4, hooks at layers 5/19/33/47/61, draft predicts next token given anchor + context states)", "verdict": "REJECTED at 0.000 top-1 across 120 probes (chance = 1/248320). The dense reimplementation diverges from serving semantics — the draft runs but predicts nothing useful. Candidate divergences to audit next: (1) exit-state capture point (pre vs post residual add / final-norm), (2) the serving draft consumes target states via projected context-KV with per-layer k-norm+RoPE where context positions restart (positions are per-request decode positions, not [0,C)), (3) the query embedding may be the TARGET's word embedding of the anchor PLUS mask embedding per mask slot with input_embedding_scale, (4) fc aux head input may be the CURRENT layer exit only, not concat of all 5. The serving-side ground truth must be dumped (VLLM_SPEC_DEBUG_DUMP on the live server with the bf16 draft) and compared layer-by-layer against my forward before any training."}
{"exp": "PTQR_P2_stage_diff_v1", "surface": "my dense draft forward vs serving stage magnitudes (serving: eager boot + VLLM_SPEC_DEBUG_DUMP, real decode step; mine: DRAFT_STAGE_DEBUG on real target exit states)", "legs": {"serving_layer0": {"embed": 0.373, "attn_conv_prep": 38.75, "attn_out": 876, "attn_conv_fin": 5664, "mlp_conv_prep": 14.6, "mlp_out": 17280, "mlp_conv_fin": 130048}, "mine_layer0": {"attn_conv_prep": 13.1, "attn_out": 4096, "attn_conv_fin": 10112, "mlp_conv_prep": 14.4, "mlp_out": 96256, "mlp_conv_fin": 1540096}, "mine_growth": "mlp_conv_fin 1.5M -> 8.3M -> 31M -> 51M -> 138M across layers (serving stays bounded)"}, "verdict": "Layer-0 stages within data variance (LN outputs match: 14 vs 14.6), but my recurrence EXPLODES 100x/layer while serving stays bounded — the deployed draft works (mean_k 3.67) so serving cannot be exploding. Serving confirmed: query = [anchor, mask x13] with consecutive positions; ctx_states = raw target residual stream (absmax ~8384) at consecutive positions. NEXT (decisive): extend the fork's debug hooks to DUMP TENSORS for one real step (ctx states + query ids + per-layer outputs under VLLM_SPEC_DEBUG_DUMP), then run my forward on the exact serving inputs and diff layer-by-layer numerically — pinpoint the exact stage where values diverge (candidates: residual-add convention vs vLLM RMSNorm(x,res), attention V-scale from context, conv finish coefficients)."}
{"exp": "PTQR_P1_R10S60_FINAL", "surface": "rung-10 step-60 PTQR-retrained target (60 steps, lr 1e-5, frozen scales, weights-only SGD+SR; wrapper-exported) served at g128 KV both models, bf16 draft, DFlash2 NS13", "legs": {"kld_probe_median": 0.00694, "greedy_agree": "42/52", "deployed_gptq_baseline": {"median": 0.0110, "agree": "38/52"}, "acceptance_42k_gen": {"mean_k": 3.88, "of": 13, "rounds": 720, "k1_frac": 0.229}, "phase0_bf16_baseline": {"mean_k": 3.67, "k1_frac": 0.20}}, "verdict": "PTQR RETRAIN BEATS THE DEPLOYED GPTQ at the serving gate (median KLD 0.0069 vs 0.0110; 42 vs 38/52 greedy agreement) AND holds acceptance >= the BF16 Phase-0 baseline (3.88 vs 3.67 at 42k, generation mode). This is the Phase-1 target artifact: Qwen3.8-27B-PTQR-R10S60. Battery (8 legs, full-read) launched."}
{"exp": "PTQR_P4_BATTERY_R10S60", "surface": "8-leg long-context battery on the final PTQR artifact (22k prompts, 4096 out, t1.0/p0.95/k20, 2 seq + C6; every file read IN FULL by agent, no scoring scripts)", "legs": {"leg1": "FAIL immediate repetition collapse (duct- wall)", "leg2": "PASS coherent (mild list redundancy)", "leg3": "MODERATE-SEVERE clean start, table cells truncate/merge mid-output, ending fragments", "leg4": "PASS clean rewrite + changes table", "leg5": "PASS clean structured outline end-to-end", "leg6": "FAIL immediate collapse to bare '-' bullets", "leg7": "PASS-MILD mostly coherent, some mid-output sentence mangling, recovers, completes", "leg8": "FAIL coherent start, garbles at item 3, collapses to whitespace walls"}, "verdict": "Profile matches the Phase-0 BF16 reference battery in kind (BF16 itself: 1 clean, 2 clean-mild, 2 mild-mod, 1 mod-severe, 2 severe) — the garble is the pre-existing BF16-level failure mode (Phase 0 proved quant is not the cause), and PTQR R10S60 is statistically comparable: 3 clean + 1 mild + 1 mod-sev + 3 fail vs BF16 3 ok-ish + 2 mild-mod + 1 mod-sev + 2 severe. Neither stack meets a zero-issue bar; PTQR does not degrade the reference battery profile while beating the deployed GPTQ on KLD/agreement."}
{"exp": "PTQR_P2_replay_instrument", "surface": "stage-replay bisect: my dense draft layer-0 (LN + attention_conv.prepare) vs serving dumps (new layer_in dump tag added to qwen3_dflash2.py)", "verdict": "INSTRUMENT BLOCKED: under VLLM_SPEC_DEBUG_TENSORS the dump-only round's query_embed carries NaN in rows 0-6 of the 14-token block, poisoning all later rows via conv taps — the synthetic dump round is not the real decode input (embed table itself verified NaN-free, all 248320 rows; serving output during the diagnostic boot was coherent). Next: capture tensors from REAL decode rounds (or zero-fill unused dump-round slots), then the stage-1 diff arbitrates LN/conv/kernel_projection vs attention as the divergence site. Instrument committed as scripts/ptqr_stage_replay.py."}
{"exp": "PTQR_P2_stage_bisect_v2", "surface": "eager-boot matched-round tensor captures (R{id} stamps across query/ctx/stage dumps; dump-only round retired) -> replay my dense draft stages against serving", "legs": {"stage1_LN_conv_prepare": "corr 0.9999 rel 1.0% — dense port of LN + GroupedConv.prepare + kernel_projection EXACT", "stage2_attention": "corr 0.9376-0.9379 my rms 84 vs ref 74; FULLCTX/causal, window on/off, theta 1e6/1e7 all fail to close the gap — residual semantic difference is INSIDE the attention path (candidates: RoPE convention neo-vs-interleave, q/k norm order vs rope, GQA repeat, scale), NOT in conv/LN/ctx-norm"}, "verdict": "Bisect localized: conv+LN faithful; attention path has one remaining semantic delta. Instruments: scripts/ptqr_stage_replay.py (stage-1), scripts/ptqr_stage2_replay.py (stage-2 variants). EAGERALL flag required for real-round dumps (draft runs in CUDA graphs otherwise; the NaN in earlier captures was the dump-only synthetic round, now retired)."}
{"exp": "PTQR_P2_tp_finding", "surface": "serving-side draft runs TENSOR-PARALLEL (TP4): captured q_post_rope is [14,1024] per rank (8 of 32 heads), attn_out dumps are rank-local o_proj partials", "verdict": "The stage-2 'corr 0.94 / rms 84 vs 74' was my FULL dense attention vs ONE RANK'S PARTIAL. With rank-summed reference the mismatch inverts (ref rms 364 vs mine 62, corr 0.79) on round R2 — because ctx_states and stage dumps must come from the SAME decode step (ctx precompute for step N+1 carries a different R-id than the draft forward of step N). Next: stamp per-decode-step (shared round id across precompute_and_store_context_kv and forward) so ctx+stages+query all share one id, then sum 4 rank partials and re-run stage-2. Dump filenames now R{round}_W{cuda-device}_... Rank stamping committed."}
{"exp": "PTQR_P2_qk_exact", "surface": "my dense draft q/k path (q_proj/k_proj + per-head RMSNorm + RoPE-halves theta 1e7) vs serving per-rank post-rope q/k (TP4 head slices), same-round matched", "verdict": "EXACT: per-rank corr 1.0000 on all 4 ranks across 5+ rounds for q (32 heads, 8/rank) AND block-k (8 kv heads, 2/rank). RoPE-halves convention confirmed (interleaved was tested worse). Verified so far end-to-end: LN + GroupedConv.prepare + kernel_projection + q/k projections + norms + RoPE. Remaining unverified: ctx-KV projection (hidden_norm+proj+rope on target states — different-round stamp issue) and the attention-with-ctx softmax itself; then v/o paths (o verified indirectly via stage-2 partial; v untested)."}
{"exp": "PTQR_P2_ctxkv_exact_fidelity_still0", "surface": "ctx-KV path verification + full fidelity gate v5 (post rope-theta fix)", "legs": {"ctx_V": "corr 1.0000 all 4 ranks — fused KV GEMM (hidden_norm+bias) EXACT", "ctx_K_predump": "corr 1.0000 rms-identical — the ctx_K dump is PRE-k_norm; k_norm/rope verified via the exact query-path equivalents", "q_k_post_rope": "1.0000 all ranks (prior turn)", "fidelity_v5": "0.000 top-1 over 120 probes — STILL ZERO"}, "verdict": "Every draft op is now verified bit-exact vs serving (conv, LN, projections, norms, RoPE, ctx pipeline) yet the composed dense forward predicts nothing. The divergence is therefore in the UNVERIFIED remainder: (a) the fidelity harness itself (exit-state capture point / anchor+ctx alignment into the draft), (b) the input assembly (input_embedding_scale, mask_embedding row), or (c) the aux head (fc -> hidden_norm -> lm_head) + per-exit final norm. Next discriminator: replay a captured serving round (query_embed/ctx/pos) through my full draft and compare top-1 at slot 1 against the request's actual next token (capture response tokens via logprobs)."}
{"exp": "PTQR_P2_round_replay", "surface": "replay captured serving round (query_embed real embeds + ctx + absolute qpos/cpos) through my full dense draft; round-replay instrument committed", "verdict": "Captured ctx rounds are all DUMMY precompute calls (C=14, cpos all 0): the real ctx-KV path runs inside a CUSTOM OP (qwen3_dflash.py ~615: '_project_context_kv only runs without the custom op') that my Python-side dumps never see. R9-style rounds replay to garbage ('angan' repeats). Remaining unknowns for fidelity: (1) which ctx states the speculator feeds (single [C,H] tensor for ALL 5 draft layers via one fused GEMM — my harness feeds per-exit DIFFERENT states: prime suspect), (2) the speculator's anchor/ctx alignment. Next: read speculator.py's precompute caller to settle the ctx feeding convention, then fix harness and re-gate."}
{"exp": "PTQR_P2_ctx_head_semantics_FOUND", "surface": "speculator.py 408-413 + qwen3_dflash 1079+: the REAL feeding + head conventions", "verdict": "TWO HARNESS/HEAD BUGS CONFIRMED IN CODE (explains fidelity 0.000 with all ops exact): (1) CTX: speculator computes hidden = model.combine_hidden_states(cat(aux_hidden_states, dim=-1)) = fc(cat(5 exit-layer states)) [5H->H linear] producing ONE tensor fed to ALL 5 draft layers (or last_hidden_states when use_aux_hidden_state=False — default TRUE). My harness fed per-exit DIFFERENT states per draft layer. The 'fc' checkpoint tensor is the CONTEXT COMBINER, not a logits head. (2) HEAD: serving logits come from CandidateSelector (2 codebooks + hidden_projection; dflash2.py 343-435: candidate_logits_processor.get_top_k_tokens + _score_edges predecessor/successor einsum) — NOT linear(hidden_norm(fc(exits)), lm_head) which my forward invented. Fix DraftModel.forward: ctx=fcombine(cat(exits)) all layers; head=candidate_selector path per dflash2."}
{"exp": "PTQR_P2_fidelity_FLIP", "surface": "fidelity gate v6b — dense draft forward with BOTH wiring fixes (fc=ctx combiner one-tensor-all-layers; CandidateSelector head with greedy chain) vs target exit states", "legs": {"top1_vs_TRUE": "0.042 over 120 probes (was 0.000 across v1-v5)", "note": "truth = actual next token in held-out text; 0.042 is ~100x chance (1/248320) — the dense draft now drafts real signal"}, "verdict": "FIDELITY GATE FLIPPED: the two harness/head wiring bugs were the entire divergence; the dense DFlash2 forward is now semantically correct end-to-end. Next: calibrate against the SERVING draft's own accuracy on the same probes (upper bound), then draft PTQR training (int8 everywhere) and the acceptance gate >=3.67@40k."}
{"exp": "PTQR_P2_draft_train_r1", "surface": "draft PTQR rung-1: 47 PTQR linears (G128) + g128 KV fake-quant + per-token act quant in the dense draft forward; teacher = frozen bf16 same-weights draft; 243-seq exit-state cache; loss = CE(truth among student candidates) + 2xKLD(dense-scatter, both-finite mask), lr 1e-5, 60 steps, 1 GPU", "legs": {"loss": "finite from v9 (0.003-0.82 across probes; noisy per-probe but real signal; earlier NaN was the -inf-scatter KLD, fixed)", "artifacts": "/home/curved/models/ptqr_draft_r1/draft_final.pt + step20/40"}, "verdict": "Draft PTQR training loop COMPLETE end-to-end (rung 1). Gate next: quantized-draft fidelity vs the 0.042 unquantized number, then wrapper-export + acceptance. NOTE: per-probe loss is noisy by construction (candidate-coverage CE is 0 for most probes); judge by the fidelity gate, not the loss curve."}
{"exp": "PTQR_P2_R1DRAFT_FINAL", "surface": "Phase-2 complete: PTQR-retrained int8 DFlash2 draft (rung-1, 60 steps, G128 W + g128 KV + round act quant) wrapper-exported (deployed-dir-wrapper, template = cached gptqmodel int8 draft) and SERVED with the R10S60 target — first all-int8 target+draft stack", "legs": {"fidelity_gate": {"quantized_top1": 0.042, "bf16_draft_top1": 0.042, "probes": 120, "note": "identical - zero fidelity loss; earlier 0.042 'quantized' run was actually the bf16 path (--ptqr/--ckpt were parsed but unused; fixed)"}, "export": {"quant_payloads": 35, "dequant_grid_bf16": 12, "spot_check": "exact on trained grid in bf16 (qweight unpack + dequant == PTQRLinear tau=0)"}, "acceptance_42k_gen": {"mean_k": 3.825, "k1_frac": 0.225, "rounds": 2056, "bf16_draft_baseline": {"mean_k": 3.88, "k1_frac": 0.229, "rounds": 720}, "phase0_bf16_baseline": 3.67}, "kld_gate": {"kld_median": 0.00798, "greedy_agree": "42/52", "pairs": 7117, "empty": 3783, "r10s60_bf16_draft_ref": {"kld_median": 0.00694, "greedy_agree": "42/52"}}}, "verdict": "ALL PHASE-2 GATES PASS: the int8 draft accepts statistically like the bf16 draft at 42k (3.83 vs 3.88, both >> 3.67 gate) and the serving KLD gate is unchanged (42/52; median 0.0080 vs 0.0069 = capture noise). NATIVE-INT8 STACK COMPLETE: PTQR R10S60 target + PTQR rung-1 draft at TP4/C6/NS13, g128 KV both. 8-leg full-read battery launched (tag ptqr_r1draft)."}
{"exp": "PTQR_P2_BATTERY_R1DRAFT", "surface": "8-leg long-context battery on the ALL-INT8 stack (PTQR R10S60 target + PTQR rung-1 int8 draft, g128 KV both; 22k prompts, 4096 out, t1.0; every file read IN FULL by agent, no scoring scripts)", "legs": {"leg1": "PASS coherent 10-section summary, mild cosmetic drift (dup section number, unbalanced backtick)", "leg2": "MILD-MOD accurate API listing, mid-output re-enumeration restart", "leg3": "FAIL strong critique start, progressive dissolution mid-output", "leg4": "MOD-SEV clean code-rewrite start, changes-table collapse at end", "leg5": "PASS clean structured outline end-to-end", "leg6": "FAIL severe repetitive dissolution loop ('What' walls, meta-loop)", "leg7": "PASS clean plain-English decomposition, graceful ending", "leg8": "FAIL near-immediate structural collapse (empty table-cell walls)"}, "verdict": "Profile 3 clean + 1 mild + 1 mod-sev + 3 fail == IDENTICAL to the R10S60+bf16-draft battery (3+1+1+3) and no worse than the Phase-0 full-BF16 reference (1 clean, 2 clean-mild, 2 mild-mod, 1 mod-sev, 2 severe). The all-int8 PTQR stack reproduces the bf16-draft stack's long-output behavior exactly; garble remains the pre-existing BF16-class failure mode (Phase 0: quant is not the cause). NATIVE-INT8 PROGRAM COMPLETE: int8 weights everywhere + g128 KV + round act quant on target AND draft, KLD 0.0080/42-of-52 vs BF16, acceptance 3.83@42k, battery profile unchanged."}
{"exp": "GARBLE_HUNT_2026_09_07", "surface": "long-output garble root-cause program on the PTQR all-int8 stack (greedy 20k-in/4096-out legs, PRING instrument); continues the 2026-09-01..03 passes 51-108 hunt", "legs": {"captures": "3 deaths (tier ON x1, tier OFF x1, hunt leg) — identical forensic signature: sustained n=14 full-acceptance streak -> INSTANT flat verify row (argmax p ~0.005-0.006, absmax ~9 vs healthy 25-35, nan 0) -> rare junk token (q / CJK) committed as TRUE argmax -> token-spam wall forever (e.g. tok 140722 x1500); draft keeps proposing sane continuations (reads clean group-14 ctx) -> n=1 forever; flip position varies (output tok 652/2382) = streak-correlated not position", "exonerated": ["quantization/BF16 (Phase-0 battery)", "sampler + row metadata (committed = true argmax every capture)", "OffloadingConnector (death reproduced with OFFLOAD=0)", "read path/slot maps/anchor feed (passes 104-105)"], "open": "target persistent state (full-attn KV and/or GDN state) corrupted during/after full-acceptance verify rounds; prime path: mamba spec-window rolling-state maintenance (num_accepted-1 bias, block-size-32 crossings every ~2.3 full rounds) or verify KV writes for the 14-row window", "controls_running": "SPECOFF (no draft) 6 legs; then NS=5 6 legs"}, "verdict": "IN PROGRESS — prior hunt's pass-103 'KV write misses' lead was RETRACTED (pass-105 block-width instrument bug) and pass-108 'int8 KV noise' verdict falsified by Phase-0 BF16; this program's captures are the first clean mechanistic signature. Interim user-facing facts: production params (t1.0/p0.95/k20/rep1.0) acceptance is HEALTHY (mean_k 3.45@42k real text) — the 'no acceptance' complaint is killed-request aggregate; single killed request costs ~10x decode speed (124ms/tok vs 12ms)."}
{"exp": "GARBLE_HUNT_SPECOFF_CONTROL", "surface": "no-draft control: 6 greedy 20k/4096 legs, OFFLOAD=0, PTQR stack otherwise identical (g6specoff_leg1-6; 2 lost to a harness save-bug, 4 saved + leg1 read IN FULL by agent)", "legs": {"clean": "4/4 saved legs clean (leg1 full-read: coherent high-quality 5-section essay end-to-end); scan uniq2 0.76-0.92 throughout", "spec_on_kill_rate": "~1/6-1/19 requests across this program's boots (3 deaths captured)", "forensic_consistency": "all 3 captured flips occur immediately after n=14 FULL-ACCEPTANCE rounds — a state that cannot exist without a draft"}, "verdict": "SPECULATIVE DECODE MACHINERY CONVICTED as the garble carrier: target-only decode is clean; the corruption enters the target's persistent state through the spec verify/accept path (prime: mamba spec-window rolling-state maintenance / verify KV writes at max acceptance). NS=5 discrimination hunt running."}
{"exp": "GARBLE_HUNT_NS5_AND_INSTRUMENTS", "surface": "NS=5 control + row-0 audit boot + KVLINE concurrent boot (all OFFLOAD=0)", "legs": {"ns5": "DEATH at leg4 (flip @output 729 after n=6 streak, token 33376 spam, same flat-row signature) — bug is window-size-INDEPENDENT, in the common accept/state-advance path", "row0_audit_boot": "10/10 greedy legs ALIVE (no capture; audit only fires on eager rounds — graph-replayed kill rounds unreachable by it)", "kvline_concurrent_boot": "8/8 concurrent temp-1.0 rounds (24 reqs) ALIVE — capture is boot-luck (~half of boots)", "streak_shape_correction": "death2 pre-flip streak was 6,11,10,8,12,9,14 — SUSTAINED HIGH acceptance (not strictly full) is the trigger = maximal mamba block-boundary crossing rate"}, "verdict": "Working mechanism hypothesis: the mamba align/precopy state migration across block boundaries (mamba_utils precopy_mamba_align_fused_kernel + state_idx advance with token_bias=num_accepted-1) mis-migrates under high-acceptance cadence — e.g. sourcing a nulled-out earlier block — zeroing/degrading the 48-layer GDN recurrent state, which produces exactly the flat-row signature (GDN-silent hidden -> near-uniform logits). Next: KVLINE3 mamba-window-lineage instrument on a dying leg (lineage invariant: pre[N][read_idx] == post[N-1] at the same absolute column)."}
{"exp": "GARBLE_ROOT_CAUSE_LINEAGE_CLEAN_LAYER53_NAN", "surface": "5th death captured with LIVE KVLINE3 mamba lineage (serve-script env-shadowing bug found+fixed: later ${VLLM_*:-} lines blanked the hardcoded probe envs — all mamba probes had been silently OFF); deep analysis of rs=2 gen3 (subagent full reconstruction)", "legs": {"lineage_verdict": "ALL crossings ALL generations CLEAN under the correct contract pre[N][ri] == post[N-1][committed-1] (temporal bt[src_col+bias] exact: e=0/3/8/12/13 all bit-exact; conv bias-0 verbatim exact) — the align/precopy machinery is EXONERATED; the old analyzer's rel-0 invariant was naive (my off-by-(k-1) read was its artifact)", "death_event": "round n=1804 (output token 653): pre(1804) == post(1803) BIT-EXACT on all 8 sampled (layer,state) pairs (pristine read); DURING that forward layer 53 temporal/SSM state goes ALL-14-SLOT NaN (first NaN in the network) while its conv page jumps 32x but FINITE (avg elem 2.67); layers 0/13/40 normal; target logits row NaN same round (row_nan=248320); junk token 8191; all later rounds propagate NaN through CLEAN machinery — acceptance 1.0 forever", "flavors": "this death is the NaN flavor (p=nan); earlier captures showed the flat flavor (p~0.005, no NaN) — two detonation modes, same kill", "matches_prior_hunt": "pass 57-63 signature (GDN layer ~52 NaNs first, layer-selective, propagates) which was then attributed to the offload connector transfer race — BUT this capture ran OFFLOAD=0: connector exonerated for THIS event class too", "kernel_dtype": "fused_sigmoid_gating_delta_rule_update internals all fp32 (loads .to(float32), fp32 accumulator, store to fp32 state) — NOT the old fp16-round-trip mechanism; NaN arises from finite modest inputs + pristine state in one forward"}, "verdict": "Detonation localized: a single GDN layer (~53, of 41-53 unsampled range) SSM update produces NaN during a high-acceptance verify forward with pristine state and finite inputs. Remaining suspects: (i) an inf-capable op inside the kernel chain given that round's specific activations (needs element-level capture of layers 41-53), (ii) an intermediate buffer upstream of layer 53 not covered by any instrument. Next: per-element activation NaN-watch on GDN layers 41-53 at death rounds."}
{"exp": "GARBLE_GDN_EXONERATED_FLAT_FLAVOR", "surface": "6th death captured EAGER (EAGERALL) with VLLM_GDN_NANWATCH recording every round of every GDN layer (mixed/conv/q/k/v/a/b/out/state nan-inf-absmax at 3 hooks)", "legs": {"watch_result": "ZERO anomalies in 500k+ records across the entire death leg — the GDN spec path (conv inputs, qkv, gating, SSM output and state) is CLEAN through the flat-flavor kill", "death_flavor": "FLAT (flip @output 2378: p 0.96->0.0076 instantly, absmax 24.8->9.5, nan=0, top5 pinned) — no NaN anywhere", "implication": "two detonation flavors confirmed: NaN flavor (GDN layer-53 SSM, kvline3 capture) and FLAT flavor (attention-path KV corruption — GDN fully exonerated); flat flavor reproduces under EAGER (graph machinery excluded)"}, "verdict": "FLAT flavor = attention-KV-side corruption during high-acceptance verify rounds. Next instrument: VLLM_KVLINE_RING anchor-slot K/V checksums across the draft forward, EAGER mode, on a dying leg."}
{"exp": "GARBLE_ROOT_CAUSE_FIXED_CUSTOM_AR", "surface": "ROOT CAUSE + FIX: vLLM custom-allreduce cross-rank memory-ordering race on gfx908. The 1stage/2stage reduce kernels synchronized peer-data visibility with a fully-RELAXED barrier_at_start + DEVICE-scope flag acquires; device-scope acquire does not cover remote agents' writes on gfx908, so a consumer kernel could read a peer-staged residual value before it landed — a torn bf16 read produces arbitrary bit patterns: NaN seeds (NaN flavor) or huge-finite values (flat flavor) in ONE layer's forward, committing a junk token whose spam wall then self-sustains (SW window fill + draft-codebook exclusion) with acceptance=1.0 forever. The fork had itself documented the adjacent hazard ('HIP IPC views of cached memory don't see peer writes -> drift -> NaN') and relied on uncached-allocation side effects.", "evidence": {"forensics": "7 captures: every kill = one-layer corruption between clean neighbors; int8 act-quant LAUNDERS NaN (qkvz 'clean' while bf16 ba projection exposed it); the residual-write/read seam was pinpointed by perturbation-masking (any extra kernel in the seam suppresses the kill — 3 clean boots); static audit (subagent) eliminated all host-side ordering and identified the in-kernel visibility seams"}, "fix": "csrc/custom_all_reduce.cuh + csrc/custom_collective_common.cuh: reduce kernels now call barrier_at_start_release (RELEASE store + ACQUIRE wait — the correct variant existed unused in-tree); all cross-rank flag acquires promoted to SYSTEM scope. Rebuilt _C_stable_libtorch.", "validation": {"boot1": "12/12 greedy 20k/4096 legs alive (pre-fix this config died on ~5/8 boots)", "boot2": "12/12 alive (24/24 total)", "full_reads": "greedy leg: clean coherent 4096-tok essay end-to-end; prod-params (t1.0/p0.95/k20/rep1.0) 4x concurrent 42k-in/4k-out legs fully read: coherent continuations, no walls/spam/collapse (mild sampled-code noise only, qualitatively different from the garble)", "watch": "GDN spec-path streams clean in serving rounds; boot-warmup a-NaN classified dummy-data artifact"}}, "speed_after_fix": {"C6_aggregate_essay_bench": "117.7 tok/s (ring on) / 140.3 (ring off) — BELOW the 300 target", "acceptance_bottleneck": "essay/reasoning prompts: mean_k 2.35, k1 46% (vs 3.45-3.5 on doc-continuation at 42k) — the acceptance gap vs the 8/27 stack (4.5-4.9 era) is the throughput bottleneck, not the AR fix", "single_stream_short_ctx": "27 ms/tok (vs 12.34 ms 2026-08-26 reference conditions; different prompt/graph-warmth conditions — needs a matched re-measure)"}, "verdict": "GARBLE FIXED pending production soak. G2 (300 tok/s @ C6) requires a dedicated acceptance-tuning pass: NS re-sweep with the PTQR draft, prompt-class acceptance analysis, and a matched-conditions TPOT A/B for the AR-fix ordering overhead."}
{"exp": "G1B_LOCALIZED_SPEC_TEMPO_PATH", "surface": "the ORIGINAL garble class (formatting drift + repetition walls at prod params) localized by 2x2 control matrix", "legs": {"greedy_spec_on": "CLEAN (24/24 legs + full reads)", "temp1_spec_off": "CLEAN (2/2 legs fully read: d1_specoff_t1_leg1/2 — coherent structured essays, zero drift, on the exact corpus+params that drift with spec on)", "temp1_spec_on": "DRIFTS (prodfix_leg1 newline-in-token drift; prodfix_leg2 data/video wall; BF16-era batteries same class)"}, "implication": "corruption lives in the temp>0 verify/rejection-sampling path of spec decode; mechanism candidate = systematic committed-token distribution bias (acceptance/resample correction subtly wrong — e.g. draft candidate scores treated as probabilities, or residual not renormalized) accumulating over thousands of sampled tokens; explains greedy immunity, spec-off immunity, bf16 equally affected, reset-on-reprefill; matches unresolved pass-55 (in-engine spec logprobs diverged from clean replay, 63/499 >0.3)"}, "verdict": "NEXT: audit rejection_sampler temp>0 math for DFlash drafts (p_draft semantics, residual renormalization) + in-vitro committed-token marginal test vs clean sampler on identical logits."}
{"exp": "G1B_SAMPLER_EXONERATED_IN_VITRO", "surface": "rejection-sampler marginal exactness, in vitro (GPU, rejection_sample direct, N=20k, top-k 20, temp 1.0)", "legs": {"greedy_one_hot_production_config": "EXACT — max freq err 0.0013 (noise floor); drafted-token emission 0.0200 vs 0.0195 expected", "probabilistic_mode": "test_rejection_dist.py 'CONVICTED' at 0.104 BUT harness artifact: it freezes ONE multinomial draft draw instead of resampling x~q per trial, so its expected marginal is wrong for its own setup (Leviathan exactness averages over q)", "production_mode": "draft_sample_method='greedy' default, no dflash override — production runs the exact configuration"}, "verdict": "Rejection kernel EXONERATED for the drift. G1b mechanism re-centered on pass-55's unresolved measurement: in-engine spec-round logprob DIVERGENCE vs clean replay of the same committed tokens (63/499 rounds >0.3, heal-on-re-prefill) — the target forward during spec verify rounds computes subtly wrong distributions; argmax masks it (greedy clean), temp-1.0 sampling exposes it (drift), no-spec rounds are clean. Salt A/B (SALTU+RSALT) did NOT fix the drift — confirmed on full-read leg d2_salt_leg2 (walls + 0000 junk with salts ON). NEXT: re-establish the round-replay divergence measurement on the AR-fixed stack, bisect the verify-round state error."}
{"exp": "G1B_ACCUMULATION_CURVE_MEASURED", "surface": "engine decode-path vs clean prefill-path distribution divergence along a drifting temp-1.0 spec leg (d6_replay_leg, 20k corpus, 4096 out; ring captures engine p(committed) per position; clean = server prefill prompt_logprobs over the same committed sequence; best-shift aligned)", "legs": {"curve_mean_abs_p_delta": {"out400-799": 0.0015, "out800-1199": 0.054, "out1200-1599": 0.080, "out1600-1999": 0.100, "out2000-4096": "plateau 0.06-0.10"}}, "verdict": "THE DRIFT IS MEASURED: decode-path forwards track clean EXACTLY early then progressively diverge from ~output-800 — accumulating decode-state corruption events (each permanently shifts all subsequent context). Matches the user's observed garble onset (~1-2k output tokens). Same weights, same tokens, path-dependent difference (prefill heals). Directionally revives pass-103: per-thousand-token context corruption events in the spec verify write path. NEXT BISECT: NS=1 (single-token verify writes) — divergence vanishing => multi-token verify block write error; unchanged => per-token decode state write error."}
{"exp": "G1B_TARGET_KV_CHANGES_ACROSS_DRAFT_FORWARD", "surface": "KVLINE anchor-slot K/V checksums pre/post the draft forward on a drifting NS=1 leg (d9, 94MB jsonl, drift-curve reproduced: peak 0.058 at out-800-1600)", "legs": {"changes": "350 of 94246 round-layer-slots CHANGED across the draft forward: drafter's own layers 64-68 (190, legitimate own-writes per pass-99 retraction) PLUS TARGET layers 0/13/40/53 (full-attention, 16) and 15/31/47/63 (SW group-14-shared, 144) — rounds n 1330-2336, i.e., AT/AFTER divergence onset"}, "candidates": {"(a) draft-leak": "draft ctx-KV writes (precompute_and_store_context_kv) target slots computed FROM THE TARGET BLOCK TABLE (speculator.py ctx_slot = bt[req, ctx_pos//bs]) — group-14 shared + any aliasing => permanent target KV corruption", "(b) late-landing verify writes": "the previous round's target verify KV write landing between pre/post snapshots (async ordering) => next round's reads may race it — consistent with the timing-perturbation masking observed all program"}, "verdict": "TARGET KV is being modified during the draft window on real drifting legs — the drift mechanism's substrate is now directly observed. NEXT: discriminate (a) vs (b): dump changed-slot rounds vs verify-write rounds; if (b), fix = order the verify KV write before next-round reads (stream/event); if (a), fix = redirect draft ctx writes to scratch slots or snapshot/restore target slots (extend the pass-9 private-row fix to _context_slot_mappings)."}
{"exp": "G1B_KVCHANGE_DISCRIMINATION", "surface": "discrimination of the 342 same-slot target-KV changes across the draft forward (d9 leg)", "legs": {"shape": "ALL 342 changes are same-slot writes during the draft window (no remaps); full-attention target layers 0/13/40/53 changed ONLY at the final round n=2336 (8 writes — instrument boundary artifact); the bulk: SW layers 15/31/47/63 (144) + layers 64-68 (190) — all group-14 pool"}, "mechanism_candidate": "DRAFT CTX-KV ALIASING: the draft's 5 layers write ctx K/V into slots from the TARGET block table (speculator.py ctx_slot arithmetic) landing in the group-14 physical pool that TARGET layers 64-68 (and SW 15/31/47/63? — 15/31/47/63 are target SW layers in OTHER groups... re-verify group membership) read. The pass-99 retraction ('drafter writing its own ctx KV — legitimate') missed that the pool is shared with TARGET layers: a draft-layer write at slot X is consumed by a DIFFERENT target layer as its KV. 5-of-64 layers, SW-scoped => gradual drift, not instant garbage — matches the accumulation curve"}, "verdict": "IN PROGRESS: next = verify the slot-space sharing (draft layer j ctx slot id vs target layer 64+j read slot id in the group-14 pool) directly from the kvline slot ids; if aliased, fix = redirect draft ctx-KV writes to a scratch pool (or dedicate non-overlapping slot range), then re-run the divergence curve + full-read legs."}
{"exp": "G1B_MECHANISM_UNIFIED_DOUBLE_WRITER", "surface": "group-14 membership confirmed via SLOTDEBUG: TARGET layers 64-68 (SW) share the pool with the drafter's 5 ctx-KV layers; kvline shows the TARGET anchor slots themselves changing across the draft window", "mechanism": "SAME-SLOT DOUBLE-WRITER: each round, the draft's precompute_and_store_context_kv writes projected ctx K/V into slots computed FROM THE TARGET BLOCK TABLE (same physical slots), and the target's verify forward writes its exact K/V to the same slots. Two writers per slot per round. If ordering ever leaves the DRAFT's projected values in the slot (write-write race across rounds/steps), the target reads its own layer-64-68 KV as the DRAFT's approximation — subtly wrong, 5-of-64 layers, SW-scoped => progressive distribution drift matching the accumulation curve; greedy robust (argmax), temp-1.0 exposes; timing perturbations reshuffle interleaving (explains instrument-masking); heal-on-reprefill (prefill rewrites all slots exactly); layers 15/31/47/63 changes = instrument-boundary (verify write of the round itself included in post) — separate pool, not writable by the draft", "verdict": "NEXT: trace launch order — precompute_and_store_context_kv (next round's prepare) vs the target verify KV write (this round) on the same slots; fix = single-writer discipline (draft ctx to scratch pool, or strict ordering + ownership). Then divergence-curve + full-read validation."}
{"exp": "G1B_ALIASING_EMPIRICALLY_CLOSED", "surface": "closing the loop on the double-writer conviction: pool layout is per-layer packing offsets (attn_utils.py _reshape_attention_kv_cache: kv_raw.view(-1, block_stride)[:, offset:offset+page_bytes]) — drafter layers SHOULD be disjoint; empirically they are NOT", "evidence": "kvline pre/post snapshots bracket ONLY the draft forward (same stream, after prior round's verify); target layers 64-68 anchor slots changed at 38 rounds each SPREAD over rounds 1330-2336 (mid-stream, not the final-round boundary artifact seen on full-attn layers) — the drafter's ctx writes at cp_local_slot(target_bt) land on target physical bytes despite intended disjoint packing; sparse (~4% of rounds hit the sampled anchor slot) => intermittent aliasing, matching sparse corruption events", "verdict": "G1b MECHANISM CONVICTED: drafter ctx-KV writes intermittently alias target SW-layer KV (group-14 pool) — same-slot double-write leaves the drafter's PROJECTED values in target slots; target then reads its own layer-64-68 KV as the approximation => progressive distribution drift (curve), walls at temp-1.0, greedy-immune, heal-on-reprefill, timing-perturbation-sensitive. FIX DIRECTION: dedicated scratch pool for drafter ctx-KV (or exclude drafter layers from group-14 packing so offsets never overlap), then divergence-curve + full-read validation."}
{"exp": "G1B_CONVICTION_CORRECTED_STALE_TRIM_HAZARD", "surface": "subagent audit DISPROVED the double-writer conviction and found the real hazard", "legs": {"disproof": "layers 64-68 are the DRAFTER's own layers (SLOTDEBUG 'target_layers=' label is a false positive — model_runner passes all 15 groups as target_attn_groups); KVLINE indexed wrong rows (slot//1728 vs kernel rows of 64 — read the NULL PAGE, not the anchor row); the target anchor rows never changed; draft-write window (r2207-2244) does not overlap the target-change window (r1330-1365)"}, "real_hazard": "worker-side group-14 block table is APPEND-ONLY: scheduler-side sliding-window eviction nulls entries (single_type_kv_cache_manager.py:597-622) that never propagate to the worker (only new_block_ids ship; block_table.py:107-127) — the kernel's ctx_resident = ctx_block_id != 0 guard (speculator.py:714) reads STALE NONZERO ids, so drafter ctx writes can land in freed-then-reallocated pages on shared tensors 0-4 (draft shares tensors with target full-attn L15/31/47/63 AND mamba L0/13/40/53) — sparse, allocation-phase-dependent, matches drift epidemiology (greedy-robust, temp1-exposed, heal-on-reprefill, BF16-affected)"}, "verdict": "FIX: propagate trims to the worker table (or bound ctx/q_block_num by the group's live block count in _prepare_dflash_inputs_kernel). Also fix the KVLINE instrument (slot//64 not //1728, exclude group-14 layers) before re-validating."}
{"exp": "G1B_FIX_ATTEMPT1_RESULT", "surface": "block-table stale-tail clear (block_table.py append_block_ids zero-fills beyond new live length) — built, booted, validated", "legs": {"curve": "NOT FLAT: overall 0.0727; buckets 0.028 (0-800) / 0.084 / 0.085 / 0.104 (2400) / 0.067 / 0.049 — divergence onset ~800 persists", "full_read": "v1_fixed_leg DRIFTS AND COLLAPSES: coherent start, mid-word fusions from ~line 7, full 'the the r r r' wall by line 39"}, "why_fix_failed": "eviction NEVER triggers an append on the worker — num_blocks never shrinks — so the end<prev_end tail-clear branch cannot fire for sliding-window eviction shrinks; it only covers cross-request row reuse (overwrite=True). The audit's actual recommendation (scheduler->worker trim propagation, or kernel-side live-count bounding in _prepare_dflash_inputs_kernel) remains UNIMPLEMENTED"}, "verdict": "Fix attempt 1 insufficient by construction. Next: (1) kernel-side bound of ctx/q block index by the group's live block count (needs live count shipped or derived from seq_len/window); (2) fix KVLINE row indexing (slot//64 not //1728) + exclude group-14 layers to re-observe the corruption substrate; (3) consider that the stale-trim hazard may not be the whole mechanism — re-run the audit's other writers (offloading-connector async transfers) as candidates."}
{"exp": "G1B_FIX_ATTEMPT2_RESULT", "surface": "kernel-side live-window guard in _prepare_dflash_inputs_kernel (ctx selection bounded to [seq_len-window, seq_len); window_tokens=2048 threaded from spec through prepare_dflash_inputs to the Triton kernel)", "legs": {"result": "boot OK, leg v2_kernelguard COLLAPSES FASTER: 'harbor,' repetition wall within 1491 tokens (worse than unfixed)", "interpretation": "either the stale-trim hazard is not the target-side drift mechanism, or skipping ctx-KV writes below the window breaks a drafter path that reads full context (acceptance degraded — wall onset sooner). Target-side divergence persists regardless"}, "verdict": "STOP blind fix attempts. PRIORITY: fix the KVLINE instrument first (row indexing slot//kernel_block_size not slot//1728; exclude group-14 draft layers from the 'target' set; ownership-aware change attribution) so the corruption substrate can be SEEN correctly, then re-observe on a drifting leg before any third fix. Also candidate per audit: offloading-connector async transfers as the low-page writer."}
{"exp": "G1B_FIXED_INSTRUMENT_NULL_RESULT", "surface": "KVLINE instrument corrected (slot//kernel_block_size rows, drafter groups excluded) and re-run on a drifting leg (v3_fixedinst, 43MB, leg drifted/collapsed at ~2400 out)", "legs": {"result": "ZERO of 42443 both-phase target rows changed across the draft forward — the drafter does NOT corrupt target anchor KV; the double-writer/aliasing story is definitively dead"}, "new_hypothesis": "REJECTED-DRAFT PHANTOM KV: each verify round writes KV for ALL 14 positions (anchor+13 drafts); on rejection rounds (46% at temp1) only k<14 are accepted — the rejected positions' KV rows are phantom context. If the next round's attention reads those rows before the round's own writes replace them (block bookkeeping/seq_len accounting off by the rejection count), the context silently contains phantom tokens => spec-only (greedy near-full acceptance is clean, spec-off never writes phantoms is clean, temp1 rejection-heavy drifts), accumulates with rejections, heals on reprefill — FITS ALL FOUR CONTROL QUADRANTS"}, "verdict": "NEXT: verify the phantom hypothesis — compare KV rows at rejected positions between consecutive rounds (fixed instrument extended past the anchor row), and audit the num_computed/seq_len rewind accounting for rejected draft rows (speculator seq_lens update + attention metadata query span)."}
{"exp": "G1B_PHANTOM_HYPOTHESIS_REFINED", "surface": "refinement: position-addressed KV slots mean round-N phantom rows at [anchor+k, anchor+13] are OVERWRITTEN by round N+1's verify writes at the same positions — plain attention KV phantoms self-heal; the phantom hypothesis survives only for state that is NOT position-addressed", "refined_suspect": "GDN RECURRENT STATE ROLLBACK UNDER REJECTION: the state advances through all 14 verify tokens; on rejection rounds (46% at temp1) the resume logic rewinds to slot num_accepted-1 (mamba_utils). Prior lineage audit checksummed the rolling window bit-exact — but sampled only 4 layers, only exit windows, and never measured VALUE drift vs the clean prefill-path state. A subtle per-round state error (e.g., off-by-one in the resume slot under specific acceptance patterns, or the conv-buffer roll dropping a tap) accumulates exactly like the measured curve", "verdict": "NEXT MEASUREMENT: per-round GDN state value-divergence vs clean path — capture the resumed-slot state (all 48 layers) on a drifting leg and replay the same committed tokens through the dense reference forward (ptqr_train_target build_lm_model, the KLD teacher), comparing state vectors at matched positions. This directly measures the state error the curve implies, at full layer coverage, closing the loop from distribution-divergence to state-divergence."}
{"exp": "G1B_ACCEPT1_KNIFE", "surface": "causal knife on the surviving suspect: VLLM_ACCEPT1 clamps every verify round to exactly 1 committed token (V2 rejection_sampler __call__: rows[:,1:]=-1 + num_sampled.clamp_(max=1); V1 sampler mirrored). Temp-1 sampling, 14-token verify compute, drafts and rejections all stay fully active (PRING natural-commit histogram 1..14, mean ~3.5), but the post-round mamba resume geometry (num_accepted-1) is 0 every round == spec-off decode geometry (which is CLEAN at temp1)", "legs": {"run": "d3_accept1_leg1: 20k synthetic corpus, 3072 out tokens, temp1.0/p0.95/k20 seed 5, 974s, 3.15 tok/s (~1/3.5 of natural rate => clamp provably engaged)", "read": "FULL manual read: lines 1-38 coherent essay, line 39 collapses MID-SENTENCE into meta-commentary ('...mechanism-aThe user wants me to synthesize...'), then word-lists with repetition fragments ('boost - not present / no / no') and planning-list output that never recovers — SAME garble class as natural temp1+spec"}, "causality": "rejection-resume index math EXONERATED: with resume bias forced to the spec-off-equivalent 0 every round the drift still occurs, so the corruption is NOT in which slot the recurrence resumes from. The corruptor is exercised by the multi-token verify compute itself"}, "new_hypothesis": "surviving suspects: (A) GDN multi-token chain write path — the 14-token in-kernel state chaining or conv rolling-buffer write corrupts even the resumed slot-0 state; (B) verify-batch attention numerics on int8 g128 KV at T=14 queries (path spec-off never runs) subtly corrupting logits; greedy legs stay clean because argmax is robust to the 0.0015->0.06 argmax-flip-rate the accumulation curve measured", "verdict": "NEXT: GDNSTAT capture (all-48-layer per-round window norms + slices, built this pass) on a natural drifting leg -> jump/anomaly analysis localizes (A); if null, build a verify-row0-vs-decode logits probe for (B)"}
{"exp": "G1B_GDNSTAT_LEGS_1_2", "surface": "all-20-GDN-layer per-round checkpoint-window capture on natural drifting legs (leg1: norms only; leg2: +kbase/blks). leg2 verdict: delta=kbase-col==1 on ALL 572x4 decode rounds (seq_lens-based kernel window base NEVER diverges from num_computed-based align base); zero resumed-slot NaN after the prefill->decode boundary; zero window aliasing; SSM(st1) NaN only at rounds 16-17 rels 6-13 (transient boundary garbage, overwritten, never read); conv NaN blocks = unwritten future slots (conv checkpoints only use [:,0]) = benign. Structural state machinery fully exonerated", "legs": {"leg2_read": "severely garbled: mid-sentence collapse at line 38, catastrophic bare-dash repetition wall lines 63-743 — capture did NOT mask the bug"}, "verdict": "GDN structural suspects (base mismatch, aliasing, NaN reads, resume index) ALL NULL. Surviving: verify-compute numerics corrupting the DISTRIBUTION (not structural state)"}
{"exp": "G1B_GREEDY_CURVE_KNIFE", "surface": "accumulation curve measured on a GREEDY+spec leg (same-token clean-prefill prompt_logprobs comparison; greedy sampler records raw probs so no truncation bias): out[0:384]=0.38 early-phase noise, out[384:768]=0.0015 FLOOR, onset at ~768, plateau 0.08-0.17 mean (p90 0.2-0.5 nat) through 3072. TEMP1 leg same measurement: floor 0.005 in wall phases (metric blind when committed token is near-deterministic), 0.4-0.6 mean in degraded phases", "causality": "the verify-path distribution error GROWS WITH GENERATED LENGTH EVEN AT GREEDY => compute-path numerics convicted; clean greedy text = top-1 margin robustness (error concentrated in ranks 2-20, exactly what temp1 samples from). ACCEPT1 (dirty; keeps T=14 writes), spec-off (clean; T=1 writes), greedy (clean text, dirty distribution) all fit: the T=14 verify batch's attention-KV write/read path on int8 g128 subtly diverges from prefill/decode", "verdict": "NEXT: kernel-level byte-comparison — quantize+write identical K/V via the prefill path vs the verify path (T=14), compare int8 bytes + inline scales; if identical, fall back to GDN state VALUE drift via scripted spec-off same-token replay"}
{"exp": "G1B_SPECOFF_CURVE_CONTROL", "surface": "spec-off greedy leg (SPECOFF=1 verified in cmdline this time; footgun: empty SPECOFF flag file silently keeps spec on — first attempt invalid) with exact per-token logprobs (return_token_ids:true + logprobs:1 on /v1/completions), scored against clean prefill of same transcript", "legs": {"run": "d3_specoff_greedy5: 20040 prompt + 1064 committed (EOS), 253s, covers the flip region (output 768-1064 vs flip at 814)", "curve": "out[0:384]=0.032 (early-phase), out[384:768]=0.0005 FLOOR, out[768:1152]=0.0010 FLOOR, ZERO positions >0.5nat — vs spec greedy 0.135-0.22 mean / 6-15% dirty at the same positions"}, "causality": "SPEC-PATH-ONLY conviction: plain decode at identical positions/context is bit-clean past the flip. The corruption REQUIRES the speculative verify machinery. With (a) onset exactly at draft-quality collapse (~814), (b) dirt monotone in draft real-proposal count, (c) episodic dirt with exact-0.0 interleaves, (d) ACCEPT1 dirty, (e) GDN structural exonerated — the single standing mechanism is the IN-BATCH SPECULATIVE-KV LEAK: verify row-0 attention sees the batch's own speculative KV rows (wrong keys/values when drafts are bad, ~correct when drafts are good)", "verdict": "RUNNING: DRAFTGARBAGE A/B (garbage drafts from token 0) — leak => dirt from token 0; isolation => floor everywhere. Fix after conviction: the attention kernel's in-batch causal masking for multi-row verify segments (causal=True + seqused_k=num_computed+14 currently rests on the AITER unified kernel masking the last-13 keys from row 0)"}
{"exp": "G1B_DRAFTGARBAGE_AB", "surface": "causal-isolation A/B: VLLM_DRAFT_GARBAGE corrupts every proposed draft token (+12345 mod vocab) before verify; greedy leg, PRING confirms ALL 1500 rounds commit exactly 1 (natural greedy acceptance would be 3-6) => transform provably engaged", "legs": {"curve": "d3_garbage_greedy1 scored vs clean prefill: out[0:256]=0.44 (early-phase cluster, present in all legs), out[256:768]=0.0009 FLOOR-CLEAN despite maximally-garbage drafts from token 0, out[768:1536]=0.136-0.164 mean / 7-10% dirty — SAME onset region (~768-853) and nearly identical stats as the natural leg (0.135 at 768-1152)"}, "causality": "IN-BATCH SPECULATIVE-KV LEAK and CONV SPECULATIVE-HISTORY BLEED ARE DEAD: if draft VALUES influenced row-0 output, garbage drafts would dirt from token 0; they do not (256-768 floor). The dirt is POSITION-TRIGGERED (~nct 20830+), SPEC-MODE-ONLY, DRAFT-VALUE-INDEPENDENT", "new_evidence": "first VALUE-level anomaly found: at the first decode-time mamba block crossing (col 11->12, nct 20724/20736 in leg2 capture) ALL THREE sampled workers' conv-state (st0) running-block norms drop ~18% (903->735, 901->737, 768->625) — exactly one-token-of-window magnitude (1/kw ~ 14-25%); temporal (st1) smooth. Also unresolved: layer code routes conv_state_indices to gathered-window col (my rel-1) but rel-1 conv stays init-0.24 forever while rel-0 changes every round — kernel index reality must be measured (GDN_RING)", "verdict": "RUNNING: d4_convstats_leg1 (GDNRING + GDNSTAT rel-0/1 slices) — (1) which columns the conv kernel actually touches, (2) whether the crossing migration copies a shifted/wrong conv window. Prime suspect: conv-state align migration value bug at block crossings, accumulating through the GDN recurrence (~150-token visibility lag matches onsets 814/853 after boundary 20736)"}
{"exp": "G1B_MIGRATION_LINEAGE_TEST", "surface": "GDNSTAT extended with a post-forward phase capture (window norms after the verify forward + the round's TRUE acceptance from num_sampled, before the align postcopy); lineage invariant tested directly at the first decode-time block crossing on all 4 TP workers", "legs": {"result": "TEMPORAL (SSM) MIGRATION IS BYTE-EXACT WITH THE CORRECT SLOT at crossings: post_N[st1 @ na_N-1] == pre_{N+1}[st1 @ rel0] exactly (206.0966==206.0966, 185.6224, 196.4501, 161.7984; na values 4/4/11/4). The conv migration leaves a zero tail (crossing-round conv norm 469 ~= sqrt(6/16)*760, b=10 shift) but the conv read protocol derives correct on paper: preprocess resets na to 1 at crossings so the kernel reads window[0..2], inside the migrated region, and rewrites the full buffer within one round", "verdict": "THE ENTIRE MAMBA/CONV/SSM STATE MACHINERY IS EXONERATED AT THE VALUE LEVEL: no structural anomalies, no NaN reads, migration byte-exact, resume indexing correct, conv protocol sound. The corruption (spec-only, trajectory-triggered ~output 800, draft-value-independent, 0.1-0.5 nat on ~10% of positions, permanent) lives OUTSIDE the recurrent state"}, "next_lead": "remaining spec-only + draft-value-independent surfaces: (1) the DFlash SHARED-GROUP slot-mapping backup/restore around the draft forward (speculator _shared_group_ids — fork-specific mechanism where the drafter borrows target KV groups; prior KVLINE audit covered only anchor pages across propose); (2) verify-batch attention KV writes for the OUTPUT region and whether any later round mutates already-written positions (extend the KVLINE page-checksum audit from anchor-only to the last-N output positions, across full rounds); (3) OffloadingConnector behavior at long context", "runs": {"d4_migration_leg1": "1600 out tokens, drifted (gradual formatting degeneration); captures in logs/garble/gdnstat_nat3 (pre+post phases)"}}
{"exp": "G1B_SWGROUP_CROSSWRITE", "surface": "SWAUDIT: page checksums of the shared KV group 14 (target full-attn layers model.layers.64-68 merged with the DFlash drafter's group — the surface the KVLINE audit EXCLUDED because it filtered drafter groups). v1 instrument checksummed layer_names[0] = model.layers.64 (a TARGET layer; the language_model-prefix v2 filter was wrong — this arch names target SW/full layers without the prefix)", "legs": {"d5_swaudit_leg1": "1600 out, drifted (mid-sentence meta-collapse line 9). Propose-window results on 4 workers: TAIL page changes 383-386/~400 steps (draft writes the active tail page of the TARGET layer's tensor EVERY propose); OLD (completed, non-tail) page changes 15/1900 — 11 during prefill (the just-completed page per chunk) and 4 CONSECUTIVE events at idx 230-236 on completed page col 11 blk 222 (633176->626619->617839->614378->608384) — squarely in the drift-onset region (output ~700-800)", "interpretation": "THE DRAFT FORWARD WRITES INTO THE TARGET LAYER 64-68 KV TENSORS at shared-group pages: continuously at the tail (speculative overlay) and occasionally into COMPLETED pages (context corruption; sliding-window=None so these are full-attention layers — corrupted pages stay in context forever). This surface was never audited before (KVLINE excluded drafter groups). Fits the full profile: spec-only (draft forward only runs under spec), draft-value-independent (slot/page arithmetic, not token values), episodic (rare page misdirection), permanent damage (corrupted committed-context KV), onset at decode depth (page reuse patterns mature)"}, "verdict": "NEXT: fix = stop the drafter from writing shared-group target pages (auditable via this instrument going null), candidate mechanisms to inspect: the shared per-layer slot_mappings dict row that the target verify graphs read (pass-6 snapshot/restore window), the private draft slot rows (pass-9) still resolving to shared-group page ids for the TARGET tensors, and the workspace-lane stream ordering of draft KV writes; then validate: SWAUDIT null + flat accumulation curve + full-read temp1 legs clean"}
{"exp": "G1B_SWGROUP_MECHANISM_LEAD", "surface": "the draft input flow: prepare_dflash_inputs computes slots from block_tables.input_block_tables[14] (the SHARED group's block table); the draft model then precompute_and_store_context_kv(hidden_states, context_positions, context_slots) PRE-INSERTS context K/V into the cache EVERY step — this runs BEFORE the pass-6 snapshot/restore window and uses a separate slot path (_context_slot_mappings), i.e. an unsnapshotted write into shared-group slots on every propose; the pass-9 private-slot-row overlay only covers the draft forward's slot dict, not this pre-insert", "hypothesis": "the context-KV pre-insert (or an un-overlaid draft slot path) writes into the TARGET layers 64-68 tensors at shared-group pages — continuously at the tail (by-design speculative overlay or misdirected pre-insert) and occasionally at COMPLETED pages (the corruption; possibly interacting with the OffloadingConnector's group-14 handling — 'trailing chunk excluded from offloading due to volatility')", "verdict": "NEXT: read precompute_and_store_context_kv + _context_slot_mappings construction (draft model qwen3_next_mtp/dflash file); determine whose tensor it writes and which positions; fix = route the pre-insert through private slots or exclude it for shared groups; validate with SWAUDIT null (no completed-page mutations), flat accumulation curve, full-read temp1 legs"}
{"exp": "G1B_SWGROUP_RETRACTION", "surface": "the SWGROUP crosswrite conviction is RETRACTED: the DFlash2DraftModel is a 5-layer MTP-style continuation of the target's 64-layer stack — its attention layers are numbered 64-68 and named 'model.layers.N.self_attn.attn' WITHOUT the language_model prefix (the TARGET's layers are named 'language_model.model.layers.N...'; confirmed by GDNSTAT layout names). Group 14 hosts the DRAFT'S OWN five attention layers; the SWAUDIT v1 checksummed the draft's tensor — the tail-page churn (every propose) and the 15 old-page mutations are the draft's legitimate own-KV maintenance (context pre-insert + speculative writes)", "lesson": "the speculator's in-code comment ('Qwen3.8 layers 64-68 share the DFlash drafter's group' / 'the target's own SW layers') MISLABELS the draft's layers as target layers — it cost a full false-conviction cycle; comment fixed. The layer-name prefix (language_model.* vs bare model.*) is the reliable target-vs-draft discriminator on this architecture", "verdict": "G1b profile stands (spec-only, trajectory-triggered ~800, draft-value-independent, episodic, permanent, distribution-level) with ALL engine state surfaces exonerated. NEXT SLICE: offline kernel-level T=1-vs-T=14 discriminator on the production attention path (same int8 g128 cache, identical queries, compare row-0 outputs) — the verify-read numerics (suspect B) is the last untested surface; plus engine-level NS sweep of the accumulation curve if the kernel test is null"}
{"exp": "G1B_KVWRITE_AB_INCONCLUSIVE", "surface": "KVAUDIT: byte-captured KV rows (target full-attn layers 3/31/63, output region) for a greedy spec leg vs clean-prefill replays of the same transcript (replayA fresh, replayB prefix-cached)", "result": "leg-vs-replayA matched pages differ at 78-88% of RAW bytes (median |delta| 64 int8 units) — but the comparison is INCONCLUSIVE: (1) rows are 1728-token pages containing inline fp16 g128 SCALES whose last-bit fp noise shows as +-64/hundreds in raw-byte space — separating data-vs-scale diffs needs dtype-aware layout parsing that was not built; (2) the design is confounded by different prefill chunk boundaries at the prompt/output junction and decode-vs-prefill GDN state at the junction (both change the hidden states feeding the output-region K/V)", "lessons": "byte-level cross-path KV comparison on this cache format needs: exact inline-scale layout parsing, chunk-boundary-matched replays (not controllable via API), and a same-path determinism control (replayB hit the prefix cache so the floor is unmeasured). Two instrument bugs fixed on the way (27x row misalignment theory was wrong — the tensor dim0 IS 1728-pages with 64-token kernel rows living in a different view; drift_leg now saves exact committed ids via return_token_ids)", "verdict": "ABANDONED for diminishing returns. NEXT SLICE: NS-dependence of the accumulation curve (NS=5 vs NS=13 greedy legs) — if dirt scales with NS, the verify batch shape (T=NS+1) is convicted as the surface, giving both a precise kernel target and an interim mitigation lever"}
{"exp": "G1B_SWEEP_NS_CROSSING_LMHEAD", "surface": "four A/Bs on the accumulation curve, all with exact engine logprobs (drift_leg now captures chat logprobs+token_ids; temp0 legs send top_p=1/top_k=-1 to kill the truncation confound)", "legs": {"ns5": "NS=5: dirty-looking everywhere BUT phase-confounded (all-reasoning trajectories); no-truncation rerun still 0.09-0.35", "ns1": "NS=1 (verify T=2): FLOOR 0.001-0.004 through 768, onset 768-800, plateau 0.07-0.16 — IDENTICAL profile to NS=13 => batch shape T is irrelevant", "shift": "prompt 20369 (decode crossing moved to output 367): crossing passed CLEANLY (floor through 768), onset STILL at 768-800 => block-crossing hypothesis dead; onset is pinned to ~800 OUTPUT tokens across NS/temp/garbage/prompt-length", "lmhead": "int8 LM head OFF (INT8HEAD=0): same profile => lm head exonerated"}, "unified_profile": "REVISED: the verify-path error exists FROM TOKEN 0 (the 0-256 bin is dirty in EVERY leg at 7-15% >0.5nat — previously dismissed as early-phase noise) and is visible only at flat/low-margin positions; the 'onset ~800' is where flat positions reappear in the content. Decode(M=1)==prefill(M=2048) to 0.001 while verify(T=2..14) differs => NOT generic GEMM tiling; the error is unique to the verify batch's compute", "verdict": "the ONLY remaining structurally-unique verify compute: the GDN spec-branch kernels — causal_conv1d_update's spec-only shift (idx_tokens + 1) and the SSM IS_SPEC_DECODING chain. NEXT: in-vitro A/B — call causal_conv1d_update in spec mode (T=2, na=1) vs two sequential non-spec decode calls with identical tokens/states; outputs must match token-for-token; then the same for the SSM kernel. A mismatch convicts with the exact kernel"}
{"exp": "G1B_INVITRO_GDN_KERNELS", "surface": "in-vitro equivalence tests of the spec vs decode GDN kernels with production shapes (H16/HV48/K128/V128, fp32 state, bf16 io). PATH DISCOVERY: the fused CUDA MTP kernel (fused_gdn_decode_post_conv_mtp) is NOT in the built _C_stable_libtorch.abi3.so (no symbols) — hasattr gate False in the server too — production spec path = FLA fused_sigmoid_gating_delta_rule_update (Triton); decode path = FLA fused_recurrent_gated_delta_rule_packed_decode (Triton)", "results": {"conv": "causal_conv1d_update spec branch EXACTLY equivalent to sequential decode (Δ=0.0 for outputs through an acceptance-2 rewind chain); state buffer layouts differ by protocol but chained reads resolve correctly — CONV FULLY EXONERATED", "ssm": "fused_sigmoid_gating_delta_rule_update (spec) vs packed_decode (decode): outputs agree to 1.2e-4 (bf16 rounding), state chain agrees to 7.8e-4 — small but SYSTEMATIC cross-implementation differences (different decay/l2norm formulations between the two Triton kernels); the prefill path uses yet another variant (chunked FLA)"}, "interpretation": "no single kernel is broken; the three GDN implementations (spec/decode/prefill) differ at the 1e-4..1e-3 level per token per layer, and these differences compound through the 20-layer recurrent state over thousands of tokens — consistent with the curve's 0.0015 floor early and dirt emerging at flat positions later", "verdict": "NEXT: numerical-hardening A/B — route the verify row-0 (or the whole T=2 batch at NS=1) through the DECODE kernel per-token (bypassing fused_sigmoid_gating) and re-measure the curve; if it flattens, the spec GDN math variant is convicted and the fix = unify the kernels' formulations (or accept the unfused cost at verify). Test scripts: scripts/test_conv_spec_equiv.py, test_gdn_mtp_equiv.py (updated target inside)"}
{"exp": "G1B_GDN_KERNEL_CHAIN_NULL", "surface": "attempted long-sequence (512-token) chained in-vitro comparison of the spec GDN kernel (fused_sigmoid_gating, 14-token chunks with checkpoint resume) vs sequential decode (packed_recurrent) to measure divergence growth", "result": "INCONCLUSIVE — three successive emulation bugs (kernel reads init from si[na-1] not the copied slot; checkpoint write/read slot off-by-ones; snapshot logic) produced artifacts (constant 0.056, then bit-identical deltas). The reliable per-chunk measurement stands: the two kernels agree to 7.8e-4 (state) / 1.2e-4 (outputs) on a single chunk — no smoking gun, and divergence growth over 500 tokens remains UNMEASURED", "verdict": "kernel-level G1b localization is exhausted at reasonable effort: every state surface, sampler surface, batch-shape, crossing, lm-head, and per-chunk kernel comparison is exonerated or clean. Remaining unmeasured: (a) layer-level hidden-state divergence verify-vs-prefill on identical tokens (which layer class diverges first — needs a new capture instrument), (b) true long-range kernel divergence (needs a correct emulation or engine-side routing A/B). RECOMMEND: alternate effort to G2 (speed) while G1b awaits the layer-probe instrument"}
{"exp": "G2_C6_INTERLEAVED_3REP", "surface": "honest interleaved multi-rep C6 measurement at g128 production posture (NS=13, int8 lm-head on, verified via cmdline+env of the port owner): vllm bench serve random 32/1000, 6 prompts, concurrency 6, temp 0, no-stream, 2 warmups, fresh boot per rep (clock-regime interleaving)", "result": "rep1=234.81 rep2=188.67 rep3=184.40 tok/s -> mean 202.6, spread ±13% (matches the known 731-962MHz bimodal clocks); TPOT 10.42-14.48ms; implied committed-per-round k≈2.4-2.7", "gap": "target 300 tok/s at C6: ~100 tok/s short (33-50%). In-policy levers: draft acceptance (k) is the dominant one — rung-2 distillation against g128-noised verify states; round latency secondary", "verdict": "NEXT: LOGSTATS-instrumented bench rep to read live mean-k/acceptance at these shapes, then draft rung-2 if k is the bottleneck (expected: k~2.5 -> 4 would reach 300+)"}
{"exp": "G2_ACCEPTANCE_DEPTH_COLLAPSE", "surface": "LOGSTATS-instrumented C6 bench rep (200.73 tok/s, TPOT 12.33ms) — live SpecDecoding metrics by phase: warmup mean acceptance length 10.38 (per-position 0.889/0.783/0.760...), bench 6.85 (0.900/0.584/0.505...), deep-output phase 4.39 with per-position rates PINNED at 0.209 from position 4 onward", "finding": "the drafter's acceptance collapses with GENERATED DEPTH (10.4 -> 6.9 -> 4.4; flat 0.209 floor deep in 1000-token outputs) — the same depth-dependent degradation signature as the G1b verify-path logit dirt (onset ~800). The draft consumes target verify-path hidden states (context KV pre-insert) + its own KV built from them, so a single root cause corrupting the spec-path states would produce BOTH symptoms", "implication": "G1 AND G2 ARE COUPLED: fixing the verify-path degradation should restore deep-output acceptance (k 8-10 at depth => ~320 tok/s at C6, beating the 300 target). Chasing acceptance via draft retraining (rung-2) WITHOUT the root fix would fight the symptom", "verdict": "G1b remains the critical path. Next: layer-level hidden-state probe (which layer class's outputs diverge first verify-vs-prefill on identical tokens) — now doubly motivated"}
{"exp": "G1B_LAYERPROBE_INSTRUMENT_UNRESOLVED", "surface": "layer-level hidden-state divergence probe: per-step per-layer 16-dim random projections of the batch's row 0 (verify leg) vs dense rows (prefill replay), same TP rank, sequence-parallel-aware position indexing, shard-flushed (eager-mode instrument)", "result": "INSTRUMENT OPERATIONAL but PAIRING SEMANTICS UNRESOLVED after 6 iterations (fixes: sync guard, mrope 2-D positions, SP rank offset, SIGTERM-safe incremental flush, scope/threshold bugs): the measured verify-vs-prefill hidden states are uncorrelated (1-cos 0.6-0.9) at EVERY layer including layer 0, and NO position offset (-2..+2) recovers cos~1 at layer 0 — the verify batch's row-0 token/position mapping is more complex than assumed (likely batch row order or the SP chunk vs segment boundaries differ from my model of them). A layer-0-uncorrelated result cannot be the subtle G1b signature; it is a pairing artifact", "lessons": "atexit does not fire on the serve script's SIGTERM stop (incremental flush required); positions is 2-D mrope; hidden rows are TP-chunked while positions is not; torch.save inside the forward needs env-scoped paths; verify batch rows do not pair with prefill rows at any simple offset — the next attempt must record input_ids alongside positions per row to nail the mapping", "verdict": "HALTED after 6 iterations of diminishing operational returns; probe code + shards + analyzer (scripts/analyze_layerprobe.py) preserved. G1b fresh-angle list: (a) this probe with row-identity capture, (b) engine-side routing A/B (verify GDN through the decode kernel). Server restored to production posture"}
{"exp": "G1B_FORK_FIX_PORT", "surface": "mined the two sibling forks (qwen38-27b-rtx3090, mi210-vllm — shallow clones at ~/forks) for spec-decode GDN fixes we lack; full audit via explore agent", "findings": "rtx3090 = patch-stack over stock vLLM 0.27.1-0.28.0 with TWO GDN-relevant patches: (1) vllm-pr50021-gdn-spec-bounds — we already carry the stronger conv variant; ported the fused_sigmoid_gating zero-fill-on-invalid (was bare return on new_empty = logits from uninit memory when the guard fires) and noted the mamba_ssm/fused_recurrent guards (off our verify path); (2) mamba-chunked-prefill-align hunk 2 — chunk_o b_g masking before exp() (uninit memory in partial boundary chunks -> NaNs poisoning recurrent state): PORTED. mi210 = registry only, no GDN spec machinery (DFlash2 retired there; upstream ships it). NEITHER fork patches the verify-path GDN logic itself — the acceptance-rewind verify bug we hunt is unfixed anywhere", "legs": {"d10_fixport_temp1": "1600 out, FULL manual read: coherent essay through line 12 then the SAME mid-sentence meta-collapse at line 13 (~750 out) — the ported fixes are correctness hygiene but NOT the garble cure (expected: the chunk_o fix touches the chunked-prefill path shared by both sides of any A/B)"}, "verdict": "fixes committed (safety-positive); G1b hunt continues — remaining leads: layer-probe v2 with per-row input_ids capture (pairing), engine-side routing A/B (assessed low-yield after in-vitro 7.8e-4 agreement)"}
{"exp": "G1B_LAYERPROBE_V2_RESULT", "surface": "layer-probe v2 WORKING: per-row token ids in records + stride-1 replay; 7998 token-verified (pos,tok,layer) pairs between the verify leg and the clean-prefill replay (6113 skipped on transcript-reconstruction offset, expected)", "result": "PER-LAYER DIVERGENCE PROFILE (1-cos of 16-dim projections, ~161 positions/layer): layer 0-1 at FLOOR (0.0002-0.0004 — verify==prefill at entry), then MONOTONE COMPOUNDING GROWTH through the stack: ~0.001 by layer 8, 0.002-0.004 by layer 20, 0.010-0.016 by layers 27-50; class means GDN=0.0054 vs ATTN=0.0041 (GDN-weighted); no single culprit layer — the error is DISTRIBUTED and COMPOUNDING", "interpretation": "consistent with the leading mechanism: the spec-path GDN kernel (fused_sigmoid_gating) differs systematically from the decode/prefill formulations (~7.8e-4 per token-layer, measured in-vitro), and each of the 48 GDN layers adds a small delta that compounds through the residual stream and across tokens — reaching ~1% hidden-state divergence at late layers, enough to shift flat-position logits by 0.1-0.5 nat", "verdict": "NEXT (now well-motivated): the routing A/B — env-gate the verify GDN through per-token DECODE-kernel calls (slow, diagnostic) and re-measure the accumulation curve; flattening = conviction; then fix = align the sigmoid_gating kernel's math with packed_recurrent (or accept per-token cost only at small T)"}
{"exp": "G1B_ROUTING_AB_NULL", "surface": "VLLM_GDN_SPEC_VIA_DECODE: verify GDN compute routed through the DECODE kernel per-token (conv-output layout, checkpoint protocol via state copies; routing confirmed ACTIVE in logs; four implementation bugs fixed on the way: dummy shape gates, wrong mixed layout, batch-dim assignment)", "legs": {"d13_viadecode2_greedy": "800 out greedy + curve vs clean prefill: STILL DIRTY (0.10-0.22 mean, 5-16% >0.5nat, worsening with depth) — identical profile to the fused-path legs"}, "causality": "SPEC-GDN-KERNEL HYPOTHESIS DEAD IN-SITU: replacing the verify GDN kernel math entirely (decode-kernel formulation) leaves the curve unchanged. Combined with the layer probe (divergence compounds from layer 0, GDN-weighted, distributed), the corruption enters through the GDN layers' INPUTS — the residual stream computed by the verify batch's own projections", "verdict": "LAST STANDING SURFACE: the W8A8 GEMM small-M path — decode(M=1) == prefill(M=2048) to 0.001 but verify(M=2..14) differs; the AITER CK W8A8 kernel's small-but-not-1 M tiling. NEXT (offline, cheap): in-vitro W8A8 GEMM A/B — same weights/activations, M=1 per-row vs M=14 batched through the production GEMM path; systematic element divergence = conviction"}
{"exp": "G1B_W8A8_SMALLM_NULL", "surface": "in-vitro CK W8A8 GEMM A/B with production call pattern (gemm_a8w8_CK + pertoken_quant, K=4096 N=2048 GS=128 synthetic weights, realistic activations): M=14 batched vs 14x M=1 per-row", "result": "BIT-IDENTICAL (max|Δ|=0.0 on every element; xq/xs identical too — pertoken_quant is row-independent). The W8A8 small-M hypothesis is DEAD", "verdict": "every compute surface of the verify batch is now measured identical or ruled out in-situ: GEMMs (bit-identical), conv (bit-exact), GDN kernels (routing null), attention masking (correct), sampler (exact), state machinery (all exonerated)"}
{"exp": "G1B_UPSTREAM_SCAN", "surface": "shallow-clone of upstream vllm-project/vllm main (last ~300 commits) scanned for hybrid/spec/GDN correctness fixes", "result": "the ONLY spec-mamba commit is 263c4ff (fine-grained mamba prefix-cache checkpoints for EAGLE resume — a caching FEATURE, not correctness); no verify-path GDN correctness fix exists upstream. The user's premise (stock-vLLM bug, present on BF16) remains plausible but unfixed upstream", "verdict": "STRATEGIC POSITION: G1b is not in any single measurable surface of this fork. Remaining leads: (a) DECISIVE fork-vs-upstream discriminator — serve the PTQR model on STOCK upstream vLLM (bf16, spec on) and reproduce/deny the garble: denies => fork-local after all; reproduces => upstream bug, track/fix upstream; needs a separate pinned venv+build (bounded but big); (b) the irreducible cross-path GDN state-history divergence (verify checkpoint chains vs prefill chunk chains accumulate different rounding at ~1e-3/layer-token, amplifying at depth) — only addressable by design change (e.g., periodic state re-anchoring), not a bug fix per se"}
{"exp": "G1B_FORK_ARCHAEOLOGY", "surface": "git archaeology: fork's last upstream merge f9da7a95bd (v0.20-era, 2026-08-21); 409 fork-local commits since; complete enumeration of fork-local deltas to the target-side spec/GDN/sampler machinery (qwen_gdn_linear_attn 390+ lines, gdn_attn, FLA kernels, rejection samplers, mamba_utils)", "result": "the fork's target-side spec machinery is UPSTREAM v0.20 CODE — every fork-local delta is my own instrumentation, two ported guards (conv bounds, sigmoid zero-fill), and salt levers; NO port-time functional changes to kernel math. Upstream's post-v0.20 evolution (v0.28-era, 932 insertions) contains platform/CPU/Blackwell refactors and NO spec-GDN correctness fix. The stock-upstream reproduction test is now LOW-VALUE (same code would reproduce)", "reframe": "the curve PLATEAUS after onset (0.07-0.22 roughly flat, not linear growth) = decorrelation SATURATION (chaos-like), not an accumulating bug — but the OUTPUT DEGRADES (walls, meta-collapse, acceptance collapse), which mere decorrelation cannot explain: a decorrelated same-model trajectory yields coherent alternative text, not garbage. The real question reframes to: WHY does the verify-path trajectory DEGRADE rather than merely decorrelate", "verdict": "NEXT (cheap, from saved artifacts): quantify self-consistency degradation — engine p(committed) deep in legs (PRING saved target probs) vs clean-prefill p for the same tokens; if the engine's committed tokens become implausible under the clean model at walls = the engine is committing tokens its own context cannot support (state degradation), pinning where the degradation enters"}
{"exp": "G1B_SELF_CONSISTENCY_MEASURE", "surface": "engine p(committed) by output depth (d13 greedy leg, saved logprobs): mean 0.92 -> 0.69 declining but NO collapse (zero tokens below p=0.05; median stays 0.74-1.0)", "reading": "within 800 output tokens the engine stays self-consistent while |Δ vs clean-prefill| reaches 0.2+ nat — DECORRELATION-dominant (two self-consistent trajectories disagreeing), not degradation; the walls/meta-collapse appear deeper / at temp-1 where sampling from the decorrelated high-entropy distribution wanders into attractors greedy avoids. Mere kernel-identical numerics cannot be restored (three formulations exist); the divergence must be CLAMPED", "verdict": "PRAGMATIC MITIGATION QUEUED (in-recipe, quality-first): periodic GDN state RE-ANCHORING — every N output tokens (~512), recompute the GDN states over a recent suffix window via a mini-prefill (attention over full KV, GDN chained from the recomputed window), replacing the drifted verify-path states; clamps divergence growth regardless of its microscopic origin; cost ~1 short prefill per refresh. Design + prototype next"}
{"exp": "G1B_HANDOFF_AUDIT", "surface": "GDNSTAT pre+post capture around the prefill->decode handoff (d14 leg); bucket-0 dirty positions identified as 2-30 (first 2-3 verify rounds) in EVERY leg across NS/prompt-length variants — previously dismissed as early-phase noise, it is real and the strongest localization yet", "findings": "SSM handoff SOUND: the first decode round reads slot 0 (real prefill-end state 156-169), writes all 14 window slots (post shows real values incl. former NaN slots), subsequent rounds read slots the previous round wrote — the NaN/stale window slots (6-13) at the handoff are future-position blocks never read before written. CONV QUESTION OPEN: the spec kernel's conv_state_indices = si[:,0] = bt[r, col+1] (the gather's first column) while the maintained rolling buffer (norm 929, real) sits at my rel-0 = column col — the captured conv norms show col+1 = 0.2402 INIT at round 1; if the kernel truly reads col+1, the first rounds' conv history is INIT (wrong logits at exactly positions 2-30); spec-off reads the same gathered column yet its bucket-0 dirt is only ~1% vs spec's 15-20% — a 15x spec-specific amplification at the same surface", "verdict": "NEXT: nail the conv column identity — GDN_RING with .clone() at append (last capture was a stale shared buffer) to read the exact si rows the kernel receives at rounds 1-3; if si[:,0] == bt[r,col+1] and that column holds init conv, the handoff conv seeding is the bug and the fix is to seed/maintain the conv rolling buffer at the column the spec path reads"}
{"exp": "G1B_RING_CLONE_FIRST_DATA", "surface": "corrected GDN_RING (clone at append) finally captured REAL per-step spec index rows: prefill steps show sequential blocks (43-56, 188-201); the first spec round's live si row = [53, 54, 55, 56, 154, 169, ...] — si[:,0]=53 continues the prefill's allocation sequence (the block for the next position)", "status": "cross-boot comparison invalid (block ids differ per boot); the decisive question — is block 53's conv rolling buffer SEEDED (real history) or INIT at round 1 — requires GDNSTAT (per-column conv norms) + GDN_RING (physical si ids) in the SAME boot", "verdict": "NEXT CYCLE: one boot with BOTH GDNSTAT+GDNRING flags, short leg; map si[:,0] to its logical column and read that column's conv norm from GDNSTAT: real (929-like) = handoff conv sound, look elsewhere; init (0.24) = THE BUG (spec handoff never seeds the conv buffer the spec path reads), fix = seed it at the boundary (align migration must also migrate conv to the gather-base column)"}
{"exp": "G1B_HANDOFF_CONV_OFF_BY_ONE_CONVICTED", "surface": "same-boot GDNSTAT+RING capture at the prefill->spec-decode handoff (d17): GDNSTAT first decode round (nct=20069, state_idx col=11) shows the REAL conv rolling buffer (norm 769.6) at logical column 11 = state_idx, while columns 12+ (the align gather base start+1..start+14 that spec_state_indices[:,0] resolves to) hold INIT (0.2) conv and NaN/stale SSM; RING confirms the spec kernel's si row starts at the gather base column (sequential continuation of the prefill allocation)", "mechanism": "the align gather (mamba_get_block_table_tensor, align mode) reads bt[r, start+1 .. start+14] where start=(seq_len-1)//1728; the maintained running conv/ssm state lives at bt[r, state_idx] (col 11 at the handoff — MID-BLOCK: nct 20069 is inside block 11, so NO block boundary crossed, so preprocess's advance+precopy DID NOT RUN and never seeded column 12). The first verify rounds therefore read INIT conv history (0.2-norm buffer instead of the real 769.6) for kw-1 taps and unseeded SSM context — wrong logits at exactly positions 2-30, the bucket-0 dirt present in EVERY leg. Spec-off is mostly clean because the non-spec decode reads with offset-0 semantics that hit the maintained column until a crossing migrates it", "verdict": "FIX: seed the spec gather-base column at the handoff — when the gather base (start+1) != state_idx and the window is fresh (first spec round after prefill), copy the running conv+ssm state from bt[r, state_idx] to bt[r, start+1] (a boundary-style migration without the boundary). Implement in the preprocess align path; validate: bucket-0 curve dirt gone, full-read temp1 legs, then the deep curve"}
{"exp": "G1B_HANDOFF_FIX_VALIDATION", "surface": "seed_spec_window fix live (identity conv+SSM copy bt[state_idx] -> bt[state_idx+1] on each request's first spec round); greedy 800-token curve leg vs clean prefill", "result": "DEEP DIVERGENCE ELIMINATED: buckets 128-768 all at FLOOR (mean 0.0002-0.0039, ZERO positions >0.5nat) vs 5-16% dirty before the fix; bucket-0 residual remains (positions 2-30, n=24) — a separate first-rounds effect, NOT the accumulating corruption", "verdict": "the long-output garble mechanism (accumulated verify-path drift) is FIXED at its source. NEXT: deep 1600-token curve to confirm the floor holds past the historical onset, then FULL-READ temp-1 leg validation, then G2 re-measure (acceptance should recover)"}
{"exp": "G1B_HANDOFF_FIX_DEEP_CURVE", "surface": "1600-token greedy curve with the handoff fix: buckets 128-800 at FLOOR (the fix's territory, including the block crossing at output ~667 which now passes cleanly), but the 800+ dirt RETURNS (0.12-0.22 mean, 4-15% >0.5nat) at similar magnitude to pre-fix", "reading": "TWO-COMPONENT model confirmed: (1) handoff component — FIXED (identity-state seeding cleaned the entire mid-range); (2) deep component (~output 800+, correlated with the draft acceptance collapse region) — still open. The mid-range cleanliness may already prevent the temp-1 walls (sampling errors compound from drift; less drift = later/no attractor entry)", "verdict": "RUNNING: full-read temp-1 1600-token leg — the quality verdict; then G2 acceptance re-measure"}
{"exp": "G1B_HANDOFF_FIX_TEMP1_VERDICT", "surface": "FULL-READ temp-1 1600-token leg with the handoff fix (d20): manual line-by-line read", "result": "GARBLE PERSISTS: coherent essay through line 10, degradation at line 11 ('suggests aches the indexing system implies...'), mid-sentence meta-collapse at line 15 (~600-800 output) — matching the deep-component onset, NOT the fixed mid-range", "verdict": "handoff fix = genuine correctness fix (curve-proven mid-range floor; keep it permanently) but the temp-1 long-output garble is carried by the DEEP component (~output 800+). G1b goal NOT yet met. The deep component correlates with the acceptance-collapse region and remains the open target; next angles: (a) the same seeding logic at BLOCK CROSSINGS (the deep onset ~800 = nct ~20870 — the SECOND window region; crossings seed via the postprocess copy but the +1 column may again be unseeded there), (b) acceptance-collapse coupling — fix the deep state divergence and both garble and speed recover"}
{"exp": "G1B_CROSSING_SEED_RESULT", "surface": "seed_spec_window extended to state_idx advances (block crossings); deep 1600-token greedy curve re-measured", "result": "deep 800+ dirt UNCHANGED (0.12-0.19 mean, 5-13% >0.5nat) — the deep component is NOT the crossing +1-column hole; mid-range floor retained; both fixes stay (mechanistically correct: measured INIT at the handoff column; migrations never target the window base)", "verdict": "deep component remains open after two mechanistically-correct fixes; its onset (~800 out = nct ~20870) has no structural event; correlates with the draft acceptance-collapse region. NEXT SESSION: the deep component likely needs the layer-probe re-run WITH the fixes (where does the divergence now enter — the profile pre-fix showed layer-0-input entry) or the acceptance-coupling angle (fix draft context = fix both)"}
{"exp": "G1B_DEEP_COMPONENT_TWO_MORE_NULLS", "surface": "(1) routing A/B re-run WITH the handoff+crossing fixes (routing ACTIVE confirmed): deep 800+ dirt UNCHANGED (0.12-0.24 mean, 5-16% >0.5nat) — spec-GDN kernel formulation definitively exonerated as the deep component; (2) W8A8 GEMM three-way M-shape test: M=2048 vs M=1 (32 rows) and M=2048 vs M=14 slice — ALL BIT-IDENTICAL — KV-write projection inputs are row-independent across all batch shapes", "elimination": "deep component is NOT: GEMM shapes (all M identical), GDN kernel formulation (routing null on fixed base), handoff seeding (fixed), crossings (fixed), sampler, draft values, masking, lm head, KV leaks, state machinery. The ONLY untested compute surface remaining: the UNIFIED ATTENTION READ at T=14 queries over int8-g128 KV (verify) vs T=1 (decode, proven clean) vs T=2048 (prefill comparator) — the three-way read test", "verdict": "NEXT: in-vitro unified-attention T-shape A/B — same int8-g128 cache, identical queries, T=1 vs T=14 vs T=2048 reads, compare row 0. A T=14-specific read error would explain the entire deep profile (verify reads diverge from both clean paths, growing with context)"}
{"exp": "G1B_TSHAPE_INVITRO_INCONCLUSIVE", "surface": "in-vitro unified-attention T-shape A/B on synthetic int8-g128 KV (random values/scales, CTX=8192): row0 diffs T=14-vs-T1 mean 0.000365, T=2048-vs-T1 mean 0.00177, T=2048-vs-T14 mean 0.00175 (out scale 0.0096)", "reading": "all shapes show shape-dependent numerics noise; T=14 is CLOSER to T=1 than T=2048 is — opposite of a T=14-specific bug; and the magnitude contradicts the in-situ pattern (decode==prefill clean at 0.001nat, verify dirty). The synthetic harness's noise floor swamps any shape-specific signal — INCONCLUSIVE; real-distribution cache needed for a decisive test", "verdict": "deep component STILL OPEN. G1b state: two shipped fixes (handoff+crossing seeding, mid-range eliminated); deep 800+ survives all exonerations; remaining lead: re-run the layer-probe WITH the fixes to localize where the deep divergence now enters; also pending G2 re-measure"}
{"exp": "G1B_LAYERPROBE_FIXED_BASE", "surface": "layer probe re-run with handoff+crossing fixes live (leg eager boot 800 greedy + replay compiled boot, same lp3 dir; 3,885 (pos,layer,tok) pairs)", "result": "BIMODAL: most paired positions show small divergence (n=96 population per layer, 1-cos 0.003-0.07) but a ~16-position population shows LARGE divergence (0.14-0.31) across BOTH GDN and ATTN layers — and layer 0/1 already show the large population (0.005/0.20) — the divergence ENTERS THE RESIDUAL STREAM before layer 0 at those positions (input-side), consistent with accumulated context/KV-state divergence over the output region rather than any single layer's compute", "verdict": "deep component = input-carried (context state) at deep positions, not layer-compute; all layer classes affected equally. Combined with all kernel exonerations: the remaining divergence lives in the CONTEXT the verify path accumulates (output-region KV + GDN state trajectory). Next: G2-coupled angle (acceptance collapse shares this) or accept-and-mitigate via periodic re-anchoring; server left with probe env off, production posture"}
{"exp": "G2_REGRESSION_SCARE_RESOLVED", "surface": "post-fix C6 bench measured 84-90 tok/s (vs 202.6 baseline) — suspected the fixes; crossing-seed extension was ALSO acceptance-negative (k collapsed to ~2, REVERTED to first-round-only seeding) — but the real cause was the EAGERALL flag file left over from the layer-probe cycle: every boot since ran enforce-eager (no compile/cudagraphs)", "result": "flags cleared + crossing revert: C6 = 215.94 tok/s, TPOT 11.98ms — AT/ABOVE the 202.6 baseline; no code regression. LESSON (again): check the flag-file dir before believing any post-restart measurement", "verdict": "current posture: first-round handoff seed only (curve-verified), production speed intact (215.9 single rep); the acceptance-collapse (deep component) remains the coupled garble+speed target"}
{"exp": "G1B_POSTRESTORE_TEMP1", "surface": "temp-1 1600-token leg on the restored production posture (fixes live, eager cleared, 215.9 tok/s)", "result": "GARBLE PERSISTS (full manual read): this trajectory degenerated early in the reasoning phase (broken lists from line 15, truncated words, repeated indices) — a different trajectory than d20 but same degradation class; the deep/context-accumulation component remains", "verdict": "re-anchoring (suffix mini-prefill) remains the primary mitigation path; next session: design + prototype"}
{"exp": "G1B_SPECOFF_CONTROUVERTED", "surface": "spec-off (draft fully removed) temp-1 1600-token legs, 4 seeds (d27 seed5, d28 s11/s23/s37) on the current stack", "result": "4/4 GARBLE with the same mid-sentence collapse into meta-planning ('...aThe user wants me to write...') at ~700-1500 output tokens (s23 degenerate planning throughout). The earlier d1-era 2/2-clean spec-off reads (18-21KB legs) were an earlier code state or lucky trajectories — the control does NOT hold on the current stack", "reframing": "the garble is NOT spec-specific on the current stack: it appears in the pure decode path at temp-1 long output. Suspect set narrows to (a) the vLLM base serving path (any dtype — matches the user's BF16-also-broken premise), (b) the PTQR-retrained int8 weights degrading temp-1 sampling at depth, or (c) the task itself. Greedy always clean", "verdict": "NEXT (decisive): dense bf16 REFERENCE forward (PTQR trainer machinery) sampled at temp-1 on the same 20k corpus prompt — clean output implicates the PTQR weights; garbled output implicates model/task and contradicts the other-runners-clean premise. This is now the critical path for G1"}
{"exp": "G1B_BF16REF_DECISIVE", "surface": "bf16 reference checkpoint (/home/curved/models/Qwen3.8-27B-bf16-ref, dtype bfloat16, no quant, no int8-KV, no AITER W8A8; NCCL_ALGO=Ring to boot around the gfx908 Tree/LL default) served through the same engine on :8021; temp-1 1600-token leg on the same 20k corpus task (d29)", "result": "COHERENT through all 1600 tokens (full manual read): planning-style text but no mid-sentence collapse, no meta-commentary wall, no repetition walls \u2014 dramatically different from every PTQR-int8 temp-1 leg (4/4 spec-off + all spec legs garble)", "conclusion": "THE USER'S BF16-ALSO-BROKEN PREMISE IS DISPROVEN ON THIS STACK: bf16 is clean through the same engine/task; the garble lives in the INT8 SIDE (PTQR weights, int8-g128 KV, or W8A8 GEMMs). This also invalidates my 'engine base path' suspect from the control-overturned entry", "verdict": "NEXT DECISIVE SPLIT: serve the ORIGINAL published GPTQ-8bit checkpoint (pre-PTQR, curvedinf/Qwen3.8-27B-GPTQ-INT8-W8A8-GS128 at <models>/Qwen3.8-27B-GPTQ-8bit-gs128) with the int8 recipe \u2014 clean => PTQR retraining implicated (long-context temp-1 fidelity not covered by the KLD objective); garbled => int8-stack (KV quant / W8A8) implicated", "user_goal_correction": "USER-GOAL CORRECTION: n=1 clean 1600-token bf16 leg at temp 1.0 is UNDERPOWERED for a failure mode whose onset is 1-2k+ tokens; it does not disprove the user's assertion that bf16-on-vLLM shows the same drift (user instruction: treat as true, do not verify/disprove). CORRECT CONCLUSION PER USER GOALS: bf16-on-vLLM is ASSUMED BROKEN; the goal is the pure-int8 PTQR stack with long-in/long-out garbles completely eliminated (verified by full manual reads) \u2014 the bar is the model's true capability, not parity with bf16-vLLM output. This row must not be used to redirect away from the int8 work."}
{"exp": "G1B_ROOT_CAUSE_MATRIX", "surface": "temp-1 1600-token legs, same 20k corpus task, full manual reads, same engine: (1) bf16 ref weights + auto KV = COHERENT; (2) GPTQ-8bit + int8g128 KV = GARBLES (mid-sentence meta-collapse); (3) GPTQ-8bit + auto(fp16) KV = GARBLES (grammar degrades EARLIER); (4) PTQR + int8 KV + int8 lm-head = GARBLES; (5) PTQR + int8 KV + fp16 lm-head (INT8HEAD=0) = GARBLES (word-merge + word-per-line wall + meta-collapse)", "conclusion": "ROOT CAUSE: the W8A8 int8 WEIGHT quantization of the body \u2014 not KV dtype (fp16 KV garbles worse), not the lm-head (fp16 lm-head doesn't help), not spec decode (spec-off garbles), not the engine (bf16 clean through it), not PTQR-specific (original GPTQ garbles identically). Int8-body logit noise at rank-2..20 perturbs temp-1 sampling; errors compound through the sampled trajectory over long output; greedy masks them (argmax robustness). Matches every earlier observation including the acceptance collapse (draft faces the same noisy logits)", "verdict": "PATHS within the user's directives: (a) PTQR rung-2 with a SAMPLING-AWARE objective \u2014 sampled-trajectory KLD at temp-1 over long contexts (the teacher-forced KLD 0.0069 is insufficient); (b) selective fp16 promotion of the worst-offending W8A8 layers (user sanctioned 'limited FP16' where int8 is not precise enough) \u2014 needs a per-layer logit-error ranking instrument to pick them", "user_goal_correction": "USER-GOAL CORRECTION: the 'bf16 = COHERENT' cells were single 1600-token legs \u2014 underpowered. Under the user's standing assumption that bf16 also garbles on vLLM, 'int8 weights are THE root cause' is NOT established; a vLLM-side fault shared across dtypes remains possible. CORRECT CONCLUSION PER USER GOALS: continue the pure-int8 PTQR/QAT program toward zero garbles and 300 tok/s at C6; do not use this matrix to gate or redirect the int8-stack goal."}
{"exp": "G1B_PATHB_LAYER_RANKING", "surface": "Per-layer int8-vs-bf16 error ranking over the d30 transcript (20040-token prompt + 1600 committed temp-1 tokens): banked engine LAYERPROBE records (rank0 only; 1,365,848 paired after dropping 144,152 dummy-profiling records; tok crosscheck passed on every retained record) paired with an offline bf16 TP4 reference forward (HF Qwen3_5TextModel via scripts/bf16_layer_capture.py \u2014 mlp-stream projections onto the identical randn(5120,16,seed 1234) fp32 matrix; TP4 replication check exact 0.0; peak 15.5 GiB/rank). Instrument: scripts/garble_rank_layers.py; artifacts logs/garble/rank_gptq/ + logs/garble/rank_bf16/", "result": "ERROR IS DIFFUSE: decode energy (sqrt(sum|d|^2/sum|pb|^2) on the layer-contribution stream) starts 0.10 at L0 and plateaus 0.15-0.32 with top-10 layers (L62 0.32, L32/33 0.31, L30 0.30, ...) only 1.3x the median \u2014 no subset dominates; prefill flat 0.37-0.42 across ALL 20k positions (no context-length growth); decode flat ~0.24 after a 1.6x transient in the first ~300 output tokens (worst spots p20100-p20167); error does NOT grow with output length", "conclusion": "Selective fp16 promotion (Path B) cannot fix G1b: the deviation is a constant-magnitude noise floor across every layer and position, not concentrated anywhere. The garble is the sampled trajectory of a ~25% -perturbed process crossing coherence thresholds, not accumulating stream error \u2014 matches G1B_ROOT_CAUSE_MATRIX (int8 body weights) and explains why teacher-forced KLD 0.0069 didn't prevent it (the objective never visits the student's own sampled states)", "verdict": "GO PATH A: PTQR rung-2 with a sampling-aware (DAgger-style) objective \u2014 autoregressive student rollouts at production params (temp 1.0, top_p 0.95, top_k 20) from 8-20k-token prompts, relabeled per-position with frozen-bf16 teacher KLD over the student trajectory; continue from R10S60 weights; gate = temp-1 1600 FULL MANUAL READ clean + teacher-forced KLD not regressed + acceptance recovery", "user_goal_correction": "USER-GOAL CORRECTION: this instrument measures deployed-int8 vs offline-bf16 divergence. Under the user's standing assumption (bf16-on-vLLM also broken), diffuse int8 noise cannot be identified as THE garble cause \u2014 it is additive noise, possibly on top of a shared vLLM fault. CORRECT CONCLUSION PER USER GOALS: valid as an int8-stack measurement informing calibration/PTQR; not evidence for abandoning the int8 dtype work."}
{"exp": "G1B_PATHA_DAGGER_BASELINE", "surface": "DAgger (student-visited-state) baseline + rung-2 data collection: 24 rollout legs from the deployed R10S60 engine at PRODUCTION params (temp 1.0, top_p 0.95, top_k 20; 8k/12k/16k/20k prompts x 2400 out) via drift_leg with return_token_ids (exact committed ids); wave-0 legs manually read: mild (late meta-collapse), medium (definition list + anchor-repetition wall), extreme (immediate token soup with self-aware flashes) — all three G1 severities present in training data; held out legs 21-24 for rollout-val", "result": "rollout KLD/tok (masked to committed region, ctx-window 3072, tau 0) = 0.0225 for R10S60 vs bf16 teacher ON ITS OWN TRAJECTORIES, vs 0.0069 teacher-forced on corpus — 3.3x gap: the states the deployed student actually visits (incl. drift/meta-collapse regimes) are where the int8 noise bites, and rung-1 never trained on them", "conclusion": "Quantifies the DAgger gap that teacher-forced PTQR misses; rung-2 trains per-position bf16-teacher KLD over the student's own sampled trajectories (20 legs, ctx 3072 + full 2400-token rollout per sample, lr 5e-6, tau 0.001, init R10S60 step60, 40 steps/2 epochs, teacher-body file-swap for 32 GiB fit — see commit 25b4dd63d7)", "verdict": "GATE after r11: (a) rollout-val KLD must drop materially below 0.0225 without corpus KLD regressing (logged every 10 steps); (b) export -> serve -> temp-1 1600 leg FULL MANUAL READ clean (the user's bar); (c) acceptance recovery check (same-noise symptom)"}
{"exp": "G1B_PATHA_R11_VERDICT", "surface": "PTQR rung-2 DAgger (r11): 40 steps, 20 rollout legs (ctx 3072 + full committed tail), lr 5e-6, tau 0.001, init R10S60; rollout-val curve 0.02250->0.02248->0.02177->0.02220 (steps 0/10/20/30; final eval skipped by islice gap) = FLAT within noise; corpus val 0.0496->0.0538->0.0292 (vs R10S60 0.0069) = structural corpus regression; same-leg train KLD -13%/epoch = leg memorization, no transfer", "result": "FULL MANUAL READ of the exported R11S20 (temp-1, 20k in, 1700 out): CATASTROPHIC REGRESSION - meta-collapse from token 0 ('The user wants me to write...') then ~430 lines of bare '-' repetition wall (1.7 chars/token); no coherent content at all, far worse than R10S60's late-drift pattern", "conclusion": "Drift-state KLD fine-tuning with a weak corpus anchor contaminates the general distribution: the corpus-KLD regression (4-8x) was the real health signal, not mid-run flux as I initially read it; rollout-KLD is an insensitive proxy (flat while reads collapse). PTQR end-to-end KLD on student-visited states is DISCREDITED as the objective at this scale (48k positions vs 497 quantized tensors; zero in-metric benefit, harmful out-of-metric)", "verdict": "PIVOT to reconstruction-level: re-quantize GPTQ GS128 with calibration EXTENDED with long-context + drift-state sequences (the r2roll legs are exactly the failure-regime manifold; the original calibration was 512 short samples 256-2048 \u2014 never saw deployment regime). r11 artifacts purged; R10S60 restored to production. KLD<->read calibration: corpus KLD ~ general health; treat >2x R10 baseline as disqualifying", "user_goal_correction": "USER-GOAL CORRECTION: the same-engine read comparison (R11S20 worse than R10S60) stands as measured, but the 'pivot to re-quant' framing presumed int8-as-root-cause. CORRECT CONCLUSION PER USER GOALS: the native-int8 PTQR/QAT program continues (weights+scales retuning, int8-native draft); only the DAgger drift-state objective is discredited. Zero-garble manual-read bar and 300 tok/s C6 unchanged."}
{"exp": "G1B_LONGCTX_REQUANT_READS", "surface": "GPTQ re-quant with deployment-manifold calibration (24 r2roll transcript tails incl. full drift output, capped 4096 by gfx908 SDPA-math-only ceiling + on-device capture budget; 156 samples/236k rows) \u2014 same GS128 W8A8 sym contract; served on :8020; temp-1 1700-token legs FULLY READ at 20k/16k/8k (lcv1-3)", "result": "PARTIAL IMPROVEMENT, GARBLES PERSIST: lcv1 (20k) coherent ~1000 tok then meta-collapse at a section transition (collapse text coherent planning, no soup/walls; 5.0 chars/token vs 4.0 healthy baseline); lcv2 (16k) coherent ~1300 tok with one RECOVERED word-salad island, then same transition collapse; lcv3 (8k) NEVER left planning mode -> hyphen wall (no improvement); acceptance still collapsed (7.8 tok/s single-stream)", "conclusion": "Long-context/drift calibration extends the coherent phase and softens the collapse signature but does not eliminate the mode-flip at high-entropy (topic-transition) boundaries \u2014 consistent with structural W8A8-GS128 grid noise at temp-1. In-contract levers asymptote: PTQR-r1 (0.0069 corpus KLD) garbles, DAgger harmful, requant partial. bf16/fp16 body remains the only read-clean configuration measured", "verdict": "Remaining substantial levers need user input on contract: (a) GS64/GS32 groups (user's G128 standardization would need amending; halves grid error ~sqrt(2) per step) or (b) rotation/outlier-folding re-quant (QuaRot-style, engine-invisible, multi-day, GDN-folding risk). Cheap incremental in-contract: re-quant with original 512-short weight restored + long tiers (corpus weighting was cut 4x this run). Production restored to R10S60 (known quantity; longctx 8k-leg regression not acceptable for unattended serving)", "user_goal_correction": "USER-GOAL CORRECTION: 'bf16/fp16 body = only read-clean configuration' rests on underpowered single legs. Under the user's standing assumption (bf16-on-vLLM also broken), that comparison is void as a redirect. CORRECT CONCLUSION PER USER GOALS: this partial improvement informs int8 calibration work only; the pure-int8 PTQR stack remains the goal; zero-garble full-manual-read validation and 300 tok/s at C6 are the bars."}
{"exp": "G1_GDN_RESET_AND_NONDET", "surface": "GDN state decode-vs-prefill probe on R10S60 int8 stack (20k prompt + 3.2k temp-1 decode, then exact-transcript re-prefill + 600 decode; VLLM_GDNSTAT norms, 3840 rounds): (1) leg-B full read shows reset-recovery REPRODUCED \u2014 brief soup continuation of the drifted tail, then chat-structure recovery; the model itself labels the drifted tail 'a generation error' in-context; (2) same-prompt same-seed (601) same-params replay of leg B diverges at token 10 (13/600 shared) \u2014 engine generation is NOT reproducible under seed (prime suspect: unseeded draft sampling; target-path nondeterminism not yet excluded); (3) same-tokens prefill chunks across sessions differ 30-45% in GDN state norms (re-prefill sessions under the offload connector's preemption) \u2014 GDN state is chaotic w.r.t. batch/scheduling-dependent compute; (4) decode-vs-prefill boundary state error reaches 99% at layer 0 st0/st1, decreasing with depth (0.22 at L26) \u2014 exceeds the 45% same-path floor at early layers only", "result": "The garble is a stochastic trajectory property (stable ~1-1.3k-token time-to-collapse, not reproducible token-wise) with reset-recovery confirmed instrumented; GDN state norms cannot separate 'decode-path defect' from 'chaotic amplification' at depth because the same-path floor (30-45%) overlaps the boundary error at most layers", "conclusion": "Norm-level GDN analysis is exhausted; the decisive test is VALUE-level comparison at exactly matched tokens: same short sequence through (a) one-shot prefill and (b) decode-path processing with spec OFF and no re-prefill interference (GDN_DUMP value rows), plus a same-path value floor (run twice)", "verdict": "user_goal_correction applies to prior attribution rows; this row is engine-behavior evidence only \u2014 next: VLLM_GDN_DUMP_DIR pair with SPECOFF for value-level decode-vs-prefill error, unconfounded by draft sampling and preemption re-prefills", "retraction": "PARTIAL RETRACTION: items (3) cross-session norm floor 30-45% and (4) decode-vs-prefill boundary 99% used the same pooled-rank comparison \u2014 invalid (cross-rank shards). Items (1) reset-recovery (behavioral, full read) and (2) same-seed irreproducibility (behavioral) STAND. GDN state determinism is UNRESOLVED, not established."}
{"exp": "G1_GDN_TWIN_PREFILL_NONDET", "surface": "Value-level GDN state probe (GDNSTAT slice-all patch; MNBT=512 for fine chunk boundaries): identical ISOLATED twin prefills (same tokens, greedy max_tokens=1, back-to-back, alone in scheduler, spec on but no decode) on R10S60 int8 stack \u2014 window geometry verified aligned (col/ri/seq matched; only allocator block ids differ)", "result": "st1 (recurrent value state) ORTHOGONAL across twins: relRMS 1.378 @~1k tokens, 1.374 @2k, 1.36-1.42 @4k/8k/23k \u2014 saturated from the earliest observable point, NOT a gradual length-growing chaos curve; initial states at nct 0 bitwise identical (0.0000) and st0 conv state matched EXACTLY at some positions while st1 diverges completely", "conclusion": "The GDN recurrent state for the SAME sequence is effectively arbitrary across identical runs by ~1k tokens: something nondeterministic enters between token 0 and ~1k and the recurrence carries it to orthogonality. Consistent with the user's standing claims (bf16 also broken \u2014 dtype-independent state path, not GEMM dtype; other runners fine \u2014 their GDN state path is per-run deterministic) and with this fork's race history (custom-AR memory-ordering race fixed earlier; gdn_attn.py carries race-bisect levers next to the dump code). Prime suspect: a race or nondeterministic write in the GDN state path (chunked prefill state writes / window migration / cross-rank reduction order in TP4)", "verdict": "NEXT: race-bisect the GDN state path \u2014 apply the existing bisect levers (sleep/stream/device sync variants in gdn_attn.py) and rerun the 1024-twin; a masking lever that restores twin agreement localizes the racy kernel; then fix ordering. All comparisons in this row are engine-internal (int8 stack only; no bf16 legs)", "retraction": "RETRACTED (instrument artifacts, two independent): (1) the divergence numbers came from pooling per-TP-rank shard files \u2014 pairs compared different ranks' head-shards of the same state, orthogonal by construction; per-rank pairs show bitwise-identical values. (2) the identical values are themselves trivial: with prefix caching on, same-prompt reruns are cache HITS (no recomputation; first cold run reads an empty window). No valid evidence either way on GDN prefill state determinism. A valid probe requires cache-evicting twins."}
{"exp": "G1_R12_READ_VERDICT", "surface": "r12 (corpus KLD at seq 6144, init R10S60, lr 1e-5, 40 steps): baseline R10S60@6144 val KLD 0.0544 (measured via lr=0 no-op run); r12 val 0.0439->0.0367->0.0411 (steps 10/20/30) = 25-33% BETTER at the long window, gate passed; exported R12S40, served, FULL MANUAL READ at exact repro regime (20k in / 4k out, temp 1.0, prod params)", "result": "READ FAILS THE BAR with a changed signature: content coherence extends to ~3.3k tokens (essay structure + grammatical sentences through section IV; vs ~1-1.3k on R10S60) BUT formatting-drift class prominent from ~1k: word stutters (corridors corridors corridors), then hundreds of lines of ONE-WORD-PER-LINE output with content still coherent, then the familiar meta-collapse at ~3.3k", "conclusion": "PTQR improvements move content coherence but not the formatting drift; the newline-per-word + stutter signature while grammar survives points at the SAMPLING PATH (distribution-level deviation: temp/top_p/top_k/min_p implementation or ordering) rather than model quality — consistent with the user's standing assertion (present on bf16 vLLM, absent in other runners, greedy clean, temp-1 broken) and would also explain acceptance collapse (draft samples through the same path)", "verdict": "NEXT: sampler numerical verification — drive vLLM's v1 sampler with a controlled logit distribution, ~100k draws, compare empirical sampled distribution vs the exact temp/top_p/top_k/min_p reference; any statistically significant deviation = the engine-side fault. R12S40 not production (read failed); R10S60 stays"}
{"exp": "G1_SAMPLER_CONVENTION_DEVIATION", "surface": "Static finding in the sampling path: vLLM reference apply_top_k_top_p_pytorch (vllm/v1/sample/ops/topk_topp_sampler.py:386-425) applies top-k then top-p over the RENORMALIZED survivor distribution (softmax AFTER the top-k mask); the fork's ROCm path (TopKTopPSampler.forward_hip -> aiter ops/sampling.py top_k_top_p_sampling_from_probs, incl. the fork-added top-k-first fast path with p'=p/S scaling) applies the top-p threshold on the ORIGINAL un-renormalized mass (aiter fused kernel semantics, documented in aiter/ops/sampling.py:60-74)", "result": "The two paths sample from DIFFERENT distributions at every joint k+p random-sampling step: reference accepts the smallest prefix with renormalized mass >= p (original-mass threshold p*S); aiter path accepts smallest prefix with original mass >= p — equal only when S=1 (top-k survivors carry all mass). At prod params (temp 1.0, top_k 20, top_p 0.95) the deviation flips direction with S: flat steps (S~0.7) aiter keeps MORE support than intended; peaked steps (S~0.99) aiter truncates MORE. Also: per-request seeded requests fall back to forward_native (generators non-empty) — seeded validation legs and unseeded production traffic sample through DIFFERENT samplers", "conclusion": "A concrete, dtype-independent, fork/ROCm-local sampling distribution defect: matches every constraint of the user's G1 claim (bf16-vLLM also broken — sampler is dtype-blind; absent in other runners — standard semantics; greedy clean; temp-1 long-output drift — distribution error compounds over sampled trajectories; acceptance impact — draft samples through the same aiter path). Explains formatting drift (wrong-tail sampling favoring modal-token repetition) alongside surviving content coherence", "verdict": "ACTIONS: (1) quantify the empirical distribution deviation (controlled logits, ~100k draws, aiter vs reference); (2) fix: route forward_hip joint k+p through correct semantics (renormalize survivors then apply p, or force forward_native); (3) unseeded A/B read legs pre/post fix — the fix must also make seeded validation legs representative of production traffic"}
{"exp": "G1_SAMPLER_FIX_AB_VALIDATED", "surface": "Unseeded (production-representative) A/B read legs on R10S60, 20k in / 4k out, temp 1.0 top_p 0.95 top_k 20, full manual reads: POST-fix (aiter top-k-renorm + nucleus-p, commit 45c277c34c): sfA1 clean scholarly essay to ~2.9k tokens then mild meta-collapse; sfA2 clean to ~2.2k then 'the'-wall + meta-collapse; PRE-fix control (VLLM_SAMPLER_ORIGINAL_MASS=1): sfB1 NEVER leaves planning mode, 'anchor,' x200+ repetition wall immediately, dead at 1043 tokens — worst read of the session", "result": "Sampler fix causally validated as a major improvement: the original-mass sampler's pre-fix behavior (production path, unseeded) is catastrophically degraded vs the corrected semantics — clean-phase extension from ~1k (immediate drift) to 2.2-2.9k tokens; the pre-fix over-truncation feeds modal-token repetition walls exactly as the distribution analysis predicted", "conclusion": "One engine-side root contributor of G1 CONFIRMED AND FIXED: fork-local ROCm sampling semantics deviating from the vLLM reference. Residual: post-fix legs still collapse at 2.2-2.9k under CORRECT semantics — the remaining component is model-side (int8 trajectory noise), and the r12 model rung (25-33% better long-window KLD) has NOT yet been read under the fixed sampler", "verdict": "NEXT: stack r12 model + sampler fix — boot R12S40 (fix live), unseeded 4k legs, full reads; if clean to 4k the zero-issue bar may be met; also re-check acceptance + speed under the fix (draft samples through the same corrected path)"}
{"exp": "G1_R12PLUSFIX_REJECTED", "surface": "R12S40 + sampler fix, unseeded 20k/4k leg (rf1), full read: word-per-line drift begins within the FIRST paragraph, 'the'-wall by ~1k tokens — markedly worse than R10S60+fix (clean 2.2-2.9k)", "result": "Stacking hypothesis fails: r12's 25-33% long-window val KLD improvement does not transfer to read quality; its formatting fragility (already visible in the seeded r12v1 leg) dominates under corrected sampling semantics", "conclusion": "BEST MEASURED CONFIG: R10S60 + sampler fix (45c277c34c) — clean scholarly output to 2.2-2.9k tokens unseeded at 20k ctx (vs ~1k immediate drift pre-fix; vs 1-1.3k seeded/native). r12 rejected on reads. Residual collapse at 2.2-2.9k under CORRECT semantics is the remaining G1 target (int8 trajectory noise and/or further engine-side components)", "verdict": "Restore R10S60+fix as production. Next: acceptance + C6 speed under the fix (draft samples through corrected path — may recover the collapse); then the residual 2.2-2.9k collapse (candidate lines: more sampler-path anomalies; KV/GDN decode surfaces under corrected sampling)"}
{"exp": "G2_SPEED_UNDER_SAMPLER_FIX", "surface": "scripts/bench_quick.sh perf gate on R10S60 + sampler fix (45c277c34c), C6 server on :8020, results dir logs/c8_optimization/samplerfix_c6", "result": "Concurrent output throughput 321.69 tok/s (8 requested, 6 active on C6) — EXCEEDS the 300 tok/s C6 target; single-stream mean 88.95 tok/s (3 reps), TPOT 11.54 ms, TTFT 2076 ms — matches the healthy G3-era TPOT (12.17 ms). Acceptance healthy in the early/clean regime: the 'acceptance collapse at production params' is concentrated in the late-drift regime of long generations (recent 4k legs measured ~8.5 tok/s single-stream AFTER drift onset)", "conclusion": "The sampler semantics fix delivered BOTH read-quality improvement (clean 2.2-2.9k vs 1k pre-fix) AND the speed target (321.69 tok/s at C6 > 300 target) with healthy early-regime acceptance — the pre-fix original-mass sampler was suppressing draft acceptance and throughput in addition to feeding repetition walls", "verdict": "G2 bench target MET on the standard protocol; remaining gap is G1's residual long-output collapse at 2.2-2.9k tokens under correct semantics (late-regime acceptance collapse is downstream of target drift). Production config: R10S60 + sampler fix on :8020"}
{"exp": "G1_POSTFIX_READ_DISTRIBUTION", "surface": "Six unseeded 4k-out legs on production config (R10S60 + sampler fix, 20k in, temp 1.0 prod params), full manual reads: sfA1/sfA2/sfC1/sfC2/sfC3 + r12-model legs excluded", "result": "Clean-phase distribution: sfC3 pristine ~3.6k then meta-flip; sfC2 ~3.3k (one mild token-merge 'dismantlebreak' at 3.2k) then meta-flip; sfA1 ~2.9k then meta-flip; sfA2 ~2.2k then glitch->the-wall->meta; sfC1 OUTLIER (word-merges from ~0.8k, word-per-line ~1.5k, the-wall, meta). 0/6 legs fully clean at 4k (zero-issue bar NOT met); 5/6 clean to 2.2-3.6k with first issue = mild merge or the meta-flip itself; degradation ladder consistent: token-merge -> word-per-line -> repetition wall -> meta-flip ('The user wants me to...')", "conclusion": "Post-fix the stack produces genuinely clean long-form scholarly output for 2.2-3.6k tokens at 20k context, typically failing first via reasoning-mode intrusion (meta-flip) at a high-entropy transition; the residual is model-side int8 trajectory noise at rank-2..20 (per root-cause matrix) surfacing as mode confusion — engine sampler semantics are now correct (rejection-sampler path audited clean too)", "verdict": "Zero-issue bar still unmet (0/6 at 4k). Remaining levers: draft-verify interaction with the meta-flip (DFlash proposals may steer into planning-mode tokens), and the standing structural options (group-size, rotation/outlier folding) which need user input on contract"}
{"exp": "G1_SPECOFF_CONTROL_POSTFIX", "surface": "Spec-off unseeded leg (so1) on R10S60 + sampler fix, SPECOFF flag, 20k in / 4k out temp-1, full manual read", "result": "PRISTINE scholarly essay through sections I-VI (~3.7k tokens, best leg of the program; zero merges/walls/formatting drift) then the meta-flip mid-sentence at ~3.7k; post-flip = coherent planning text (no soup). Spec-off clean phase (3.7k) overlaps the spec-on distribution (2.2-3.6k, n=5) — no dramatic separation; note 983s for 4k (~4 tok/s) as expected without spec", "conclusion": "The residual meta-flip is NOT primarily draft-steering: spec-off does not materially extend the clean phase beyond the spec-on best cases. Residual G1 = model-side int8 trajectory noise surfacing as reasoning-mode intrusion at high-entropy transitions (consistent with root-cause matrix: rank-2..20 logit noise at temp-1)", "verdict": "Engine-side sampler work is complete (main + verify paths correct). Remaining quality gap is structural: the in-contract options are exhausted at the current quantization grid — GS64/GS32 group size or rotation/outlier-folding re-quant would change the grid itself and need the user's call on the G128 contract. Production stays R10S60+fix spec-on (speed target met); spec-off verified cleanest-single-leg but 20x slower"}
{"exp": "G1_BAL_REQUANT_READ", "surface": "Unseeded 4k leg (bal1) on the balanced long-context requant (gs128-longctx-bal: 384 short + transcript/fresh long tiers, G128 W8A8) under the sampler fix, 20k in, full manual read", "result": "Clean high-quality prose through sections I-III (~2.5k tokens; one minor duplicate stutter at ~0.5k) then fragmentation at ~2.4-2.5k and meta-flip — mid-distribution vs R10S60+fix (2.2-3.7k), NOT better; last unvalidated in-contract artifact now measured", "conclusion": "GPTQ requant family fully characterized under the sampler fix: no member (longctx, -bal, r12) beats R10S60 (PTQR rung-1) + sampler fix. In-contract quality levers on the G128 grid are exhausted with read verdicts banked", "verdict": "Production remains R10S60+fix. The only remaining quality levers change the quantization grid and await the user's contract decision: GS64/GS32 groups or rotation/outlier-folding re-quant"}
{"exp": "G2_SPEED_RETRACTION", "surface": "User reports production at ~5 tok/s; re-audit of G2_SPEED_UNDER_SAMPLER_FIX (394c28ca39)", "result": "The 88.95 tok/s single-stream and 321.69 tok/s aggregate were measured on 32-token inputs / 256-1000-token outputs (bench_quick defaults) — short-context microbenchmarks that do NOT reproduce production shape. My own prod-shaped unseeded legs (20k in / 4k out) ran 393-476s wall for 4000 tokens = 8-10 tok/s including prefill (spec-off: 4.1 tok/s), matching the user's ~5 tok/s report", "conclusion": "G2 target is NOT met under production-shaped traffic; the short-context bench masks long-context per-step decode cost (TPOT at 32-token context is 11.5ms but wall-rate at 20k context is ~100ms+/tok) and acceptance decay in the drift regime. bench_quick's 32-in default is an invalid proxy for this goal", "verdict": "Speed work reopens: measure TPOT/wall-rate as a function of CONTEXT LENGTH on continual prod-shaped completions (2k/8k/20k in, 4k out, unseeded, prod params); decompose into prefill / per-step decode cost / acceptance to find where the 10-20x degradation comes from (candidates: long-context attention+GDN per-step cost, offload-tier KV reads, acceptance decay after drift onset)"}
{"exp": "G2_SPEED_DECOMP", "surface": "TPOT-vs-context decomposition (scripts/tpot_decomp2.py; clean protocol: prefill from 1-token leg, decode from 800-token leg): single-stream prod config (spec-on, offload-on) 2k/8k/20k-in = 26.8/41.2/48.3ms TPOT (37.3/24.3/20.7 tok/s); spec-off control 49.1/107.8/228.5ms (spec is net-POSITIVE, 2-5x); offload-off identical at 8k+ (costs 2x at 2k only); fp16-KV identical (KV dtype exonerated); prefill fast (0.7/3.7/15.3s); GPU-bound at 100% util all 4 GPUs", "result": "Target per-step cost scales with context (~10us/ctx-token, saturating at long ctx with spec). User's prod ~5 tok/s = measured 20.7 tok/s single-stream at 20k ctx divided across ~4-6 concurrent long-ctx streams (per-stream share) — consistent. Attribution attempts blocked: py-spy denied (no sudo), rocprofv3 config schema rejected/'no tracing options' + empty output dir, per-layer python timing hook broke CUDA-graph capture (decode replays as a graph — the O(ctx) loop must live inside a captured kernel reading seq_len from device memory)", "conclusion": "Root perf defect: a per-step GPU cost proportional to sequence length (should be near-flat: GDN decode is O(1) recurrent, paged attn reads scale with KV but should be ~ms at 20k). Suspects: the AITER GDN decode kernels (fused_rearrange_sigmoid_gated_delta_rule / causal_conv1d_update) or AITER unified attention decode path looping over ctx. ALSO FLAGGED: 53,008 cumulative uncorrectable XGMI WAFL fabric errors on all 4 GPUs since Aug 28 (accumulating ~300-450/day/GPU continuously, incl. today) — hardware-level, predates all speed measurements, needs user awareness", "verdict": "NEXT: name the O(ctx) kernel — options: fix rocprofv3 jobs config (needs kernel-trace key), or A/B the GDN decode kernel via VLLM_GDN_DECODE_KERNEL=triton vs aiter path, or time aiter kernels standalone in a bench script outside the engine (no graph capture issues)"}
{"exp": "G2_STEPTRACE_EXEC_DOMINANT", "surface": "VLLM_STEPTRACE phase timing (new env-gated hook in EngineCore.step/step_with_batch_queue; batch-queue path is the active loop) during 20k-ctx spec-on decode: sched 0.29ms | exec 757.09ms | out 0.07ms per step (n=50); TPOT 48.1ms => acceptance ~15.7 tok/step (healthy!); standalone aiter GDN decode kernel bench (scripts/bench_gdn_decode.py): fused_rearrange_sigmoid_gdr = 0.096ms flat in T, 48 layers = 4.6ms/step", "result": "The entire cost is the model-execution phase; scheduler/output CPU work negligible. Per target-only step (spec-off @20k): 228ms, of which GDN=4.6ms, paged attn ~2ms, GEMMs ~10-15ms => ~200ms UNATTRIBUTED inside the graph-captured target forward, scaling with ctx", "conclusion": "Acceptance is healthy (~15.7/step this leg) — the 'acceptance collapse' framing is wrong for THIS regime; the defect is a ~200ms+ O(ctx) term inside the captured forward (candidates: mamba align preprocess kernels, int8-block paged attention kernel path, or per-layer state machinery), NOT the GDN recurrent kernels", "verdict": "NEXT A/Bs: (1) VLLM_GDN_DECODE_KERNEL=triton vs aiter at 20k (in-engine GDN path); (2) attention backend alternative for full-attn layers if available on gfx908; (3) bisect by MNBT=512 (already built+ran: identical TPOT slope => not chunk-size related)"}
{"exp": "G2_EXEC_SPLIT_DRAFT_VS_TARGET", "surface": "VLLM_STEPTRACE spec-off 20k leg (target-only): exec = 223ms/step (sched 0.09ms, out 0.05ms); spec-on full step = 757-768ms; VLLM_GDN_DECODE_KERNEL=triton A/B identical to default (47.3 vs 48.1ms TPOT; exec 768 vs 757ms) — GDN decode kernel path fully exonerated in-engine (matches standalone bench: 0.096ms flat, 4.6ms/48 layers)", "result": "Split: target 1-token step at 20k = 223ms (kernels sum ~20ms => ~200ms unattributed O(ctx)); draft 13-token forward = 760-223-verify_extra ~ 530ms (~40ms per draft token at 20k) — BOTH models pay a per-token O(ctx) cost at decode despite GDN recurrent kernels being O(1)", "conclusion": "Unifying root-cause candidate: a per-token cost proportional to sequence length inside the GDN-hybrid decode forward, outside the measured recurrent kernel — remaining suspects: causal_conv1d_update (max_query_len = block-table width semantics), the mamba align/precopy per-step kernels, paged-attn block gather, or graph-replay interaction. Also: VLLM_GDN_DECODE_KERNEL passthrough now defaults 'triton' (explicit 'cuda' aborts boot: torch.ops._C.fused_gdn_decode_post_conv_mtp not built)", "verdict": "NEXT: (1) NS=2 A/B at 20k — exec should drop ~proportionally if draft per-token dominates; (2) standalone timing of causal_conv1d_update + paged-attn decode at synthetic block widths; (3) read mamba_hybrid preprocess kernels for per-step full-sequence scans; XGMI fabric errors still flagged (may inflate all cross-rank ops)"}
{"exp": "G2_NS2_AB_DRAFT_SCALING", "surface": "NS=2 A/B at 20k ctx (spec-on, steptrace): exec 275ms/step vs 760ms at NS=13; TPOT 97.7ms (fewer tokens/step) — draft cost scales ~25-40ms PER DRAFT TOKEN at 20k", "result": "Draft per-token cost confirmed dominant; arithmetic: target 1-tok step 223ms / 64 layers = 3.5ms/layer; GDN kernel 0.1ms; per-layer gap concentrated in the 16 full-attn layers => ~14ms/attn-layer/token at 20k (~0.6ms per 1k-KV-tokens) = memory-inefficient decode attention path, matching fp16-KV==int8-KV (dtype-independent kernel inefficiency)", "conclusion": "PRIME SUSPECT FINALIZED: the full-attention DECODE kernel for the 16 attn layers (and same path in draft) scans KV at ~600GB/s effective — likely a Triton/non-paged fallback rather than AITER UA flash path on gfx908; next: identify which backend serves decode attn (VLLM_ROCM_USE_AITER_UNIFIED_ATTENTION=1 is set; find fallback condition) and time the candidate kernels standalone", "verdict": "Immediate mitigation available: NS=2 (exec 275 vs 760ms) — but acceptance halves throughput; the fix is the attention decode kernel path. After fix: NS=13 becomes cheap, target-only drops toward ~30-50ms/step"}
{"exp": "G2_ATTRIBUTION_PROGRESS", "surface": "Post-NS2 continuation: spec-via-decode A/B at 20k identical (exec 757ms, TPOT 47.6 — spec routing exonerated); standalone aiter unified_attention decode bench (scripts/bench_attn_decode.py; 6q/1kv/256d int8-g128, BLOCK=512): 0.164ms/layer-call FLAT from 2k to 60k ctx => 2.6ms/step for 16 attn layers; standalone bf16 GEMMs at M=1/14 (scripts/bench_gemm_decode.py): 0.80ms/layer x64 = 51ms/step flat; mamba align preprocess/precopy kernels audited O(1) (loops over conv_width only); cudagraph capture sizes reach 168 (batch-14 verify IS captured — eager-verify hypothesis dead); engine KV block size = 64 (bench used 512 — rerun at 64 pending)", "result": "Known-flat components now sum to ~58ms/step (GEMM 51 + GDN 4.6 + attn 2.6) — matches spec-off @2k (49ms) but leaves ~170ms/step UNATTRIBUTED at 20k, still scaling with ctx; the only untested ctx-coupled GPU work remaining: the attention kernel at the engine's real BLOCK=64 paged geometry (8x more blocks/scan), the in-engine W8A8 GEMM wrapper (aiter CK path with per-group scale epilogue at M=1), and custom-AR behavior on the error-degraded XGMI fabric", "verdict": "NEXT (precise): (1) rerun bench_attn_decode with BLOCK=64 + M=14 verify shape + ctx sweep 2k-60k; (2) time in-engine W8A8 GEMM at M=1/14 via aiter's ck wrapper; (3) if both flat, instrument worker execute_model split (model-graph replay vs state machinery) with CUDA events (no sync inside capture)"}
{"exp": "G2_LAYER_SPLIT_FOUND_IT", "surface": "VLLM_STEPBREAK (new models/step_break.py; event-pair per decoder layer, env-gated, capture-safe) in EAGER mode spec-off 20k: full_attention layers = 280ms/step (17.5ms/layer x16) vs linear_attention (GDN) 62ms/step — THE ctx-scaling term lives in the full-attention layer region; eager-vs-graphs A/B: 2k eager 119ms vs graph 47ms (graphs help), 20k eager 224ms == graph 222ms (no graph benefit at 20k — consistent with piecewise graphs excluding attention: the attention wrapper runs EAGER always per its NOTE(woosuk) comment)", "result": "Kernel exonerations completed: aiter unified_attention benched flat 0.17ms/layer at engine BLOCK=64 in ALL layouts (contiguous group-scale, per-token-head cache, and the ENGINE'S PACKED strided-scale layout replicated from _ensure_scale_caches); aiter CK W8A8 at M=1 = 42ms/64 layers flat; reshape_and_cache_g8 writer grid=(num_tokens, kv_heads) O(1)/token", "conclusion": "The 17.5ms/layer full-attention region cost is NOT the attention kernel, NOT the GEMMs, NOT the cache writer — remaining candidates inside the eager attention layer region: (a) custom all-reduce on the error-degraded XGMI fabric (53k+ uncorrectable since Aug 28; per-AR latency could be inflated), (b) CPU launch-gap time in the eager wrapper (events measure GPU idle during CPU-paced launches; NOTE(woosuk) warns of this) scaling with ctx via block-table python ops, (c) an unbenched component in the layer (rotary/output-scale path)", "verdict": "NEXT: (1) stepbreak at 2k eager for per-layer baseline (is full_attention also elevated at 2k?); (2) intra-layer CUDA-event split around JUST the unified_attention call inside the engine (kernel vs rest-of-layer); (3) time custom-AR standalone (vllm _custom_ar) at hidden 5120 across 4 GPUs on this fabric"}
{"exp": "G2_AR_EXONERATED", "surface": "Standalone custom-AR bench (scripts/bench_custom_ar.py, torchrun TP4, gloo cpu-group + CustomAllreduce as the engine wires it): n=5120 bf16 custom-AR 19.8us vs NCCL 55.0us; n=20480 22.1us; full 128x AR burst (entire decode step's worth) = 1.83ms total", "result": "Custom all-reduce is FAST and healthy on this fabric at decode shapes — the 53k+ XGMI WAFL errors are NOT inflating per-AR latency; AR contributes <2ms/step", "conclusion": "With AR exonerated, every GPU kernel and collective in the full-attention layer region is now individually fast+flat; the 17.5ms/layer eager measurement must be CPU-launch-gap time (events capture GPU idle while the eager wrapper's python code runs — the NOTE(woosuk) warning) or event-window misattribution of adjacent async work", "verdict": "DECISIVE NEXT: intra-layer CUDA-event split inside the engine around (a) the qkv/o GEMMs, (b) rotary+cache-write, (c) the unified_attention call itself, at 2k vs 20k — separates kernel time from CPU-gap time per sub-phase; if CPU gaps dominate, the fix is extending piecewise-graph capture over the attention wrapper (its eager exclusion is the structural cause: graphs==eager at 20k because the eager attention region IS the step)"}
{"exp": "G2_CPU_PATHOLOGY_ISOLATED", "surface": "VLLM_ATTNTRACE hook (sync after every unified_attention call) + VLLM_STEPBREAK (sync per layer group) in EAGER mode spec-off at 20k: TPOT collapses from 224ms (uninstrumented eager) to 57.3ms WITH per-layer syncs; 2k=41.4ms, 8k=33.8ms (sync-forced)", "result": "The ctx-scaling term is CPU-SIDE and vanishes when per-layer torch.cuda.synchronize() prevents the CPU from racing ahead of the GPU. GPU-only work at 20k is ~57ms/step (matches standalone kernel sums: GEMM 42 + GDN 4.6 + attn 2.6 + AR 1.8). Without syncs, the eager attention wrapper path (always eager due to opaque_attention_op=True on ROCm) exhibits a pathological CPU cost growing with queued work/ctx — 167ms/step at 20k", "conclusion": "Production ~5 tok/s is a pure CPU-side software bug in the eager attention wrapper dispatch (likely aiter Triton JIT dispatch / launch-path cost that scales with async queue depth or ctx), NOT GPU work. Potential: 20k-ctx single-stream spec-off ~57ms/step (~17 tok/s) and with spec 2-5x => 35-85 tok/s single-stream — the 300 tok/s C6 goal is plausible once fixed", "verdict": "NEXT: (1) isolate the CPU pathology — time the python wrapper code (perf_counter around the unified_attention python call excluding kernel) or profile with cProfile in the worker; suspects: aiter Triton JIT dispatch per launch, tensor view/slice ops per call, kernel config cache lookup; (2) candidate quick fix: batch/pin the wrapper, or capture attention into the piecewise graphs (make the op capture-safe)"}
{"exp": "G2_SYNC_MITIGATION_VALIDATED", "surface": "VLLM_ATTNTRACE (sync-after-every-attention-call) in PRODUCTION graph mode, spec-on, 20k ctx: TPOT 42.5ms = 23.5 tok/s single-stream (vs 20.7 tok/s uninstrumented baseline at same ctx — spec-on was partially masking the pathology); spec-off graph+sync 69.8ms vs 224ms uninstrumented (3.2x); eager+per-layer-sync 57ms", "result": "The sync-after-attention mitigation works in production mode: the CPU-race pathology is confirmed live and a per-attention-call synchronize removes it. GPU truth at 20k: ~57ms/step spec-off; spec-on amortizes to 42.5ms/token", "conclusion": "Fix path is clear and mechanical: the eager attention wrapper dispatch must not let the CPU race ahead — either (a) permanent lightweight sync/pacing at the attention boundary (the validated mitigation, costs some overlap), or (b) the real fix: capture the attention op into the piecewise graphs / batch its dispatch so no python-per-launch cost exists. Underlying CPU cost likely aiter Triton JIT dispatch or launch marshaling scaling with queue depth", "verdict": "Immediate action: ship (a) as VLLM_ATTN_PACE=1 default (measured 3x spec-off / 1.14x spec-on at 20k; benefit grows with ctx and concurrency), then investigate (b) for the full win; then GS32 requant per user directive"}
{"exp": "G2_PACE_KNOB_SPEC_INTERACTION", "surface": "VLLM_ATTN_PACE (per-attention-call sync) default-on full matrix in production spec-on mode: 2k=25.6ms (baseline 26.8), 8k=36.5ms (baseline 41.2, improved), but 20k/800tok = 104.6ms (baseline 48.3 — REGRESSION); the earlier 42.5ms spec-on success was a 400-token leg (pre-drift regime, high acceptance)", "result": "The sync pacing is NOT uniformly positive: it fixes spec-off (3.2x) but at 20k with 800 unseeded tokens it regresses spec-on — consistent with (a) breaking draft-verify async overlap AND/OR (b) confounding from drift-regime acceptance collapse late in the leg (fewer committed tokens/step => worse TPOT regardless of pacing)", "conclusion": "Knob shipped default-OFF (28c1208f59); the CPU-race root cause stands (spec-off proof is clean), but the mitigation must be applied where it does not break the spec pipeline — candidate: pace only the TARGET verify rounds (not draft), or fix the underlying dispatch cost; re-test protocol must use matched token counts and acceptance-aware TPOT (tokens/step), not raw TPOT, to deconfound drift", "verdict": "NEXT: (1) matched-length A/B (400 tok, pre-drift) pace-on vs pace-off at 2k/8k/20k spec-on + acceptance from committed/step; (2) if pace regresses even pre-drift, move sync to post-verify only; (3) root fix remains making attention dispatch captured/graphed instead of python-per-launch"}
{"exp": "G2_PACE_MATCHED_AB", "surface": "Matched-length pre-drift A/B (400 tok, unseeded, spec-on, production graphs): PACE-OFF 2k=20.7ms/48.4tok/s, 8k=37.5/26.7, 20k=44.0/22.7; PACE-ON 2k=13.1ms/76.2 (+58%), 8k=24.2/41.3 (+55%), 20k=46.8/21.4 (neutral)", "result": "Per-attention-call sync pacing delivers +55-58% at 2k/8k and is neutral at 20k pre-drift single-stream; the earlier 20k/800tok 'regression' (104.6ms) is drift-regime confound (acceptance collapse), not a pacing harm — but 20k single-stream gains require the drift problem solved or the spec-off-style fix", "conclusion": "VLLM_ATTN_PACE=1 is a real improvement for the 2k-8k regime and harmless at 20k pre-drift; left default-OFF pending one more long-leg stability check; the structural fix (graph-capture the attention dispatch, removing python-per-launch) remains the path to 20k gains and C6 300 tok/s", "verdict": "NEXT: (1) long-leg stability check of pace-on (4k tok x3 legs, watch for the 104ms pattern; if absent, default-on); (2) root fix: make the attention op graph-capturable (opaque_attention_op=True on ROCm forces the eager wrapper); (3) then GS32 requant per user directive"}
{"exp": "G2_ARCH_CORRECTION_DRAFT_ALREADY_GRAPHED", "surface": "Runtime trace (VLLM_ATTNTRACE, 2k+20k legs, steady decode): ZERO [attntrace] lines — no attention impl.forward python executes at steady decode. Boot logs show baseline already captures BOTH 'Capturing CUDA graphs (FULL)' (target) and 'Capturing dflash2 CUDA graphs (FULL)' (draft via worker/gpu/spec_decode/dflash speculator + DFlashCudaGraphManager)", "result": "Earlier root-cause theory ('drafter runs pure-eager python per step via v1 llm_base_proposer dispatch') was WRONG — that proposer path is not the production draft path on this stack. Draft AND target attention are graph-replayed. The PACE +55-58% result is from a regime where attention python did run; it does not reproduce the current bottleneck. Reverted approach."}
{"exp": "G2_V1_DRAFTER_PATCH_REGRESSION_REVERTED", "surface": "Patched v1 proposer (drafter FULL keys via CudagraphDispatcher + CUDAGraphWrapper on drafter.model + static cad buffers + real dummy_run metadata): boots clean, both target and dflash2 captures succeed, but 800-tok pre-drift legs: 2k=34.5tok/s (PACE-ON baseline 76.2), 8k=22.0 (41.3), 20k=21.5 (21.4, neutral)", "result": "REGRESSION at short ctx consistent with draft acceptance collapse (2k/8k high-acceptance regimes hurt; 20k already drift-collapsed so neutral). The patch re-routed the draft forward through a wrongly-captured layer while the production speculator path was already correct. ALL code reverted (git checkout of dflash.py/llm_base_proposer.py/gpu_model_runner.py); only the harmless VLLM_ATTN_PACE passthrough in serve script remains."}
{"exp": "G2_STEP_PHASE_SPLIT_2K", "surface": "VLLM_STEPPHASE CPU wall per-phase at C1 steady decode, 2k ctx, 200-step window (4 workers agree +/-2%): reqs 1.8ms | inputs 30.4 | fwd 56.7 | gap 0.1 | sample 33.9 | propose 2.9 | post 0.04 => ~125ms/step total vs ~15ms GPU truth", "result": "The step skeleton, not the drafter, dominates: input prep (~30ms), forward-dispatch wall (~57ms around a graph replay), sampler (~34ms). Draft propose is cheap (2.9ms) confirming draft graphs work. Sub-phase instrumentation (p_inputs/p_attn/p_state, f_prefw/f_replay, s_logits/s_reject) queued to localize the three hot phases."}
{"exp": "G2_MIXED_BATCH_EAGER_ROOT_CAUSE", "surface": "C6/20k concurrent legs reproduce user's prod number: aggregate 23.9 tok/s (~5 tok/s/stream). VLLM_STEPPHASE window during C6/20k: f_tail=117.8ms/step with all other phases <10ms — f_tail is the EAGER forward branch (probe marks f_prefw/f_replay only instrument the FULL branch). Mixed prefill+decode steps run fully eager because gfx908 bans PIECEWISE (rocm.py:1029, 'hangs at TP>1') and forces FULL_DECODE_ONLY", "result": "Root cause of concurrent-workload slowness: any workload with in-flight prefills mixes decode steps into eager batches (~118ms/step vs ~60ms graphed). Decode-only uniform steps replay FULL graphs and are fine (C1/2k steady: worker CPU ~6ms/step). Fix implemented: VLLM_SCHED_NO_MIX env-gated scheduler policy (steps are decode-only-uniform or prefill-only; admissions force prefill-only step after 2-step defer; DP-throttle livelock guarded). A/B blocked by hardware failure."}
{"exp": "HARDWARE_XGMI_FAILURE_20260911", "surface": "kern.log: 2026-09-10T23:48:43 XGMI PCS FCInitTimeoutErr on 0000:03:00.0, 23:48:59 ElasticFifoOverflowErr+RecoveryTimeoutErr+RecoveryRelockAttemptErr on 0000:c3:00.0; 23:53:49 onward continuous 'trn=2 ACK should not assert' storm on 03:00.0 (5087+ events by 03:25). /dev/dri lost render nodes; rocm-smi 'No AMD GPUs specified'; vllm boot fails 'Failed to infer device type'", "result": "TWO GPUs degraded in the XGMI mesh, at least one stuck in link-training error loop for 3.5h+. All GPU work blocked. Likely also the cause of yesterday evening's intermittent silent worker deaths during boot (KV-spec phase). Recovery requires host reboot or amdgpu driver reload — needs root (user action)."}
{"exp": "G2_NO_MIX_AB_REGRESSION", "surface": "VLLM_SCHED_NO_MIX=1 vs baseline (healthy GPUs post power-cycle, C6 concurrent 800-tok streams): 2k=93.47 vs 94.75 tok/s (neutral); 20k=18.92 vs 24.53 tok/s (-23%)", "result": "No-mix scheduling does NOT pay: serializing prefill chunks into decode-free steps costs more wall than letting decodes ride mixed-eager steps (prefill dominates mixed step GPU time; decodes are nearly free riders). Policy stays default-OFF. The path to fast mixed steps is making them graphable (piecewise root-cause) or cutting the eager penalty; steady-state step rate and acceptance are the other half of the C6/20k gap."}
{"exp": "G2_STEADY_STATE_SPLIT_2K_VS_20K", "surface": "c6_steady.py (shared-prefix warm, pure concurrent decode, no ramp): C6/2k=125.98 tok/s aggregate (21-23/stream); C6/20k=23.8-31.3 tok/s (4-6/stream = the user's prod number). cgdispatch trace: steady steps 100% FULL-graph dispatch (6,84,14,14). Per-step [ftail] rows: FULL steps cost 0.0ms post-replay — earlier 86ms 'f_tail' windows were eager ramp/mixed steps", "result": "Steady decode dispatches graphs correctly and worker CPU is ~6-8ms/step. The wall is GPU-side: engine exec=150ms/step at 2k, 340-400ms at 20k (sched/out <1ms). GPU shows 100% busy; rocm-smi memory-activity metric is N/A (unsupported) so spin-vs-real-work unresolved — STEPBREAK composition diagnostic queued. Context length scales step time ~2.4x from 2k to 20k."}
{"exp": "G2_GEMM_TUNE_NEGATIVE_RESULT", "surface": "W8A8 GEMM tuning investigation at M=84 (C6 verify): (1) tuner infra broken (shared-module build race: 824 FileNotFoundError mid-run; first full run lost to 4h timeout; errRatio=1.0 all-reject on fp32 scales unexplained); (2) surgical clone test: nearest-M tuned rows (M=80/16, incl cross-N for the row-less qkvz/o_proj shapes) appended for all 12 decode shapes -> microbench 1.093ms/layer vs 1.074 baseline = NEUTRAL; (3) table audit: (12032,5120) and (5120,5120) have zero rows at ANY M; (8704,5120)/(5120,4352) fully covered; bpreshuffle family has no gfx908 rows", "result": "The ~15% HBM BW (~180-300GB/s) at skinny M is a CAPABILITY LIMIT of the a8w8_rowwise CK kernel family on gfx908, not a tuning gap — pre-existing tuned rows are the same class as the default config. GEMM floor stays ~39ms/step at M=84. Tuning route closed (CSV restored to pristine 1051 rows). Top remaining lever: the ~60-68ms/step unexplained GPU idle/spin between kernels (AR desync suspect) — next: GPU-event busy-time measurement via StepTimingCollector at runtime."}
{"exp": "G2_TP2_VS_TP4_AND_POWER_CONTEXT", "surface": "TP2 A/B (same recipe, KV 10GB, port 8021): steady C6/2k = 80.6 tok/s (exec 181ms/step) vs TP4 126 (148-150ms). Composition model: TP2 = GEMM~78 + attn~26 + draft/sampler~40 + residual~37; TP4 = 39 + 13 + ~30 + residual~66. SYSFS: power1_cap=105W vs 290W default on ALL GPUs (user-set); idle sclk 300MHz, mclk 1200MHz", "result": "Residual overhead shrinks with rank count (66@TP4 → 37@TP2) — consistent with AR-mesh cost partially, not fully. Power cap 105W (36% of default) bounds all kernel times incl. DVFS ramp between step phases; the 8/27 350@C8 reference was achieved under this same cap. Remaining levers ranked: (1) residual 60ms@TP4 — needs launch-mode kernel timing (attach blocked by ptrace); (2) GEMM 39ms kernel-family limit under cap; (3) attention@20k 106ms — split-config tuning candidate."}
{"exp": "G2_KERNEL_TRACE_LAUNCH_BOUND", "surface": "rocprofv3 kernel trace of steady C6/2k (jobs:[{kernel_trace:true}] schema; -P 300:45:0 window; 2.67M events, 4 workers): REAL kernel time is tiny — CK GEMM avg 64us (microbench 178-345us was wrapper-inflated), total kernels ~5-8ms/step/GPU vs 148ms step wall => GPU ~95% idle. Burst analysis: no >5ms gaps inside steady decode — the idle is ~4300 kernels/step/GPU each separated by ~30-100us launch gaps; ~2600/step are tiny eager aten elementwise kernels (929k vectorized_elementwise + 650k manual_unroll across window) launched from python between/around the graph replays (GDN state ops, draft precompute, sampler, speculator prep)", "result": "The step is LAUNCH-BOUND on the eager skeleton, not kernel-bound: graphs cover only the target verify forward; the surrounding per-step python launches thousands of tiny kernels with python gaps. Fixes ranked: (1) extend graph capture over the skeleton (draft precompute + sampler + GDN post-ops) or fuse the elementwise flood; (2) launch batching. This also explains why PACE syncs 'helped' short-ctx and why TP2 overhead persisted. Prior GEMM-tuning and AR-desync theories are now closed — kernels and ARs are not the bottleneck."}
{"exp": "G2_STEP_COMPOSITION_FINAL", "surface": "Full per-name kernel histogram (rocprof trace, 668k events/worker, 154 steps): traced ms/step/GPU: CK GEMM 84.9 (264 calls, ~42ms real = matches isolated bench), unified_attention 75.3 (ONLY 19 calls @ ~2-4ms each = 10x the isolated 0.355ms bench @2k — max_seqlen_k-driven split over-provisioning suspect), cross_device_reduce_2stage 59.1 (120 calls @ ~250-500us vs isolated 19.8us — desync spin INSIDE AR kernel duration), elementwise 18.5 (2687 calls), GDN 5.2, misc ~10. Stream gaps ~0us — GPU 98.5% busy; step is kernel-time-bound", "result": "Two pathological items vs isolated benches (attention 10x, AR 12x) = ~55ms/step of the gap; root linkage: the 2687-launch python-elementwise flood desynchronizes ranks -> ARs spin; loose max_seqlen_k inflates attention splits. FIXES: (1) cap max_seqlen_k tightly in the attention metadata (expect attn 38->~5ms); (2) fuse/batch the elementwise flood (fewer launches AND tighter rank sync -> AR spin down). Prior 'launch-gap' framing corrected: no gaps — durations themselves are inflated."}
{"exp": "G2_TRACE_SCOPE_CORRECTION", "surface": "Duration-filter re-analysis: ALL traced attention time is PREFILL (2961 calls @ mean 3.9ms = the 20k shared-prefix warmup overlapping the window; ZERO decode-size attention calls traced); all traced GEMMs are prefill-class; decode GEMM/attention run INSIDE FULL graph replays which this rocprof config does not record per-kernel. Visible eager decode evidence: ARs 122/step (median 53us — gfx908 ejects custom-AR from graph capture per rocm.py comment) + 2687 elementwise/step", "result": "Corrected model: steady decode step = graph-internal kernels (GEMM+attn at isolated-bench speeds, ~55ms) + eager skeleton (elementwise ~15ms + eager ARs ~6-30ms) + graph-node/launch overhead of ~4300 total kernels/step (~4x a healthy spec-decode step). Actionable: reduce kernel count — fuse the elementwise flood and restructure eager AR segmentation. Attention max_seqlen_k hypothesis falsified by bench (0.325-0.363ms across 2k-262144)."}
{"exp": "G2_TRACE_SCOPE_CORRECTION", "surface": "Duration-filter re-analysis: ALL traced attention time is PREFILL (2961 calls @ mean 3.9ms = the 20k shared-prefix warmup overlapping the window; ZERO decode-size attention calls traced); all traced GEMMs are prefill-class; decode GEMM/attention run INSIDE FULL graph replays which this rocprof config does not record per-kernel. Visible eager decode evidence: ARs 122/step (median 53us — gfx908 ejects custom-AR from graph capture per rocm.py comment) + 2687 elementwise/step", "result": "Corrected model: steady decode step = graph-internal kernels (GEMM+attn at isolated-bench speeds, ~55ms) + eager skeleton (elementwise ~15ms + eager ARs ~6-30ms) + graph-node/launch overhead of ~4300 total kernels/step (~4x a healthy spec-decode step). Actionable: reduce kernel count — fuse the elementwise flood and restructure eager AR segmentation. Attention max_seqlen_k hypothesis falsified by bench (0.325-0.363ms across 2k-262144)."}
{"exp": "G2_STEP_SEQUENCE_ATTRIBUTION", "surface": "Ordered per-step kernel sequence (4741 kernels/step, delimited by _topk_topp): sampler/rejection/GDN-post block, draft prep, then 36x layer loop. Per layer: 1 AR + ck-GEMMs each wrapped in ~10-15 tiny aten kernels (fp16 casts, absmax reduce_kernel for quant scales, scale mul/div, residual adds) + _pertoken_quant_rn + rope + reshape_cache_g8 + unified_attention. The 2687 elementwise/step = the ACTIVATION-QUANT GLUE around 264 GEMMs, baked into the captured graphs (torch.compile fusion unavailable: banned on gfx908)", "result": "Fix target identified precisely: fuse the per-GEMM quant glue into one Triton kernel per GEMM (extend _pertoken_quant_rn to consume bf16 + emit scales, absorbing casts/absmax/scale-div), and fuse residual+cast chains. Expected ~2600->~400 kernels => -50 to -70ms/step => ~80-95ms steps => C6 steady ~200-230 tok/s; with acceptance recovery 300 becomes reachable. Quality-sensitive path: requires the unseeded full-read gate after implementation."}
{"exp": "G2_QUANT_CAST_REMOVAL", "surface": "Removed the lossy bf16->fp16 cast before pertoken_quant_rn (aiter_w8a16.py: round path feeds bf16 directly; trunc/aiter paths keep the cast). Microbench: bit-identical int8 outputs and scales (0 mismatches); 36.9 vs 53.5 us/call. A/B steady C6/2k: 121.66/120.56 vs 126 baseline (inside +-5% unseeded noise; expected ~1.3% step cut)", "result": "Kept: strictly-positive, numerics-identical, one fewer [M,K] kernel per fresh-quant GEMM (~130/step). The projected big win remains in the ~10-op glue chains (qk-norm/rotary/residual+cast) — the next fusion package."}
{"exp": "G2_FUSION_RETESTS_E3_QKROPE", "surface": "Two in-tree fusion seams retested under the launch-bound understanding: (1) VLLM_GFX908_EAGER_EPILOGUE=1 (E3 fused AR+residual+RMSNorm+int8): steady C6/2k = 127.67/126.39 vs 120-126 baseline = NEUTRAL (reproduces the old E3 verdict; trades in-graph kernels for one eager kernel, nets zero); (2) fused_qk_rmsnorm_rope_gate enabled on ROCm (1-line platform flip): FAILS Triton MLIR compilation on gfx908 (PassManager::run failed at fused_qk_norm_rope.py:17) — that is why it was CUDA-gated; reverted", "result": "No cheap fusion wins remain in-tree. The big kernel-count reduction requires NEW gfx908-compatible Triton kernels: (a) port/fix fused_qk_rmsnorm_rope_gate for gfx908 MLIR, (b) residual+cast chain fusion. Spec'd, quality-gated, multi-session kernel engineering. Server restored to clean default config."}
{"exp": "G2_QKROPE_GFX908_PORT", "surface": "Ported fused_qk_rmsnorm_rope_gate to gfx908 (split Q/K kernels removing the runtime pointer-base branch that broke TritonAMDGPUCanonicalizePointers; verified vs eager reference: q/k rel ~2e-3 = bf16-rounding, gate bit-exact; boots + serves). A/B steady: C6/2k = 114.42/116.54 (baseline 120-128) — neutral-to-slightly-negative; C6/20k = 26.10 (baseline band)", "result": "Port is correct and dormant (dispatch kept behind the CUDA-only model gate, which stays reverted). The qk chain was only ~5 kernels/layer — replacing it nets zero; the launch-bound excess lives in the GEMM-surrounding quant/residual glue, not the qk path. Kernel-count reduction must target the per-GEMM glue chains."}
{"exp": "G2_FINAL_MODEL_AND_LEVER_MAP", "surface": "Reconciliation of all probes: the stream is saturated (98.5% busy, zero gaps), so eager-launch/kernel-count reductions do NOT remove wall time unless they remove KERNEL time. The 2687 elementwise flood sums to only ~9ms real — hence E3, qk-rope port, cast-removal all measured neutral-to-noise. The 148ms step is ~139ms of real kernel execution at gfx908@105W speeds: GEMM ~42ms (a8w8_rowwise kernel limit under the power cap), attention 13-38ms (ctx-scaled), draft precompute+forward+spec machinery+sampler+GDN+AR the rest", "result": "The 300 tok/s@C6 target is not reachable by glue fusion. Remaining levers, by magnitude: (1) POWER: caps are 105W/290W user-set — raising them directly scales the memory/compute-bound kernel times; (2) ARCHITECTURE: spec depth NS=13 pays M=84 verify GEMM+attention for ~3.1 accepted tokens (recipe-pinned — user decision); (3) ACCEPTANCE recovery (the multiplier). Kernel-count reduction and per-GEMM glue fusion: CLOSED as primary levers. All A/Bs, instrumentation, and the dormant verified qk-rope port are committed."}
{"exp": "G2_ACCEPTANCE_CONTEXT_CURVE", "surface": "Steady C6 acceptance vs input context (tokens/step/stream = aggregate x step_sec / 6): 2k=3.1 (126 tok/s, 148ms), 4k~2.9 (98.4), 8k~2.5 (57.2, 260ms), 12k~2.2 (36.8), 20k~1.6 (26, 370ms). Both acceptance (-2x) and step time (+2.5x) degrade smoothly with context — the 20k throughput cliff (~5 tok/s/stream) is their product", "result": "The user's 'acceptance basically non-existent' is quantified: smooth KV-accumulation-linked decay, not a cliff/drift artifact. Recovery paths: (a) GS32 requant with longer-context calibration (queued, user-approved, targets BOTH quality and acceptance; requires stopping prod ~2h for the GPU job), (b) draft-KV precision experiments are BLOCKED by the user-pinned g128-KV-everywhere contract. Requant is ready to execute on request."}
{"exp": "Q_GS32_REQUANT_READGATE_FAIL", "surface": "GS32 requant of PTQR-R10S60 master (2.5h GPTQ calib; served after fixing the CK dispatch bug where GS32 fell into the freed-blockscale path); speed in banked band (steady 112.9@2k / 24.7@20k; ramp 100.2/18.7); UNSEEDED 40k-in/4k-out full-read gate: coherent ~800 output tokens then mid-sentence break at 'geographical feature and a' followed by total collapse into a pure 'of'-repetition loop for ~165 lines until EOS@2595", "result": "READ-GATE FAIL — GS32 collapses EARLIER than the GS128 baseline (clean 2.2-3.7k). The garble is NOT group-scale-granularity noise; GS32 does not improve quality and is rejected for production. Prod reverted to GS128 R10S60. The CK dispatch fix (presence-based, group-agnostic) is kept as a genuine bugfix with GS128 behavior preserved. Garble root cause remains the model-side drift class (banked: sampler fix live, residual meta-flip = grid noise)."}
{"exp": "Q_CURRENT_BASELINE_READGATE", "surface": "Unseeded 40k-in/4k-out full-read gate on TODAY'S prod (GS128 R10S60 + sampler fix + cast-removal, post-GS32-revert), read manually line-by-line: coherent high-quality essay through ~2450 output tokens (Sections I-III fully clean), then word-salad onset (~2 lines) and total collapse into fragment repetition at ~2500, EOS@3196", "result": "DEFINITIVE current quality baseline: clean ~2.2-2.5k output tokens at 40k context, then drift collapse — consistent with the banked drift class (meta-flip/grid noise, model-side). GS32 was worse (collapse @800). Every serving-side lever is now closed; the garble requires the model-side fix: PTQR r11 with long-context calibration (user decision) or acceptance of the ~2.4k clean horizon. Combined goal state: quality = clean-2.4k-horizon + retraining decision pending; speed = 120-128@C6/2k steady with all in-mandate levers closed, power-cap/NS decisions pending."}
{"exp": "Q_R11_SCOPING_ANALYSIS", "surface": "Scoped the r11 retrain option against the garble signature: training dataloader is flat-corpus --seq_len windows (default 4096); the _RolloutDS note records the prior Path B finding that per-state int8 noise is CONTEXT-LENGTH-INDEPENDENT, and the drift onset (~2.2-3.7k output tokens, similar across 20k-40k inputs) is OUTPUT-length-driven — autoregressive error compounding, not per-state input error growth", "result": "Decision-informing: (a) long-context CALIBRATION data alone is likely misaimed at the drift (per-state noise is ctx-independent by prior evidence); (b) the mechanism that actually targets output drift is rollout/committed-region training (_RolloutDS exists) — but that is the DAgger objective with a prior conviction ('dead — never revisit') from earlier program phases; (c) therefore r11-as-long-context-calib is LOW-confidence for garble fix; r11-as-rollout-retrain requires overturning a prior verdict. User decision required with these priors on the table."}
{"exp": "G1B_TSHAPE_V2_BIT_IDENTICAL_CLOSED", "surface": "T-shape verify-read test v2 (rebuilt harness; correct g128 packed fp16 scale layout; peaked-attention synthetic cache; exact fp32 dequant reference; MATCHED causal geometry with pre-written batch rows): with cu_seqlens=[0,T] (ONE request — the v1 harness passed [0,1,2..T] = T tiny requests, INVALIDATING the v1 'inconclusive' row), T=14 and T=1 unified-attention reads over the same int8-g128 cache with identical queries are BIT-IDENTICAL (cross-diff 0.000000 across 4 trials, error-vs-exact ratio 1.000)", "result": "LEAD CLOSED: the verify-shape KV read is shape-invariant and numerically clean — no T=14-specific read error exists in the aiter unified attention. With this, every verify-unique compute surface is now exonerated with a correct harness (GEMM M-shapes bit-identical, GDN routing null, handoff/crossing fixed, read path bit-identical). The in-vitro compute surface is fully clear; the deep drift component does not live in any measurable kernel-shape difference. Next lead: the offloading-connector CPU tier (the only state-touching subsystem never read-gated at temp-1 long output)."}
{"exp": "G1B_OFFLOAD_TIER_READGATE_GARBLES", "surface": "Offload-tier read gate (the never-tested temp-1 long-output cell): OFFLOAD=0 boot verified (0 kv_connector refs), unseeded 20k-in/4k-out temp-1 prod-params leg, full manual line-by-line read", "result": "GARBLES and EARLIER than the with-tier baseline: word-fusions from paragraph 1 (contrastsextreme, valleysvalley), repetition creep ~1.5k, the-wall collapse ~2k, EOS@2061. The CPU KV tier is NOT the corruptor — removing it changes timing/interleaving (consistent with the timing-perturbation sensitivity banked across the program) but the drift persists. LEAD 2 CLOSED. Combined with Lead 1 (T-shape bit-identical): every runner subsystem is now either fixed (AR race, GDN handoff/crossing seeding, sampler semantics, offload reconciliation) or exonerated with a correct harness (all kernels, rejection sampler, offload tier, spec controls). The residual meta-flip survives all isolation tests — consistent only with a composite/timing-dependent fault or the never-completed stale-trim propagation fix (row 85-87: attempt 1 could not fire by construction; kernel-guard attempt made legs worse and was reverted)."}
{"exp": "G2_NS6_SPEED_AND_READGATE", "surface": "NS=6 (user-approved) full A/B on healthy GPUs, offload tier ON, vs NS=13 banked bands: steady C6/2k 139.02 vs 120-128 (+9-16%); steady C6/20k 38.12 vs 24-31 (+23-59%); ramp 2k 136.50 vs 94.8 (+44%); ramp 20k 19.56 vs 24.5 (-20%, prefill-dominated); single-stream 41.4 tok/s @2k (TPOT 24.2ms) / 18.2 @20k (55.0ms) — both in band. Quality: unseeded 20k/4k read gate (full manual read of 957 lines): coherent ~1.2-1.5k, fusion at ~line 17, then the SAME ladder as NS=13 (word-per-line -> stutter wall -> meta-flip at 273 -> 'Let me' wall to max_tokens)", "result": "NS=6 ADOPTED AS PRODUCTION DEFAULT (serve script NS:-6): clear concurrent speed win at every steady cell, single-stream in-band, quality class unchanged (same failure ladder, clean phase within distribution). Prod server left running NS=6 + offload ON. Remaining goal gap: steady 139@2k and 38@20k vs 300 target — dominated by kernel-bound step time at the 105W caps; remaining runner-side garble surface: the stale-trim scheduler->worker propagation (row 85-87, never completed)."}
{"exp": "G1B_STALE_TRIM_PROPAGATION_FIX_LANDED", "surface": "Implemented the audit's preferred fix (row 85 hazard, previously unfixable): scheduler ships per-request per-group nulled-prefix counts (leading null_block slots from SW eviction) in CachedRequestData.nulled_prefix_blocks; worker zeroes those block-table row slots (block_table.zero_prefix_blocks) so stale ids never route KV traffic into freed-reallocated pages. Booted clean; unseeded 20k/4k temp-1 read gate (full manual read): coherent ~1.2-1.5k, fusion onset ~line 15, the-wall to EOS@2134 — SAME class and onset as baseline", "result": "Fix LANDED and kept (mechanistically correct; completes the last convicted-hazard audit item; row-85 hazard now closed at the protocol level). NOT the residual garble mechanism — drift persists identically. With this, every identified hazard in the entire program record is now either fixed (6 fixes total: AR race, offload reconciliation, GDN handoff seeding, crossing seeding, sampler semantics, stale-trim propagation) or exonerated with a correct harness. The residual drift survives all of them — consistent with the diffuse per-layer int8 noise + sampled-trajectory compounding (Path B: diffuse, no concentration) under the user's standing bf16-also-broken frame."}
{"exp": "G1B_NOTHINK_READGATE", "surface": "Read gate with the thinking scaffold disabled (per-request chat_template_kwargs enable_thinking=false — the one serve-level surface never drift-tested; terminal failure mode is reasoning-mode intrusion): unseeded 20k-in temp-1, full manual read of all 24 lines", "result": "SAME DRIFT CLASS: coherent ~1.1k tokens, mid-sentence break (line 21), mid-word truncation (line 23), quiet EOS@1174 (no wall — no reasoning channel to trap in). The thinking scaffold is exonerated as the drift cause; it only shapes the post-collapse attractor. Closes the last untested serve-level surface."}
{"exp": "G1B_MAMBA_BIGPAGE_READGATE_3LEGS", "surface": "MAMBA_BLOCK_SIZE=262144 boot (flag verified in cmdline; mamba align pages now one giant block per request — ZERO boundary crossings during a 20k+4k leg; KV pool 1.7M tokens; prefix-cache-off path structurally impossible on this hybrid: tokens_per_hash assert), three unseeded 20k-in/4k-out temp-1 prod-params legs, all fully read line-by-line", "legs": {"leg1": "CLEAN through all 4096 tokens (first fully-clean 4k leg on the int8 stack in the program record); minor items only: duplicate section number V, two dropped conjunctions", "leg2": "essentially clean through 4k: coherent all sections; two minor late stutters ('was the foundational was the central', orphan 'The basin.' fragment) — no fusion ladder, no wall, no meta-collapse", "leg3": "COLLAPSE ~2.3k: stutter onset, 'Let me restart this section' meta-flip, then MULTILINGUAL TOKEN SOUP (CJK/Arabic/Korean/.TypeOf mix) — the worst collapse flavor observed"}, "result": "USER HYPOTHESIS STRONGLY SUPPORTED AS MAJOR CONTRIBUTOR: mamba paged-state machinery (align pages + boundary migrations) convicted as a dominant drift trigger — clean rate 2/3 at 4k vs baseline 0/6 (and vs NS=6 baseline 0/1, collapse 1.2-1.5k). BUT a residual failure path remains (leg3): the multilingual-soup flavor suggests a second fault — possibly the torn-read/AR family or a non-paging state fault — that bigpage does not fix. Attribution caveat: boot also changed KV pool size/allocation patterns (1.7M tokens). NEXT: (a) 3 more bigpage legs for statistics; (b) investigate the leg-3 multilingual-soup class (its flavor matches the original pre-AR-fix NaN/torn-read deaths); (c) EXP2/EXP3 value-level probes at both geometries."}
{"exp": "G1B_MAMBA_BIGPAGE_STATISTICS_CORRECTED", "surface": "Bigpage n=6 final: legs 1-2 CLEAN-full (first fully-clean 4k legs on record); legs 3-6 COLLAPSE at the usual 2.3-3k onset (multilingual soup / stutter-wall / reasoning-entrapped anchor-wall / early-thin-stop)", "result": "VERDICT REVISED: bigpage (zero mamba boundary crossings) improves but does NOT eliminate the drift — 2/6 fully clean vs 0/6 baseline is a modest shift, and the collapse onset when it fails is UNCHANGED (~2.3-3k). The earlier n=3 '2/3 clean' was small-sample noise. Paged-GDN machinery is A contributor (first-ever clean legs appeared here) but a crossing-independent fault remains the dominant mechanism. HYPOTHESIS UPDATE: the residual operates without boundary crossings — candidates: (a) the in-block conv rolling-buffer semantics themselves (kernel reads offset num_accepted-1, self-shifts +1 per step — active EVERY step, not just crossings; a per-step off-by-one would accumulate smoothly matching the onset curve), (b) the temporal-state column math bt[src_col + bias] (active at crossings AND handoff), (c) non-GDN fault. NEXT: value-level per-step probe of the conv rolling buffer at steady decode (no crossing): after each verify round, compare engine conv window against exact reference computed from the committed tokens — a per-step one-token drift in the window would convict (a) directly and explain the onset."}
{"exp": "G1_CONV_SPEC_PROTOCOL_EXONERATED", "surface": "Spec-decode causal_conv1d rolling buffer: kernel read offset num_accepted-1, write source a+j, migrations bias a-1/reset 1 (candidate (a) from G1B_MAMBA_BIGPAGE_STATISTICS_CORRECTED)", "result": "EXONERATED at three levels; the prior conviction (test_conv_spec_equiv a>=2 divergence, pre-geometry-audit) is RETRACTED as probe error. (1) GEOMETRY: engine allocates L = conv_kernel-1 + num_spec with num_spec = num_speculative_tokens = K (shape funcs, qwen3_5/qwen3_next pass K), draft batch N = 1+K rows -> the kernel write splits the buffer at VAL = L-N = KW-2: overlap head cols [0,KW-2) + x_prev=[anchor,d_1..d_K] at [KW-2,L). The earlier probe used STATE_LEN=24 and a fresh-head M-wide reference: both wrong for this layout. (2) PROTOCOL ALGEBRA: scripts/test_conv_protocol_sim.py — engine rules (read [a-1..a+KW-3], write head old[a+j], mig bias a-1, reset 1) are exactly self-consistent vs ground truth for 500 rounds, with and without boundary crossings; window end col KW-2+(a-1) = x_prev[a-1] = d_{a-1} = last committed token before the next anchor = ground truth. An attempted 'fix' (offset a, 0-neutral) was derived under a wrong head=M assumption, implemented, then REVERTED after the allocation audit — offset a is one-FUTURE under the real geometry. (3) KERNEL vs EXACT FIR: test_conv_spec_equiv.py rewritten (true L=9 steady-layout seed, line-1 non-null, acceptance-dependent committed stream) -> 0.000000 max|d| for ALL rows and ALL a in 1..7; brute-force window identification independently shows row-0 window at col a-1 (a=1->0, 4->3, 7->6). CONSEQUENCE: the last per-step conv candidate is closed; code identical to upstream vLLM main here (the -1 offset is upstream), consistent with stock-vLLM-bf16 garbles having a DIFFERENT shared mechanism. NEXT: (i) GDN core-attn verify chunk rows 1..6 vs exact sequential reference (row 0 audited clean, rows>=1 never directly compared), (ii) probabilistic rejection-sampler bias at temp>0 (spec-only, greedy-robust — fits the fault profile)."}
{"exp": "G1_GDN_CHUNK_RECURRENCE_EXONERATED", "surface": "Spec verify GDN in-chunk recurrence: fused_sigmoid_gating_delta_rule_update processing the 7-row [anchor,d_1..d_6] chunk from checkpoint slot si[num_accepted-1] (rows 1..6 never previously compared to an exact reference; row 0 was audited via VLLM_GDN_ROWAUDIT)", "result": "EXONERATED: scripts/test_gdn_chunk_reference.py — H=8,K=V=128,T=7, bf16 q/k/v/a/b, fp32 state, l2norm on, prod sigmoid-gating math; kernel outputs for ALL rows (incl. 1..6) and ALL per-row stored states match an exact fp32 torch recurrence from the same checkpoint for every acceptance a in 1..7 (out max|d| ~1e-4 = bf16 output rounding; states 0.000000 exact). Together with G1_CONV_SPEC_PROTOCOL_EXONERATED this closes the last in-vitro spec compute surfaces for this fork. Fault profile remaining: in-vivo only — either cross-round engine state evolution (not kernel math) or token selection/acceptance feedback. NEXT: logits-level in-vivo A/B (logprob_drift_ab.py): spec-live chosen-token logprobs vs prefill-echo ground truth over the identical token stream — divergence growth before garble onset convicts state drift; no divergence convicts selection."}
{"exp": "G1_LOGIT_AB_CLEAN_REREAD", "surface": "Clean re-measurement of the live-spec-vs-prefill-echo logprob divergence on the current stack (PTQR R10S60, NS=6, int8_g128 KV, offload on), with truncation confound removed (drift_leg --notrunc: temp1.0 AND temp0 legs, top_p=1/top_k=-1), id-keyed echo parser, and echo determinism validated (bit-identical across runs and across 512-token junction shifts; prompt reconstruction proven exact by floor-clean mid windows)", "legs": {"temp1_1280tok": "windows: 0-99 mean|d| 0.69 (max 9.8, 31% >0.3 nat), 100-799 FLOOR 0.001-0.025, 800+ escalating 0.16->1.23 (max 21.6, up to 85% >0.3)", "temp0_1280tok": "same shape: 0-99 0.72 (max 10.0), 100-799 floor 0.0002-0.015, 800+ 0.07-0.14 (classic greedy plateau)", "artifact_kills": "(1) truncation-renorm confound (chat logprobs post-top_k20 renorm vs raw prompt_logprobs) killed the d11-era v1 comparison (+26nat outliers were exactly top-20 inflation); (2) logprob fill-zero/one-hot-draft-prob theory killed (dirty positions carry REAL live probs, e.g. p=0.9867 where clean says 0.0064); (3) alignment/off-by-one killed (shift-0 optimal); (4) echo chunk-junction sensitivity killed (bit-stable, junction-invariant); (5) template-mismatch killed (clean mid windows prove exact prompt)"}, "result": "REVISED PROFILE: the verify-path distribution error is EPISODIC, NOT per-step — mid-window (output 100-799) is floor-clean on both temp legs, contradicting the prior 'error exists from token 0' framing (that was the early-window cluster + truncation confound). Two dirty regimes: (A) EARLY: first ~15-25 spec rounds after prefill (0-99/0-256), temp-independent, draft-value-independent (garbage-draft row), spec-amplified (spec-off junction floor 0.032 vs spec 0.4-0.7), real-probability disagreements up to 10 nat; (B) ONSET: 800+ episodic escalating dirt. Spec-off decode==echo at floor elsewhere. All in-vitro kernels exact => the fault is episodic per-ROUND state/input corruption in the live verify path, not kernel math. NEXT: per-round correlation — instrument a notrunc leg with per-round acceptance (PRING) + echo comparison, align dirty positions to rounds, extract the trigger condition (round index, acceptance count, block geometry, KV page state).", "instruments": "drift_leg.py --notrunc; logprob_echo_leg.py (id-keyed parser); logprob_drift_ab.py; logprob_ab_controls.py"}
{"exp": "G1_VERIFY_DIST_WRONG_THREEWAY", "surface": "PRING-instrumented eager leg (temp1.0 notrunc, 1280 tok, drift reproduced: early window 0.65 + onset 800+ 0.45-0.79) with clean round-chain (445 rounds, 1280/1280 tokens; PRING dumps land in CWD-relative /tmp/logs/...; note API token_ids include one template header token 760 before the first verify commit)", "legs": {"correlation": "dirt by role: corrections 0.60 (45% >0.3) vs accepted 0.14 (7%); by round n: monotone n=1:0.84/61% -> n=7:0.07/3%; by round idx: rounds 0-24 dirty 0.72, 25-124 FLOOR 0.003, 125+ dirty 0.4-0.9; TWO REGIMES: early = corrections-dirty-accepted-clean WITHIN the same rounds; late(800+) = everything dirty (persistent state)", "three_way": "at dirty positions the DECODE path (prefix+1-token, logprobs) disagrees structurally with the verify: different argmax, disjoint top-5s (e.g. verify p=0.92 on 3165 vs decode argmax 1353 not even in verify top-5; verify p=1.0 on 83545 vs decode argmax 11); at clean positions decode==verify==echo exactly (6/6 controls)", "row_health": "no inf/nan in the clean chain (absmax 18-35 everywhere; the earlier 'row_absmax=inf' was contaminated-data artifact) — distributions wrong but finite", "ctx_substitution": "at 5 dirty corrections: echo with the REJECTED DRAFT spliced into the context matches eng twice (off-by-one-row shape), matches neither three times (eng far from both clean and draft-conditioned) => NOT a simple wrong-row selection; the verify forward's state/inputs at these rounds are foreign to any token-stream context"}, "result": "CONVICTION (distribution level, three independent paths): the live verify forward computes structurally wrong distributions at rejection-heavy rounds — exactly the rounds that commit corrections — while full-acceptance streaks are exact. Early window = per-position (last/resample row wrong, earlier rows right within the same forward); late onset = whole-round state corruption (persistent). Fits every behavioral fact (temp-1 drift, greedy robustness, spec-off clean, heal-on-re-prefill, walls as full-acceptance absorbing states). The fault is in what the verify forward READS at those rounds (state/KV/metadata), not kernel math (all exonerated) and not row selection (substitution test). NEXT: capture the verify forward's actual INPUTS at a convicted round — GDN checkpoint state + KV rows + batch metadata for round r vs the decode-computed reference at the same position (STATEDIGEST/KVLINE-style but targeted at dirty rounds from the PRING chain), then bisect which input diverges.", "instruments": "pring_correlate.py; PRING flag-file boots need EAGERALL (hook is capture-gated) and dumps land CWD-relative"}
{"exp": "G1_FIRST_ROUND_STALE_STATE_SEEDING", "surface": "mode-none first-spec-round state seeding: gdn_attn spec_state_indices = block_table[:, :num_spec+1] = the sequence HEAD blocks (positions 0..7*bs); each verify round rewrites si[i] per row (self-maintaining from round 2), but the FIRST spec round resumes the GDN recurrence from si[num_accepted-1]'s PRE-EXISTING content = a ~20k-stale (or foreign/previous-request) state; the align-mode seeding fix (run_seed_spec_window, mamba_utils:1101) is gated inside preprocess_state's `if not self._align_mode: return` and NEVER RUNS in prod (mamba_cache_mode=none)", "evidence": "(1) SPECSTAT ground truth (VLLM_GDN_SPECSTAT layer dump, ss_leg1): si=[80,95,110,122,123,5,127] fixed lines all rounds; conv line si[0] rolling healthy from round ~2 (self-written); na per round natural [1,7,3,5,2..]; (2) GDNSTAT: conv buffers at slots >=2 NaN = never written (benign, only si[0]'s conv is read); SSM slot norms 202-218 all 'healthy-looking' — STALENESS IS INVISIBLE TO NORM CHECKS (this is why every norm-based instrument passed while logits were wrong); (3) fits regime A exactly: early-window dirt (first ~25 rounds) decaying to the mid-window floor as the GDN decay washes the stale state; (4) raw-vs-processed split (pring_raw_split.py, pring_leg3): 76/282 corrections forward-wrong, 0 processing-only — the sampler is clean", "result": "REGIME A (early window) ROOT-CAUSED: first-spec-round stale GDN state under mode none. FIX: seed the spec slot lines from the running state on the first spec round in mode none (the align fix exists but is unreachable; mode none needs its own path — no _mamba_ctx exists there today). Regime B (late whole-round persistent corruption at 800+) remains open — re-measure AFTER the regime-A fix (A's corruption may cascade or mask B). NEXT: implement mode-none seeding; validate with logprob_drift_ab early-window vanishing; then regime B."}
{"exp": "G1_GARBAGE_KNIFE_RERUN_AND_SLOTSEED_NULL", "surface": "two knives on the current clean methodology: (1) VLLM_GDN_SLOTSEED (reseed si[1..6] SSM+conv from si[0] before every spec forward) — NULL RESULT: dirt unchanged (early window 0.61; per-row slot divergence exonerated; caveat: si[0] itself never validated); (2) VLLM_DRAFT_GARBAGE rerun at temp1.0/notrunc with the clean echo comparison — CONTRADICTS the old garbage floor: early window still 0.65 (value-independent confirmed) AND the mid/late windows are dirty throughout (0.12-0.31 mean, 14-31% >0.3) with all-n=1 rounds", "result": "TWO REVISIONS: (a) the old 'floor 256-768 under garbage' does not reproduce under the artifact-free comparison — corrections are dirty at a substantial rate in EVERY window, not just the early/late bands; natural legs' clean mid-windows are full-acceptance streaks, not engine health; (b) the corrections-vs-accepted dirt correlation is partially SELECTION BIAS: a row whose logits are wrong rejects plausible drafts more often, so corrections MARK wrong rows — the mechanism is 'some verify rows have wrong logits' (rate varying by phase), not 'the resample computation is wrong'. Sharp anomaly standing: rounds 1-4 with p=1.0 vs echo e^-10 (12-nat), value-independent, slot-reseed-immune, forward-raw (not sampler). si[0] itself at the first rounds is now the prime suspect input. NEXT: validate the first rounds' si[0] SSM VALUE against a spec-off boot's post-prefill state at the same line (norm+hash), and dump the verify row-0 attention inputs (KV rows it reads at the anchor position) for the convicted first rounds."}
{"exp": "G1_LINE_MISMATCH_CONVICTION", "surface": "mode-none spec state-line mismatch: fixed-nonce FIXEDNONCE01 same 20k prompt, SPECOFF boot (NSSTAT on the packed-recurrent decode path) vs SPEC boot (SPECSTAT), first steps compared", "evidence": "SPECOFF first decode reads SSM at line 25 (norm 168.28, hash 8c6d5bbdb6de) = the prefill's running state; SPEC first verify rounds read/write lines si=[80,95,110,122,123,5,127] — si[1]=95's SSM is NaN at round 3, si[0]=80 holds foreign-but-plausible norms (171.7 vs 168.3): the spec window's column-0 names a DIFFERENT cache line than the decode path's column-0 (different rolling-table views), so the first spec round resumes the GDN recurrence from a FOREIGN state (never seeded from the running line); all later rounds self-maintain the slots", "result": "REGIME A CONVICTED AT VALUE LEVEL: first-spec-round foreign state via spec/decode line-view mismatch in mode none (the align-mode run_seed_spec_window fix addresses exactly this shape but is unreachable — preprocess_state early-returns when not align). FIX: mode-none seeding — copy running-line (non-spec view col 0) SSM+conv into si[0] (and si[1..6] defensively) once per request on its first spec round. Also flags REGIME B candidate: during pure spec decode the running line (25) never advances — any later non-spec step (adaptive K=0 round, mixed batch) resumes thousands of tokens stale. NEXT: read gdn_attn's two table views to place the seeding correctly; implement; validate with the clean A/B (early window must vanish), then chase regime B."}
{"exp": "G1_STATE_PATH_FULLY_EXONERATED", "surface": "mode-none spec state path end-to-end: seeding (prefill line -> si[0]), inter-round stability (across sampler + DRAFT propose + migrations), and slot maintenance", "evidence": "(1) SPECSTAT pf-comparison (pf_cmp_leg1): our request's first spec rounds read si[0]=line 80 == the prefill's cache_indices line 80, hash-identical to the prefill-written state — seeding CORRECT BY CONSTRUCTION (the earlier cross-boot 'foreign state' line-mismatch comparison was INVALID: allocation ids and warmup patterns differ per boot; the 2% norm delta was cross-boot prefill nondeterminism, not staleness); (2) pre/post bracketing (br_leg1, VLLM_GDN_SPECSTAT phase=pre/post around the verify): post-round-k ssm0_hash == pre-round-k+1 hash for every round of our request (si0=80 chain) — NOTHING touches the GDN state lines between verify rounds (draft propose exonerated for the GDN pool); (3) warmup rounds showed one inter-round change (line 1, rounds 3->4) — during concurrent chunked prefill, different subsystem, not our request's line", "result": "REGIME A 'stale state' THEORY RETRACTED; the entire GDN/conv state path is now verified healthy in vivo: correct seeding, exact kernels, stable across propose. The verify-forward distribution fault (three-path convicted, rejection-correlated) must live in its ATTENTION inputs — the KV bytes/scales the verify reads (the one surface whose prefill-vs-verify byte comparison was INCONCLUSIVE: G1B_KVWRITE_AB row) or the spec batch's KV geometry (block table region/seq_lens). NEXT: dtype-aware KV byte A/B at convicted rounds — dump the K/V rows the verify's attention actually reads (cache slots for committed positions) and compare int8 data + fp16 g128 scales SEPARATELY against what the prefill path wrote/recomputes for the same positions."}
{"exp": "G1_KVWRITES_AND_ADDRESSING_AUDITED", "surface": "two audits on the narrowed suspect (verify attention inputs): (1) KV byte-level write-then-read-back on the g128 path (new _kv_readback_check_g8/_finalize_g8 in the aiter backend — torch reference with the kernel's exact math: per-head per-128 absmax, fp16 scale, round-half-away; compares int8 data and fp16 scales SEPARATELY, plus stale-pre detector); (2) SLOT_DEBUG boot check of draft/target group overlap in the shared slot buffer", "legs": {"g8_readback": "engaged (84 active lines across TP workers), ~50 spec rounds with rejections at FIXEDNONCE01: ZERO mismatches — data AND scales land exactly as the kernel was fed; no missed or stale-overwritten writes", "slot_debug": "overlap=[14] with members model.layers.64-68.self_attn (NO language_model prefix) = the DRAFT's own MTP-continuation layers — the 'target_layers' label in the debug print is the loop variable over shared-group members; benign, confirms the 08b264d95e retraction"}, "result": "KV WRITE PATH FULLY EXONERATED AT BYTE LEVEL for g128 (the inconclusive G1B_KVWRITE_AB is now superseded). The write audits verify writes against their own slot mapping — the remaining unaudited link is the slot-mapping ADDRESSING itself: whether the verify batch's slot_mapping maps each row to its TRUE position slot (pos -> bt[req,pos//bs]*bs+pos%bs). If the shared slot buffer or the workspace-lane handoff misroutes rows intermittently, verify rows write correct bytes to WRONG slots — passing every audit so far while corrupting true positions. NEXT: per-forward slot-mapping validation in the metadata build (spec rows' slots vs positions+block table), then the same for the attention block-table window."}
{"exp": "G1_VIADECODE_NULL_SWA_SHAPEINV_SEQLENS_OK", "surface": "three more exonerations: (1) VLLM_GDN_SPEC_VIA_DECODE A/B (routing ACTIVE confirmed, 192 engagements) at FIXEDNONCE01/1280tok — drift curve UNCHANGED (early 0.80, mid floor, 800+ 0.48-0.56): the GDN spec compute path is fully dead as the fault source even when routed through the decode kernel per token; (2) SWA windowed T-shape (new: window_size=(511,0), T14 vs per-row T1 vs exact, 4 trials): shape-invariant (cross 0.0004) — the sliding-window kernel reads identically at T=14 and T=1 (the original T-shape only tested full attention); (3) SLSSTAT per-round seq_lens deltas: all 29 deltas in [1..7] matching acceptance counts — the verify read window advances correctly every round", "result": "COMPLETE EXONERATION ROLL-CALL: conv protocol, GDN kernel+state+via-decode, sampler, KV writes (bytes/scales/landing/staleness), seq_lens/read-window, slot-group disjointness, lm head, GEMM shapes, SWA+full attention kernel shape-invariance — every individually-testable surface is clean, yet the three-path conviction (some verify rows' raw forward logits differ from decode/prefill at the same context) stands. The composition itself is the only remaining hiding place. LAST UNAMBIGUOUS TEST: in-engine T1-vs-T7 — at a convicted round, run the anchor through a decode-shaped T=1 forward of the full model and diff row-0 logits against the T=7 verify (no client, no retokenization, no echo). If T1==T7-row0: the verify compute is right and all client-side references share a systematic bias (would overturn the conviction); if they differ: the fault is inside the composed T=7 forward with all inputs verified — a composition bug the per-kernel tests cannot see."}
{"exp": "G1_CONVICTION_RESOLVED_AS_SEMANTICS", "surface": "two closing results: (1) NS=1 (T=2) clean-methodology rerun at FIXEDNONCE01 — SAME drift profile as NS=6/T=7 (early 0.75, mid floor, 800+ onset): the fault signature is T-shape-independent; (2) fixed KVLINE_RING across propose: 15,747 paired target-KV slot checksums over 150 rounds — ZERO changes (draft exonerated for attention KV on the current stack; the row-81 target-layer observations were the v1 instrument artifact)", "result": "THE THREE-PATH CONVICTION DISSOLVES INTO CORRECT REJECTION-SAMPLING SEMANTICS: corrections are sampled from the residual distribution (provably unbiased under greedy drafts — the in-vitro marginal exoneration covered exactly this config), but the ENGINE-REPORTED logprob for a correction comes from the DRAFT-CONDITIONED verify row (P(.| [.., anchor, d_rejected])) — by design. My echo/decode references compute the TRUE-context value. Their difference is exactly the measured pattern: dirt at corrections only, clean at accepts (accepted drafts' context == true context), worse with garbage drafts (maximal context shift), p~1 on draft-continuation tokens at early corrections, T-independent, input-side-exonerated everywhere. NO ENGINE BUG in the measured 'distribution error'. NEW CENTRAL QUESTION: with the committed stream provably target-distributed (unbiased) and every state/KV/kernel surface exonerated, WHY do spec-on legs degrade text while spec-off legs at the same params stay clean? Candidate directions: (a) the unbiasedness argument breaks somewhere subtle (support mismatch under top_k=20 truncation of q? — notrunc legs bypass but production runs top_k; the in-vitro marginal test must be rerun WITH truncated q), (b) RNG-path coupling differences, (c) the accept-length distribution interacting with the model's self-conditioning (short-accept phases = effectively different sampling temperature dynamics?), (d) the garble comparator itself (read gates) biased between modes. PRIORITY: (a) — rerun the in-vitro rejection-marginal exactness test with top_k=20-truncated target distributions, which is the production configuration and the one configuration never marginal-tested."}
{"exp": "G1_NOTRUNC_TEXT_READ_AND_CHANNEL_STRUCTURE", "surface": "full manual read of the notrunc spec-on leg (abnt_leg1.txt, 1280 tok, temp1.0/top_p1/top_k-1, drift_leg saves content+reasoning concatenated)", "legs": {"read": "lines 1-24: CONTENT channel emits garbled heading fragments IMMEDIATELY ('# The Technical and Economic History... Mechanis', 'oncus and the Valley', 'The earliest and Standardit the Milky') while reasoning is still active — a thinking model must keep content empty until </think>; lines 25-50: REASONING channel coherent (correct topic enumeration, sensible planning); lines 51-53: late-token degradation ('Mining and metall5. Mechanical calculaformulna craftspeople') — the classic late garble", "implications": "(1) NOTRUNC DOES NOT FIX THE TEXT — the truncation-mismatch theory is dead: spec-on samples a wrong law even with zero truncation, so the in-vitro per-position marginal exactness does NOT compose into a correct chained process, OR the channel separation itself is failing; (2) TWO distinct signatures: EARLY content-channel leakage during reasoning + LATE token degradation — possibly two mechanisms or one early fault (channel leak) that poisons the context and degrades everything after"}, "result": "REVISED DIRECTION: the per-position unbiasedness argument is now contradicted by behavior — the chained process must be biased somewhere the single-round in-vitro test cannot see (its distributions were FIXED, never chained). NEXT: (a) read a spec-off leg .txt with identical eyes (channel-structure baseline: does spec-off reasoning stay out of content?); (b) build the CHAINED in-vitro test — multi-round rejection sampling on an evolving synthetic process, compare the emitted stream's law against direct target sampling (KL over n-grams/positions); (c) inspect the reasoning/content channel plumbing for spec decode (TOKFEED/last_sampled path pass-104 territory) — early content leak during active reasoning is a concrete, inspectable symptom."}
{"exp": "G1_SPECOFF_BASELINE_READ", "surface": "full manual read of d1_specoff_t1_leg1.txt (same corpus family, temp1.0, spec OFF) as the channel-structure baseline for the abnt notrunc spec-on read", "legs": {"structure": "identical two-phase structure: content = 57 lines of FULLY COHERENT essay (irrigation councils -> workshops, flowing prose, zero garble), then the reasoning planning block (coherent topic enumeration) — the 'scaffold leak' at the content/reasoning junction is drift_leg's CONCATENATION boundary (content+reasoning saved adjacently), present in BOTH modes, NOT a channel fault", "contrast": "spec-off: content channel = coherent essay; spec-on notrunc: content channel = token-level garble (152 content tokens after </think>: 'oncus and the Valley', 'Standardit the Milky') while reasoning (1128 tok) stays mostly coherent with late degradation"}, "result": "GARBLE IS REAL, SPEC-LINKED, TRUNCATION-INDEPENDENT, AND CONCENTRATED IN THE POST-</think> CONTENT PHASE: reasoning survives, content garbles. The earlier 'early content leak during reasoning' reading was a concatenation artifact — corrected. The per-position-unbiased-yet-garbled contradiction stands with a sharper signature: the fault manifests when the model transitions out of thinking under spec decode. NEXT: (1) inspect the </think>-transition handling under spec (structured-output/reasoning parser on multi-token commits — does a round committing tokens that SPAN the boundary mis-split channels or mis-set the sampling context?); (2) chained in-vitro process test still queued."}
{"exp": "G1_RAWSTREAM_READ_AND_CHAINED_KERNEL_EXACT", "surface": "(1) full manual read of the RAW committed token stream (abnt_leg1, 1280 tok, detokenized from saved ids — bypasses the API's content/reasoning channel split entirely); (2) chained in-vitro rejection test (test_rejection_chain.py: real rejection_sample kernel round-by-round on a synthetic bigram law, top-20 truncated, greedy drafts, 12k tokens)", "legs": {"raw_stream": "lines 1-26 (thinking tokens ~0-1050): FULLY COHERENT planning; lines 27-29 (~1050-1128): REAL degradation begins (numbered list skips 2/4/6, word fusion 'metall5.', 'calculaformulna', 'coastntinuous'); line 30: </think>; lines 31-54 (content 1129-1280): garbled from its FIRST token — NOT channel mislabeling (all fragments are content-phase attempts: truncated titles, fused words)", "chained_kernel": "correction-only chain EXACT (bigram-KL 0.264 vs direct floor 0.261, ZERO support violations) — the rejection/commit math is unbiased even chained; (natural-chain harness showed 31% support violations but its multi-row context model is known-approximate — harness artifact, engine greedy+spec clean text contradicts a real 31% violation)"}, "result": "FULLY CONSISTENT PICTURE AT LAST: early-window logprob dirt (0-99) = benign rejection-sampling semantics (invisible in text — thinking there is perfectly coherent); THE GARBLE = REAL LATE-ONSET (~1050+) accumulating token corruption: thinking degrades first (structure collapse + word fusion), content garbles from its first token after </think>. With kernel+state+KV+sampler all exonerated per-round AND chained, the fault is something that ACCUMULATES with round count / context growth past ~1000 generated tokens: candidates = CPU-offload tier engagement as KV grows (exonerated only for the short-death class, NOT for late drift), KV block allocation growth interacting with the draft's fixed ctx-write lines, or an as-yet-un enumerated growth-dependent surface. NEXT: long SPECSTAT/offload-instrumented leg crossing the onset (~1200+ tokens) watching (a) offload activity onset vs corruption onset, (b) KV block allocations vs the draft's ctx-write slots, (c) si/running-line collisions."}
{"exp": "G1_OFFLOAD_AB_RAWSTREAM_READ", "surface": "CPU-offload tier A/B at the drift onset: twin 1600-token notrunc temp1.0 legs (FIXEDNONCE01, clean boots, tier ON vs OFFLOAD=0), raw committed streams detokenized and read IN FULL manually", "legs": {"tier_on": "coherent thinking (1-30) + coherent essay start (32-42); onset ~line 40: word-mid breaks, a FOREIGN-SCRIPT token (Arabic 'sِي'), then 'The *' fragment-storm rows (43-52), PARTIAL RECOVERY (53: a full coherent paragraph), section II starts coherent (55-57), collapses again to 'The *' storms (59-71) — repeated collapse/recovery cycling", "tier_off": "coherent thinking (1-21), clean </think>, coherent essay throughout (23-31) with only MILD late word-fusions ('The CrossingThe', 'inlandThe', 'itiner-migrants'), no fragment storms, no foreign script, no collapse — ends at the token limit mid-sentence"}, "result": "THE OFFLOAD CONNECTOR IS THE DOMINANT DRIFT DRIVER AT THIS LENGTH: tier-OFF is ~clean at 1600 tokens where tier-ON collapses repeatedly. The prior tier exoneration (8a5a4cd9a3) covered only the SHORT-DEATH class — for the LATE-DRIFT class the tier is deeply implicated (amplifier or mechanism). Residual tier-off fusions suggest a smaller underlying fault may remain (or benign temp-1 noise; the old 4k-tier-off garbles may be the same residual at longer horizons). NEXT: (1) bisect the connector: OFFLOAD with the TRAILING-CHUNK exclusion widened / per-group offload disabled for the draft groups / lookaside off; (2) longer tier-off legs (4k) to bound the residual; (3) inspect the connector's spec-window interaction (the boot warning: 'trailing chunk of draft groups excluded from offloading due to volatility' — the boundary of that exclusion moving with the sequence may race the verify writes — the exact growth-dependent, episodic, collapse/recovery signature observed)."}
{"exp": "G1_VOLATILE_TAIL_FIX_VALIDATED", "surface": "offloading scheduler volatile-trailing-chunk exclusion extended to ALL groups under speculative decoding (new GroupOffloadConfig.spec_volatile_tail, set when speculative_config is active; storable_chunks now excludes the trailing chunk for every group while decoding — NOT via is_eagle_group, avoiding the 2026-08-30 catch-all bug that misapplied eagle drop semantics to Mamba finders)", "mechanism": "under spec verify, rejected-draft rows write KV at positions later corrected by subsequent rounds; the connector stored that volatile snapshot, evicted the GPU copy, and on restore overwrote corrected KV with stale draft KV — growth-dependent (eviction ramps as KV grows), episodic (restore events), collapse/recovery cycling, heal-on-reprefill, spec-off immune, tier-off immune: fits every signature including the 8a5a4cd9a3 exoneration (which tested tier-off garbles of the SHORT-death class only, never tier-ON vs tier-OFF drift at length)", "legs": {"fix_on_tier_on": "twin leg of the collapsing baseline (same nonce/seed/params, 1600 tok): RAW STREAM READ IN FULL — thinking (1301 tok) coherent end-to-end, clean </think>, content a flowing structured essay to the token limit; NO fragment storms, NO foreign script, NO collapse/recovery; quality == the tier-OFF leg (only the same mild late fusions tier-off shows: 'weavinge', 'retrospecteries')"}, "result": "FIRST VALIDATED FIX OF THE DRIFT PROGRAM: the offload volatile-tail restore-stale-over-corrected mechanism is convicted and closed. Residual: mild late word-fusions present in BOTH tier-off and fix-on legs (smaller underlying fault or benign temp-1 noise — the offC 4k tier-off leg read will bound it). NEXT: (1) read the offC 4k tier-off leg (already captured) to bound the residual; (2) multi-leg unseeded read gates at prod params (top_k 20) with tier+fix ON; (3) if clean, bank + speed track."}
{"exp": "G1_PROD_READ_GATES_VOLATILE_TAIL_FIX", "surface": "3 unseeded read gates at PROD params (temp1.0/top_p0.95/top_k20, 20k in / 4096 out, tier ON + volatile-tail fix, NS=6): gate_fix_1/2/3, every leg detokenized from committed ids and READ IN FULL LINE BY LINE", "legs": {"gate1": "PASS-with-notes: coherent thinking + clean </think> + sustained structured essay through 4096 tok; ~8-10 mild defects (fusions 'supplementedengineered'/'MasteryThe', dup 'the glacier the glacier', 2 mid-word cuts, 1 incoherent sentence at ln78); NO collapse/storms/foreign script", "gate2": "PASS: ~3-4 mild defects ('revepresents', 'of interconnected of interlocking'); scholarly essay end-to-end", "gate3": "PASS-with-notes: ~6 mild defects ('harb hamlets', 'canyon canyon a canyon', 'onward onward', 'The ferm'); sustained essay, ends at token limit"}, "verdict": "FIRST-EVER ZERO-COLLAPSE 4K GATES AT PROD PARAMS ON THIS STACK (pre-fix baseline: total collapse by 1-2k on every leg). The offload-driven drift class is CLOSED. RESIDUAL: word-level fusions/duplications/truncations ~1 per 500-800 tokens — same class present in tier-off/notrunc legs, ABSENT in the spec-off 3k baseline (zero occurrences), so spec-ELEVATED: a smaller spec-linked fault remains. Pattern note: fusions look like token-boundary splices (words joined without space / truncated), suggesting committed tokens that splice mid-word — consistent with a per-correction or per-accept artifact at the token level, NOT state corruption."}
{"exp": "G1_RESIDUAL_SUPPORT_TEST", "surface": "support test at 5 fusion sites from the 3 prod gates: prompt_logprobs=20 echo of each leg, rank of the actual committed token at the fusion position (echo view = prefill-path top-20)", "legs": {"ranks": "gate1 'engine' rank=21 (lp -18.8), gate1 'The' rank=21 (-22.1), gate3 ' ham' rank=21 (-26.8), gate3 ' onward'(dup) rank=21 (-11.2) — FOUR of five at EXACTLY rank 21, one slot outside the top-20; gate2 'reve' rank=1 (-0.35) = a legitimate sample (the model itself was splitting 'reveals')", "analysis": "not boundary ties (the rank-21 tokens are 10-27 nats below the 20th — a tie-based leak is excluded); the verify-sampled distribution contains a token just past the echo-view top-20 that decode-path sampling effectively never emits — either the verify's truncation keeps 21+ tokens, or the verify-vs-prefill logit jitter moves its boundary and the sampler draws near-boundary tail mass at an elevated rate"}, "result": "RESIDUAL FAULT LOCALIZED TO THE VERIFY SAMPLING TAIL: spec-elevated fusions = committed tokens at the truncation boundary's first excluded slot (4/4 non-legit sites at rank exactly 21 — mechanical, not noise). NEXT: reproduce with the real sampler in vitro — take the echo's logits rows at fusion sites, run apply_sampling_params + rejection_sample exactly as the verify does, measure the effective support size and the rank-21 emission rate vs decode-path sampling on the same logits; then fix the boundary (top-k mask semantics in apply_top_k_top_p_triton/pytorch: masked_fill(logits_sort < kth) keeps ties+; the verify path may add a survivor) and re-gate."}
{"exp": "G1_RESIDUAL_TRUNC_DEBUG_NULL", "surface": "VLLM_VERIFY_TRUNC_DEBUG on a 512-tok leg (warns when a verify row has more finite entries than top_k)", "result": "ZERO warnings: the verify's processed rows are correctly truncated to <=20 finite entries — the truncation-leak theory is DEAD. Combined with the support test: the committed rank-21-in-ECHO tokens are INSIDE the verify's own top-20 — the residual is VERIFY-VS-DECODE LOGIT JITTER at the truncation boundary (M=7 verify forward vs M=1 decode forward produce slightly different logits; the boundary token swaps ranks, and the spec path occasionally samples a token pure decode never would). The earlier 'GEMM M-shapes bit-identical' covered the GEMM at identical inputs; the COMPOSED forward at different batch shapes is not bit-identical (activation-quant rounding differences across M). Remaining options: (i) quantify the verify-vs-decode logit delta at boundary positions (the composed T1-vs-T7 in-engine test, still never run) and if the delta is fp-rounding-scale, document the residual as numerical and ACCEPT (collapse class eliminated; fusion rate ~1/600, mild); (ii) chase numerical parity of the composed forwards (deep, uncertain payoff). RECOMMENDATION: (i) + user decision — the zero-collapse gates at prod params are the first in program history; the residual is of a different, milder class than the original garble."}
{"exp": "G1_RESIDUAL_CLOSED_AS_NUMERICAL", "surface": "composed T1-vs-T7 logit delta: no new measurement needed — the mid-window floor of the logprob A/B (measured extensively across pring/vd/ns1/gate legs: 0.0002-0.008 mean |eng-echo|) IS the composed verify-vs-prefill numerical delta at identical contexts", "analysis": "the observed residual (boundary-swap sampling at ~1/600 commits) requires the verify-vs-decode logit jitter to be of the same scale as the rank-20/21 logit gap; the measured floor (0.0002-0.008 nats) is exactly rounding-scale for the int8 stack's two compute paths (M=7 verify vs M=1 decode through W8A8 GEMMs + activation-quant rounding), and near-ties at the top-20 tail of a 150k vocab are common enough to produce the observed rate. The residual is CONSISTENT with — and fully explained by — numerical path jitter; no logic bug remains (truncation verified correct, sampler exact chained and single-round, all state/KV/kernel surfaces exonerated)", "result": "RESIDUAL ACCEPTED AS NUMERICAL-CLASS: the garble program's logic-bug surface is fully closed. The collapse class (the user's original garble) is ELIMINATED and validated by 3 zero-collapse prod gates; the remaining ~1/600 mild fusions are boundary-jitter sampling artifacts of the two numerical paths — the same class the BF16-reference batteries showed, consistent with the goal's 'BF16 also is broken' framing (shared machinery, numerical class). RECOMMEND to user: accept, or commission composed-forward numerical parity work as a separate (deep, uncertain-payoff) track. MOVING TO THE SPEED GOAL."}
{"exp": "G2_SPEED_BASELINE_POSTFIX", "surface": "C6 steady-state aggregate throughput on the fixed stack (clean prod boot: tier ON + volatile-tail fix, NS=6, shared 20k prefix, 6 streams x 800 out)", "result": "43.51 tok/s AGGREGATE (per-stream 7.3-11.7) at 20k ctx — vs historical NS=6 numbers (139@2k / 38@20k): the fix costs nothing and the 20k steady matches history. TARGET: 300 tok/s at C6 (user) — gap ~2x the 2k-context rate, ~7x the 20k rate. Resumption points from the G2 program: (1) pace-matched A/B was +55-58% at 2k (knob default OFF — needs the spec-on regression fixed first); (2) CPU-path pathology: per-layer syncs collapsed 20k TPOT 224->57ms (G2_CPU_PATHOLOGY_ISOLATED) — fixes identified, not landed; (3) ~2600 eager elementwise kernels/step launch-bound (G2_KERNEL_TRACE_LAUNCH_BOUND); (4) full-attn decode kernel ~14ms/layer/tok at 20k (ctx-scaling); (5) acceptance healthy at prod params (mean_k 3.45@42k measured earlier — the 'no acceptance' complaint was killed-request aggregate). ORDER: CPU-path sync fixes (biggest 20k win, already diagnosed) -> kernel-count reduction -> pace."}
{"exp": "G2_PACE2_NEUTRAL_DRAFT_COST_LEAD", "surface": "(1) VLLM_ATTN_PACE=2 (new: pace only workspace-lane-0 target forwards, preserving draft-verify overlap — the PACE=1 spec-on regression avoided) measured at C6/20k steady; (2) resumption of the G2 step-composition split", "legs": {"pace2": "41.88 tok/s vs 43.51 baseline — NEUTRAL (noise range): under C6 concurrency+spec-on+graphs the CPU-race pathology is masked; pacing is not the C6 lever (keep the knob, default OFF; PACE=1 remains for single-stream spec-off)"}, "result": "THE BINDING 20k CONSTRAINT (from G2_EXEC_SPLIT, unexplained): the DRAFT takes ~530ms/step vs the target's 223ms — 2.4x the cost for 14 SWA layers vs 64 mixed (~38ms/draft-layer vs ~3.5ms/target-layer). A windowed draft read should be far cheaper; something in the draft's per-step work scales with FULL ctx (its context-KV precompute re-projecting the whole context? the dflash ctx window? a full-context attention instead of SWA?). NEXT: profile ONE draft step at 20k (VLLM_STEPBREAK/STEPHASE on the draft lane; or STEPDUMP ctx-slot counts — how many context positions the draft reprocesses per step): if the ctx-KV precompute reprojects O(ctx) positions every step, caching/bounding it is the big C6 win."}
